# nl-rag distilled facts — domain: suffix-array-index
# one declarative sentence per line; load with:  neuron --db app.db import pack.facts --scope knowledge
[src:suffix-array-index/burrows-wheeler-transform] The **Burrows–Wheeler transform** (**BWT**) rearranges a character string into runs of similar characters, in a manner that can be reversed to recover the original string.
[src:suffix-array-index/burrows-wheeler-transform] Since compression techniques such as move-to-front transform and run-length encoding are more effective when such runs are present, the BWT can be used as a preparatory step to improve the efficiency of a compression algorithm, and is used this way in software such as bzip2.
[src:suffix-array-index/burrows-wheeler-transform] The algorithm can be implemented efficiently using a suffix array thus reaching linear time complexity.
[src:suffix-array-index/burrows-wheeler-transform] It was invented by David Wheeler in 1983, and later published by him and Michael Burrows in 1994.
[src:suffix-array-index/burrows-wheeler-transform] Their paper included a compression algorithm, called the **Block-sorting Lossless Data Compression Algorithm** or **BSLDCA**, that compresses data by using the BWT followed by move-to-front coding and Huffman coding or arithmetic coding.
[src:suffix-array-index/burrows-wheeler-transform] The transform is done by constructing a matrix (known as the Burrows–Wheeler matrix) whose rows are the circular shifts of the input text, sorted in lexicographic order, then taking the final column of that matrix.
[src:suffix-array-index/burrows-wheeler-transform] The output of the encoding phase is the last column `L = BNN^AA$A` after step 3, and the index (0-based) `I` of the row containing the original string `S`, in this case `I = 6`.
[src:suffix-array-index/burrows-wheeler-transform] It is not necessary to use both `$` and `^`, but at least one must be used, else we cannot invert the transform, since all circular permutations of a string have the same Burrows–Wheeler transform.
[src:suffix-array-index/burrows-wheeler-transform] The following pseudocode gives a simple (though inefficient) way to calculate the BWT and its inverse.
[src:suffix-array-index/burrows-wheeler-transform] It assumes that the input string `s` contains a special character "EOF", which is the last character and occurs nowhere else in the text.
[src:suffix-array-index/burrows-wheeler-transform] If the original string had several substrings that occurred often, then the BWT-transformed string will have several places where a single character is repeated many times in a row, creating more-easily-compressible data.
[src:suffix-array-index/burrows-wheeler-transform] Similarly, rotations beginning with "e " are grouped together, but "e " is often preceded by "h", so we see the output above contains a run of five consecutive "h" characters.
[src:suffix-array-index/burrows-wheeler-transform] Thus it can be seen that the success of this transform depends upon one value having a high probability of occurring before a sequence, so that in general it needs fairly long samples (a few kilobytes at least) of appropriate data (such as text).
[src:suffix-array-index/burrows-wheeler-transform] The remarkable thing about the BWT is not that it generates a more easily encoded output—an ordinary sort would do that—but that it does this *reversibly*, allowing the original document to be re-generated from the last column data.
[src:suffix-array-index/burrows-wheeler-transform] Take the final table in the BWT algorithm, and erase all but the last column.
[src:suffix-array-index/burrows-wheeler-transform] Given only this information, you can easily reconstruct the first column.
[src:suffix-array-index/burrows-wheeler-transform] The last column tells you all the characters in the text, so just sort these characters alphabetically to get the first column.
[src:suffix-array-index/burrows-wheeler-transform] Then, the last and first columns (of each row) together give you all *pairs* of successive characters in the document, where pairs are taken cyclically so that the last and first character form a pair.
[src:suffix-array-index/burrows-wheeler-transform] Sorting the list of pairs gives the first *and second* columns.
[src:suffix-array-index/burrows-wheeler-transform] To obtain the third column, the last column is again prepended to the table, and the rows are sorted lexicographically.
[src:suffix-array-index/burrows-wheeler-transform] Continuing in this manner, you can reconstruct the entire list.
[src:suffix-array-index/burrows-wheeler-transform] Then, the row with the "end of file" character at the end is the original text.
[src:suffix-array-index/burrows-wheeler-transform] A number of optimizations can make these algorithms run more efficiently without changing the output.
[src:suffix-array-index/burrows-wheeler-transform] There is no need to represent the table in either the encoder or decoder.
[src:suffix-array-index/burrows-wheeler-transform] In the encoder, each row of the table can be represented by a single pointer into the strings, and the sort performed using the indices.
[src:suffix-array-index/burrows-wheeler-transform] In the decoder, there is also no need to store the table, and the decoded string can be generated one character at a time from left to right.
[src:suffix-array-index/burrows-wheeler-transform] Comparative sorting can even be avoided in favor of linear sorting, with performance proportional to the alphabet size and string length.
[src:suffix-array-index/burrows-wheeler-transform] A "character" in the algorithm can be a byte, or a bit, or any other convenient size.
[src:suffix-array-index/burrows-wheeler-transform] One may also make the observation that mathematically, the encoded string can be computed as a simple modification of the suffix array, and suffix arrays can be computed with linear time and memory.
[src:suffix-array-index/burrows-wheeler-transform] There is no need to have an actual 'EOF' character.
[src:suffix-array-index/burrows-wheeler-transform] Instead, a pointer can be used that remembers where in a string the 'EOF' would be if it existed.
[src:suffix-array-index/burrows-wheeler-transform] In this approach, the output of the BWT must include both the transformed string, and the final value of the pointer.
[src:suffix-array-index/burrows-wheeler-transform] The inverse transform then shrinks it back down to the original size: it is given a string and a pointer, and returns just a string.
[src:suffix-array-index/burrows-wheeler-transform] A complete description of the algorithms can be found in Burrows and Wheeler's paper, or in a number of online sources.
[src:suffix-array-index/burrows-wheeler-transform] The algorithms vary somewhat by whether EOF is used, and in which direction the sorting was done.
[src:suffix-array-index/burrows-wheeler-transform] In fact, the original formulation did not use an EOF marker.
[src:suffix-array-index/burrows-wheeler-transform] Since any rotation of the input string will lead to the same transformed string, the BWT cannot be inverted without adding an EOF marker to the end of the input or doing something equivalent, making it possible to distinguish the input string from all its rotations.
[src:suffix-array-index/burrows-wheeler-transform] Increasing the size of the alphabet (by appending the EOF character) makes later compression steps awkward.
[src:suffix-array-index/burrows-wheeler-transform] There is a bijective version of the transform, by which the transformed string uniquely identifies the original, and the two have the same length and contain exactly the same characters, just in a different order.
[src:suffix-array-index/burrows-wheeler-transform] The bijective transform is computed by factoring the input into a non-increasing sequence of Lyndon words; such a factorization exists and is unique by the Chen–Fox–Lyndon theorem, and may be found in linear time and constant space.
[src:suffix-array-index/burrows-wheeler-transform] The algorithm sorts the rotations of all the words; as in the Burrows–Wheeler transform, this produces a sorted sequence of *n* strings.
[src:suffix-array-index/burrows-wheeler-transform] The transformed string is then obtained by picking the final character of each string in this sorted list.
[src:suffix-array-index/burrows-wheeler-transform] The one important caveat here is that strings of different lengths are not ordered in the usual way; the two strings are repeated forever, and the infinite repeats are sorted.
[src:suffix-array-index/burrows-wheeler-transform] For example, "ORO" precedes "OR" because "OROORO..." precedes "OROROR...".
[src:suffix-array-index/burrows-wheeler-transform] For example, the text "^BANANA$" is transformed into "ANNBAA^$" through these steps (the red $ character indicates the EOF pointer) in the original string.
[src:suffix-array-index/burrows-wheeler-transform] The EOF character is unneeded in the bijective transform, so it is dropped during the transform and re-added to its proper place in the file.
[src:suffix-array-index/burrows-wheeler-transform] The string is broken into Lyndon words so the words in the sequence are decreasing using the comparison method above.
[src:suffix-array-index/burrows-wheeler-transform] (Note that we're sorting '^' as succeeding other characters.) "^BANANA" becomes (^) (B) (AN) (AN) (A).
[src:suffix-array-index/burrows-wheeler-transform] Up until the last step, the process is identical to the inverse Burrows–Wheeler process, but here it will not necessarily give rotations of a single sequence; it instead gives rotations of Lyndon words (which will start to repeat as the process is continued).
[src:suffix-array-index/burrows-wheeler-transform] Here, we can see (repetitions of) four distinct Lyndon words: (A), (AN) (twice), (B), and (^).
[src:suffix-array-index/burrows-wheeler-transform] (NANA... doesn't represent a distinct word, as it is a cycle of ANAN....) At this point, these words are sorted into reverse order: (^), (B), (AN), (AN), (A).
[src:suffix-array-index/burrows-wheeler-transform] The whole string is now a Lyndon word, and running it through the bijective process will therefore result in a transformed result that, when inverted, gives back the Lyndon word, with no need for reassembling at the end.
[src:suffix-array-index/burrows-wheeler-transform] The bijective transform includes eight runs of identical characters.
[src:suffix-array-index/burrows-wheeler-transform] In total, 18 characters are used in these runs.
[src:suffix-array-index/burrows-wheeler-transform] When a text is edited, its Burrows–Wheeler transform will change.
[src:suffix-array-index/burrows-wheeler-transform] This Python implementation sacrifices speed for simplicity: the program is short, but takes more than the linear time that would be desired in a practical implementation.
[src:suffix-array-index/burrows-wheeler-transform] It essentially does what the pseudocode section does.
[src:suffix-array-index/burrows-wheeler-transform] The inverse transform repeatedly inserts `r` as the left column of the table and sorts the table.
[src:suffix-array-index/burrows-wheeler-transform] After the whole table is built, it returns the row that ends with ETX, minus the STX and ETX.
[src:suffix-array-index/burrows-wheeler-transform] Following implementation notes from Manzini, it is equivalent to use a simple null character suffix instead.
[src:suffix-array-index/burrows-wheeler-transform] The sorting should be done in colexicographic order (string read right-to-left), i.e.
[src:suffix-array-index/burrows-wheeler-transform] (The above control codes actually fail to satisfy EOF being the last character; the two codes are actually the *first*.
[src:suffix-array-index/burrows-wheeler-transform] As a lossless compression algorithm the Burrows–Wheeler transform offers the important quality that its encoding is reversible and hence the original data may be recovered from the resulting compression.
[src:suffix-array-index/burrows-wheeler-transform] The lossless quality of Burrows algorithm has provided for different algorithms with different purposes in mind.
[src:suffix-array-index/burrows-wheeler-transform] To name a few, Burrows–Wheeler transform is used in algorithms for sequence alignment, image compression, data compression, etc.
[src:suffix-array-index/burrows-wheeler-transform] The following is a compilation of some uses given to the Burrows–Wheeler Transform.
[src:suffix-array-index/burrows-wheeler-transform] The advent of next-generation sequencing (NGS) techniques at the end of the 2000s decade has led to another application of the Burrows–Wheeler transformation.
[src:suffix-array-index/burrows-wheeler-transform] In NGS, DNA is fragmented into small pieces, of which the first few bases are sequenced, yielding several millions of "reads", each 30 to 500 base pairs ("DNA characters") long.
[src:suffix-array-index/burrows-wheeler-transform] In many experiments, e.g., in ChIP-Seq, the task is now to align these reads to a reference genome, i.e., to the known, nearly complete sequence of the organism in question (which may be up to several billion base pairs long).
[src:suffix-array-index/burrows-wheeler-transform] A number of alignment programs, specialized for this task, were published, which initially relied on hashing (e.g., Eland, SOAP, or Maq).
[src:suffix-array-index/burrows-wheeler-transform] In an effort to reduce the memory requirement for sequence alignment, several alignment programs were developed (Bowtie, BWA, and SOAP2) that use the Burrows–Wheeler transform.
[src:suffix-array-index/burrows-wheeler-transform] The Burrows–Wheeler transformation has proved to be fundamental for image compression applications.
[src:suffix-array-index/burrows-wheeler-transform] For example, Showed a compression pipeline based on the application of the Burrows–Wheeler transformation followed by inversion, run-length, and arithmetic encoders.
[src:suffix-array-index/burrows-wheeler-transform] The pipeline developed in this case is known as Burrows–Wheeler transform with an inversion encoder (BWIC).
[src:suffix-array-index/burrows-wheeler-transform] The results shown by BWIC are shown to outperform the compression performance of well-known and widely used algorithms like Lossless JPEG and JPEG 2000.
[src:suffix-array-index/burrows-wheeler-transform] BWIC is shown to outperform those in terms of final compression size of radiography medical images on the order of 5.1% and 4.1% respectively.
[src:suffix-array-index/burrows-wheeler-transform] The improvements are achieved by combining BWIC and a pre-BWIC scan of the image in a vertical snake order fashion.
[src:suffix-array-index/burrows-wheeler-transform] More recently, additional works have shown the implementation of the Burrows–Wheeler Transform in conjunction with the known move-to-front transform (MTF) achieve near lossless compression of images.
[src:suffix-array-index/burrows-wheeler-transform] Cox et al. presented a genomic compression scheme that uses BWT as the algorithm applied during the first stage of compression of several genomic datasets including the human genomic information.
[src:suffix-array-index/burrows-wheeler-transform] Their work proposed that BWT compression could be enhanced by including a second stage compression mechanism called same-as-previous encoding ("SAP"), which makes use of the fact that suffixes of two or more prefix letters could be equal.
[src:suffix-array-index/burrows-wheeler-transform] With the compression mechanism BWT-SAP, Cox et al. showed that in the genomic database ERA015743, 135.5 GB in size, the compression scheme BWT-SAP compresses the ERA015743 dataset by around 94%, to 8.2 GB.
[src:suffix-array-index/burrows-wheeler-transform] BWT has also been proved to be useful on sequence prediction which is a common area of study in machine learning and natural-language processing.
[src:suffix-array-index/burrows-wheeler-transform] In particular, Ktistakis et al. proposed a sequence prediction scheme called SuBSeq that exploits the lossless compression of data of the Burrows–Wheeler transform.
[src:suffix-array-index/burrows-wheeler-transform] SuBSeq exploits BWT by extracting the FM-index and then performing a series of operations called backwardSearch, forwardSearch, neighbourExpansion, and getConsequents in order to search for predictions given a suffix.
[src:suffix-array-index/burrows-wheeler-transform] The predictions are then classified based on a weight and put into an array from which the element with the highest weight is given as the prediction from the SuBSeq algorithm.
[src:suffix-array-index/burrows-wheeler-transform] SuBSeq has been shown to outperform state of the art algorithms for sequence prediction both in terms of training time and accuracy.
[src:suffix-array-index/fm-index] In computer science, an **FM-index** is a compressed full-text substring index based on the Burrows–Wheeler transform, with some similarities to the suffix array.
[src:suffix-array-index/fm-index] It was created by Paolo Ferragina and Giovanni Manzini, who describe it as an opportunistic data structure as it allows compression of the input text while still permitting fast substring queries.
[src:suffix-array-index/fm-index] The name stands for Full-text index in Minute space.
[src:suffix-array-index/fm-index] It can be used to efficiently find the number of occurrences of a pattern within the compressed text, as well as locate the position of each occurrence.
[src:suffix-array-index/fm-index] The query time, as well as the required storage space, has a sublinear complexity with respect to the size of the input data.
[src:suffix-array-index/fm-index] The original authors have devised improvements to their original approach and dubbed it "FM-Index version 2".
[src:suffix-array-index/fm-index] A further improvement, the alphabet-friendly FM-index, combines the use of compression boosting and wavelet trees to significantly reduce the space usage for large alphabets.
[src:suffix-array-index/fm-index] The FM-index has found use in, among other places, bioinformatics.
[src:suffix-array-index/fm-index] Using an index is a common strategy to efficiently search a large body of text.
[src:suffix-array-index/fm-index] When the text is larger than what reasonably fits within a computer's main memory, there is a need to compress not only the text but also the index.
[src:suffix-array-index/fm-index] When the FM-index was introduced, there were several suggested solutions that were based on traditional compression methods and tried to solve the compressed matching problem.
[src:suffix-array-index/fm-index] In contrast, the FM-index is a compressed self-index, which means that it compresses the data and indexes it at the same time.
[src:suffix-array-index/fm-index] An FM-index is created by first taking the Burrows–Wheeler transform (BWT) of the input text.
[src:suffix-array-index/fm-index] For example, the BWT of the string T = "abracadabra$" is "ard$rcaaaabb", and here it is represented by the matrix M where each row is a rotation of the text, and the rows have been sorted lexicographically.
[src:suffix-array-index/fm-index] The transform corresponds to the concatenation of the characters from the last column (labeled L).
[src:suffix-array-index/fm-index] The BWT in itself allows for some compression with, for instance, move to front and Huffman encoding, but the transform has even more uses.
[src:suffix-array-index/fm-index] The rows in the matrix are essentially the sorted suffixes of the text and the first column F of the matrix shares similarities with suffix arrays.
[src:suffix-array-index/fm-index] How the suffix array relates to the BWT lies at the heart of the FM-index.
[src:suffix-array-index/fm-index] It is possible to make a last-to-first column mapping LF(i) from an index i to an index j, such that F[j] = L[i], with the help of a table C[c] and a function Occ(c, k).
[src:suffix-array-index/fm-index] The last-to-first mapping can now be defined as LF(i) = C[L[i]] + Occ(L[i], i).
[src:suffix-array-index/fm-index] For instance, on row 9, L is a and the same a can be found on row 5 in the first column F, so LF(9) should be 5 and LF(9) = C[a] + Occ(a, 9) = 5.
[src:suffix-array-index/fm-index] For any row i of the matrix, the character in the last column L[i] precedes the character in the first column F[i] also in T.
[src:suffix-array-index/fm-index] Finally, if L[i] = T[k], then L[LF(i)] = T[k - 1], and using the equality it is possible to extract a string of T from L.
[src:suffix-array-index/fm-index] The FM-index itself is a compression of the string L together with C and Occ in some form, as well as information that maps a selection of indices in L to positions in the original string T.
[src:suffix-array-index/fm-index] The operation *count* takes a pattern P[1..p] and returns the number of occurrences of that pattern in the original text T.
[src:suffix-array-index/fm-index] Since the rows of matrix M are sorted, and it contains every suffix of T, the occurrences of pattern P will be next to each other in a single continuous range.
[src:suffix-array-index/fm-index] The operation iterates backwards over the pattern.
[src:suffix-array-index/fm-index] For every character in the pattern, the range that has the character as a suffix is found.
[src:suffix-array-index/fm-index] The new range is [C[r] + Occ(r, start-1) + 1 ..
[src:suffix-array-index/fm-index] 10 + 2] = [11..12], if start is the index of the beginning of the range and end is the end.
[src:suffix-array-index/fm-index] This range over L is all the characters of T that have suffixes beginning with *ra*.
[src:suffix-array-index/fm-index] The new range is [C[b] + Occ(b, start-1) + 1 ..
[src:suffix-array-index/fm-index] This range over L is all the characters that have a suffix that begins with *bra*.
[src:suffix-array-index/fm-index] Now that the whole pattern has been processed, the count is the same as the size of the range: 8 - 7 + 1 = 2.
[src:suffix-array-index/fm-index] If the range becomes empty or the range boundaries cross each other before the whole pattern has been looked up, the pattern does not occur in T.
[src:suffix-array-index/fm-index] Because Occ(c, k) can be performed in constant time, count can complete in linear time in the length of the pattern: O(p) time.
[src:suffix-array-index/fm-index] The operation *locate* takes as input an index of a character in L and returns its position i in T.
[src:suffix-array-index/fm-index] To locate every occurrence of a pattern, first the range of character is found whose suffix is the pattern in the same way the *count* operation found the range.
[src:suffix-array-index/fm-index] Then the position of every character in the range can be located.
[src:suffix-array-index/fm-index] To map an index in L to one in T, a subset of the indices in L are associated with a position in T.
[src:suffix-array-index/fm-index] If L[j] has a position associated with it, locate(j) is trivial.
[src:suffix-array-index/fm-index] If it's not associated, the string is followed with LF(i) until an associated index is found.
[src:suffix-array-index/fm-index] By associating a suitable number of indices, an upper bound can be found.
[src:suffix-array-index/fm-index] *Locate* can be implemented to find *occ* occurrences of a pattern P[1..*p*] in a text T[1..*u*] in O(*p* + *occ* logε *u*) time with $O\left(H_{k}(T)+{{\log \log u} \over {\log ^{\epsilon }u}}\right)$ bits per input symbol for any *k* ≥ 0.
[src:suffix-array-index/fm-index] The FM-Index is available in multiple languages, including C++, Java and Rust.
[src:suffix-array-index/lcp-array] In computer science, the **longest common prefix array** (**LCP array**) is an auxiliary data structure to the suffix array.
[src:suffix-array-index/lcp-array] It stores the lengths of the longest common prefixes (LCPs) between all pairs of consecutive suffixes in a sorted suffix array.
[src:suffix-array-index/lcp-array] For example, if *A* := [aab, ab, abaab, b, baab] is a suffix array, the longest common prefix between *A*[1] = aab and *A*[2] = ab is *a* which has length 1, so *H*[2] = 1 in the LCP array *H*.
[src:suffix-array-index/lcp-array] Augmenting the suffix array with the LCP array allows one to efficiently simulate top-down and bottom-up traversals of the suffix tree, speeds up pattern matching on the suffix array and is a prerequisite for compressed suffix trees.
[src:suffix-array-index/lcp-array] The LCP array was introduced in 1993, by Udi Manber and Gene Myers alongside the suffix array in order to improve the running time of their string search algorithm.
[src:suffix-array-index/lcp-array] Let A be the suffix array of the string $S=s_{1},s_{2},\ldots s_{n-1}\$$ of length n , where $\$$ is a sentinel letter that is unique and lexicographically smaller than any other character.
[src:suffix-array-index/lcp-array] Let $S[i,j]$ denote the substring of S ranging from i to j .
[src:suffix-array-index/lcp-array] Thus, $S[A[i],n]$ is the i th smallest suffix of S .
[src:suffix-array-index/lcp-array] Let $\operatorname {lcp} (v,w)$ denote the length of the longest common prefix between two strings v and w .
[src:suffix-array-index/lcp-array] Then the LCP array $H[1,n]$ is an integer array of size n such that $H[1]$ is undefined and $H[i]=\operatorname {lcp} (S[A[i-1],n],S[A[i],n])$ for every $1<i\leq n$ .
[src:suffix-array-index/lcp-array] Thus $H[i]$ stores the length of longest common prefix of the lexicographically i th smallest suffix and its predecessor in the suffix array.
[src:suffix-array-index/lcp-array] Note that $H[1]$ is undefined, since there is no lexicographically smaller suffix.
[src:suffix-array-index/lcp-array] LCP array construction algorithms can be divided into two different categories: algorithms that compute the LCP array as a byproduct to the suffix array and algorithms that use an already constructed suffix array in order to compute the LCP values.
[src:suffix-array-index/lcp-array] Manber & Myers (1993) provide an algorithm to compute the LCP array alongside the suffix array in $O(n\log n)$ time.
[src:suffix-array-index/lcp-array] Kärkkäinen & Sanders (2003) show that it is also possible to modify their $O(n)$ time algorithm such that it computes the LCP array as well.
[src:suffix-array-index/lcp-array] (2001) present the first $O(n)$ time algorithm (FLAAP) that computes the LCP array given the text and the suffix array.
[src:suffix-array-index/lcp-array] Assuming that each text symbol takes one byte and each entry of the suffix or LCP array takes 4 bytes, the major drawback of their algorithm is a large space occupancy of $13n$ bytes, while the original output (text, suffix array, LCP array) only occupies $9n$ bytes.
[src:suffix-array-index/lcp-array] Therefore, Manzini (2004) created a refined version of the algorithm of Kasai et al.
[src:suffix-array-index/lcp-array] (2001) (lcp9) and reduced the space occupancy to $9n$ bytes.
[src:suffix-array-index/lcp-array] Kärkkäinen, Manzini & Puglisi (2009) provide another refinement of Kasai's algorithm ( $\Phi$ -algorithm) that improves the running time.
[src:suffix-array-index/lcp-array] Rather than the actual LCP array, this algorithm builds the *permuted* LCP (PLCP) array, in which the values appear in text order rather than lexicographical order.
[src:suffix-array-index/lcp-array] Gog & Ohlebusch (2011) provide two algorithms that although being theoretically slow ( $O(n^{2})$ ) were faster than the above-mentioned algorithms in practice.
[src:suffix-array-index/lcp-array] As of 2012, the currently fastest linear-time LCP array construction algorithm is due to Fischer (2011), which in turn is based on one of the fastest suffix array construction algorithms (SA-IS) by Nong, Zhang & Chan (2009).
[src:suffix-array-index/lcp-array] Fischer & Kurpicz (2017) based on Yuta Mori's DivSufSort is even faster.
[src:suffix-array-index/lcp-array] (2001) show how to simulate a bottom-up traversal of the suffix tree using only the suffix array and LCP array.
[src:suffix-array-index/lcp-array] Abouelhoda, Kurtz & Ohlebusch (2004) enhance the suffix array with the LCP array and additional data structures and describe how this *enhanced suffix array* can be used to simulate *all three kinds* of suffix tree traversals.
[src:suffix-array-index/lcp-array] Fischer & Heun (2007) reduce the space requirements of the enhanced suffix array by preprocessing the LCP array for range minimum queries.
[src:suffix-array-index/lcp-array] Thus, *every* problem that can be solved by suffix tree algorithms can also be solved using the *enhanced suffix array*.
[src:suffix-array-index/lcp-array] Deciding if a pattern P of length m is a substring of a string S of length n takes $O(m\log n)$ time if only the suffix array is used.
[src:suffix-array-index/lcp-array] By additionally using the LCP information, this bound can be improved to $O(m+\log n)$ time.
[src:suffix-array-index/lcp-array] Abouelhoda, Kurtz & Ohlebusch (2004) show how to improve this running time even further to achieve optimal $O(m)$ time.
[src:suffix-array-index/lcp-array] Thus, using suffix array and LCP array information, the decision query can be answered as fast as using the suffix tree.
[src:suffix-array-index/lcp-array] The LCP array is also an essential part of compressed suffix trees which provide full suffix tree functionality like suffix links and lowest common ancestor queries.
[src:suffix-array-index/lcp-array] Furthermore, it can be used together with the suffix array to compute the Lempel-Ziv LZ77 factorization in $O(n)$ time.
[src:suffix-array-index/lcp-array] The longest repeated substring problem for a string S of length n can be solved in $\Theta (n)$ time using both the suffix array A and the LCP array.
[src:suffix-array-index/lcp-array] It is sufficient to perform a linear scan through the LCP array in order to find its maximum value $v_{max}$ and the corresponding index i where $v_{max}$ is stored.
[src:suffix-array-index/lcp-array] The longest substring that occurs at least twice is then given by $S[A[i],A[i]+v_{max}-1]$ .
[src:suffix-array-index/lcp-array] The issue with using standard binary search (without the LCP information) is that in each of the $O(\log N)$ comparisons needed to be made, we compare P to the current entry of the suffix array, which means a full string comparison of up to m characters.
[src:suffix-array-index/lcp-array] At any point during the binary search algorithm, we consider, as usual, a range $(L,\dots ,R)$ of the suffix array and its central point M , and decide whether we continue our search in the left sub-range $(L,\dots ,M)$ or in the right sub-range $(M,\dots ,R)$ .
[src:suffix-array-index/lcp-array] In order to make the decision, we compare P to the string at M .
[src:suffix-array-index/lcp-array] If P is identical to M , our search is complete.
[src:suffix-array-index/lcp-array] But if not, we have already compared the first k characters of P and then decided whether P is lexicographically smaller or larger than M .
[src:suffix-array-index/lcp-array] Let's assume the outcome is that P is larger than M .
[src:suffix-array-index/lcp-array] The trick now is that LCP-LR is precomputed such that an $O(1)$ -lookup tells us the longest common prefix of M and $M'$ , $\mathrm {lcp} (M,M')$ .
[src:suffix-array-index/lcp-array] We already know (from the previous step) that M itself has a prefix of k characters in common with P : $\mathrm {lcp} (P,M)=k$ .
[src:suffix-array-index/lcp-array] The overall effect is that no character of P is compared to any character of the text more than once (for details see ).
[src:suffix-array-index/lcp-array] The total number of character comparisons is bounded by m , so the total complexity is indeed $O(m+\log N)$ .
[src:suffix-array-index/lcp-array] We still need to precompute LCP-LR so it is able to tell us in $O(1)$ time the lcp between any two entries of the suffix array.
[src:suffix-array-index/lcp-array] We know the standard LCP array gives us the lcp of consecutive entries only, i.e. $\mathrm {lcp} (i-1,i)$ for any i .
[src:suffix-array-index/lcp-array] However, M and $M'$ in the description above are not necessarily consecutive entries.
[src:suffix-array-index/lcp-array] The key to this is to realize that only certain ranges $(L,\dots ,R)$ will ever occur during the binary search: It always starts with $(0,\dots ,N)$ and divides that at the center, and then continues either left or right and divide that half again and so forth.
[src:suffix-array-index/lcp-array] Another way of looking at it is : every entry of the suffix array occurs as central point of exactly one possible range during binary search.
[src:suffix-array-index/lcp-array] So there are exactly N distinct ranges $(L\dots M\dots R)$ that can possibly play a role during binary search, and it suffices to precompute $\mathrm {lcp} (L,M)$ and $\mathrm {lcp} (M,R)$ for those N possible ranges.
[src:suffix-array-index/lcp-array] So that is $2N$ distinct precomputed values, hence LCP-LR is $O(N)$ in size.
[src:suffix-array-index/lcp-array] Moreover, there is a straightforward recursive algorithm to compute the $2N$ values of LCP-LR in $O(N)$ time from the standard LCP array.
[src:suffix-array-index/lcp-array] Let $ST_{i}$ be the partial suffix tree for $0\leq i\leq n$ .
[src:suffix-array-index/lcp-array] Further let $d(v)$ be the length of the concatenation of all path labels from the root of $ST_{i}$ to node v .
[src:suffix-array-index/lcp-array] Start with $ST_{0}$ , the tree consisting only of the root.
[src:suffix-array-index/lcp-array] To insert $A[i+1]$ into $ST_{i}$ , walk up the *rightmost* path beginning at the recently inserted leaf $A[i]$ to the root, until the deepest node v with $d(v)\leq H[i+1]$ is reached.
[src:suffix-array-index/lcp-array] Add a new internal node y and a new edge $(v,y)$ with label $S[A[i]+d(v),A[i]+H[i+1]-1]$ .
[src:suffix-array-index/lcp-array] The new label consists of the *missing* characters of the longest common prefix of $A[i]$ and $A[i+1]$ .
[src:suffix-array-index/lcp-array] Thus, the concatenation of the labels of the root-to- y path now displays the longest common prefix of $A[i]$ and $A[i+1]$ .
[src:suffix-array-index/lcp-array] Connect w to the newly created internal node y by an edge $(y,w)$ that is labeled $S[A[i]+H[i+1],A[i]+d(w)-1]$ .
[src:suffix-array-index/lcp-array] The new label consists of the *remaining* characters of the deleted edge $(v,w)$ that were not used as the label of edge $(v,y)$ .
[src:suffix-array-index/lcp-array] Add $A[i+1]$ as a new leaf x and connect it to the new internal node y by an edge $(y,x)$ that is labeled $S[A[i+1]+H[i+1],n]$ .
[src:suffix-array-index/lcp-array] Thus the edge label consists of the remaining characters of suffix $A[i+1]$ that are not already represented by the concatenation of the labels of the root-to- v path.
[src:suffix-array-index/lcp-array] This creates the partial suffix tree $ST_{i+1}$ .
[src:suffix-array-index/lcp-array] The nodes that are traversed in step i by walking up the *rightmost* path of $ST_{i}$ (apart from the last node v ) are removed from the *rightmost* path, when $A[i+1]$ is added to the tree as a new leaf.
[src:suffix-array-index/lcp-array] These nodes will never be traversed again for all subsequent steps $j>i$ .
[src:suffix-array-index/lcp-array] Therefore, at most $2n$ nodes will be traversed in total.
[src:suffix-array-index/lcp-array] The LCP array H only contains the length of the longest common prefix of every pair of consecutive suffixes in the suffix array A .
[src:suffix-array-index/lcp-array] Because of the lexicographic order of the suffix array, every common prefix of the suffixes $S[i,n]$ and $S[j,n]$ has to be a common prefix of all suffixes between i 's position in the suffix array $A^{-1}[i]$ and j 's position in the suffix array $A^{-1}[j]$ .
[src:suffix-array-index/lcp-array] Therefore, the length of the longest prefix that is shared by *all* of these suffixes is the minimum value in the interval $H[A^{-1}[i]+1,A^{-1}[j]]$ .
[src:suffix-array-index/lcp-array] This value can be found in constant time if H is preprocessed for range minimum queries.
[src:suffix-array-index/lcp-array] Thus given a string S of length n and two arbitrary positions $i,j$ in the string S with $A^{-1}[i]<A^{-1}[j]$ , the length of the longest common prefix of the suffixes $S[i,n]$ and $S[j,n]$ can be computed as follows: $\operatorname {LCP} (i,j)=H[\operatorname {RMQ} _{H}(A^{-1}[i]+1,A^{-1}[j])]$ .
[src:suffix-array-index/suffix-array] In computer science, a **suffix array** is a sorted array of all suffixes of a string.
[src:suffix-array-index/suffix-array] It is a data structure used in, among others, full-text indices, data-compression algorithms, and the field of bibliometrics.
[src:suffix-array-index/suffix-array] Suffix arrays were introduced by Manber & Myers (1990) as a simple, space efficient alternative to suffix trees.
[src:suffix-array-index/suffix-array] They had independently been discovered by Gaston Gonnet in 1987 under the name *PAT array* (Gonnet, Baeza-Yates & Snider 1992).
[src:suffix-array-index/suffix-array] Li, Li & Huo (2016) gave the first in-place ${\mathcal {O}}(n)$ time suffix array construction algorithm that is optimal both in time and space, where *in-place* means that the algorithm only needs ${\mathcal {O}}(1)$ additional space beyond the input string and the output suffix array.
[src:suffix-array-index/suffix-array] Enhanced suffix arrays (ESAs) are suffix arrays with additional tables that reproduce the full functionality of suffix trees preserving the same time and memory complexity.
[src:suffix-array-index/suffix-array] A sorted array of only some (rather than all) suffixes of a string is called a sparse suffix array.
[src:suffix-array-index/suffix-array] Let $S=S[1]S[2]...S[n]$ be an ${\textstyle n}$ -string and let $S[i,j]$ denote the substring of S ranging from i to j inclusive.
[src:suffix-array-index/suffix-array] The suffix array A of S is now defined to be an array of integers providing the starting positions of suffixes of S in lexicographical order.
[src:suffix-array-index/suffix-array] This means, an entry $A[i]$ contains the starting position of the i -th smallest suffix in S and thus for all $1\leq i\leq n$ : $S[A[i-1],n]<S[A[i],n]$ .
[src:suffix-array-index/suffix-array] Each suffix of S shows up in A exactly once.
[src:suffix-array-index/suffix-array] These strings are sorted (as in a paper dictionary), before their starting positions (integer indices) are saved in A .
[src:suffix-array-index/suffix-array] The text ends with the special sentinel letter `$` that is unique and lexicographically smaller than any other character.
[src:suffix-array-index/suffix-array] So for example, $A[3]$ contains the value 4, and therefore refers to the suffix starting at position 4 within S , which is the suffix `ana$`.
[src:suffix-array-index/suffix-array] It has been shown that every suffix tree algorithm can be systematically replaced with an algorithm that uses a suffix array enhanced with additional information (such as the LCP array) and solves the same problem in the same time complexity.
[src:suffix-array-index/suffix-array] Advantages of suffix arrays over suffix trees include improved space requirements, simpler linear time construction algorithms (e.g., compared to Ukkonen's algorithm) and improved cache locality.
[src:suffix-array-index/suffix-array] Suffix arrays were introduced by Manber & Myers (1990) in order to improve over the space requirements of suffix trees: Suffix arrays store n integers.
[src:suffix-array-index/suffix-array] Assuming an integer requires 4 bytes, a suffix array requires $4n$ bytes in total.
[src:suffix-array-index/suffix-array] This is significantly less than the $20n$ bytes which are required by a careful suffix tree implementation.
[src:suffix-array-index/suffix-array] However, in certain applications, the space requirements of suffix arrays may still be prohibitive.
[src:suffix-array-index/suffix-array] Analyzed in bits, a suffix array requires ${\mathcal {O}}(n\log n)$ space, whereas the original text over an alphabet of size $\sigma$ only requires ${\mathcal {O}}(n\log \sigma )$ bits.
[src:suffix-array-index/suffix-array] For a human genome with $\sigma =4$ and $n=3.4\times 10^{9}$ the suffix array would therefore occupy about 16 times more memory than the genome itself.
[src:suffix-array-index/suffix-array] Such discrepancies motivated a trend towards compressed suffix arrays and BWT-based compressed full-text indices such as the FM-index.
[src:suffix-array-index/suffix-array] These data structures require only space within the size of the text or even less.
[src:suffix-array-index/suffix-array] A suffix tree can be built in ${\mathcal {O}}(n)$ and can be converted into a suffix array by traversing the tree depth-first also in ${\mathcal {O}}(n)$ , so there exist algorithms that can build a suffix array in ${\mathcal {O}}(n)$ .
[src:suffix-array-index/suffix-array] A naive approach to construct a suffix array is to use a comparison-based sorting algorithm.
[src:suffix-array-index/suffix-array] These algorithms require ${\mathcal {O}}(n\log n)$ suffix comparisons, but a suffix comparison runs in ${\mathcal {O}}(n)$ time, so the overall runtime of this approach is ${\mathcal {O}}(n^{2}\log n)$ .
[src:suffix-array-index/suffix-array] More advanced algorithms take advantage of the fact that the suffixes to be sorted are not arbitrary strings but related to each other.
[src:suffix-array-index/suffix-array] One of the first algorithms to achieve all goals is the SA-IS algorithm of Nong, Zhang & Chan (2009).
[src:suffix-array-index/suffix-array] The algorithm is also rather simple (< 100 LOC) and can be enhanced to simultaneously construct the LCP array.
[src:suffix-array-index/suffix-array] The SA-IS algorithm is one of the fastest known suffix array construction algorithms.
[src:suffix-array-index/suffix-array] A careful implementation by Yuta Mori outperforms most other linear or super-linear construction approaches.
[src:suffix-array-index/suffix-array] A well-known recursive algorithm for integer alphabets is the *DC3 / skew* algorithm of Kärkkäinen & Sanders (2003).
[src:suffix-array-index/suffix-array] It runs in linear time and has successfully been used as the basis for parallel and external memory suffix array construction algorithms.
[src:suffix-array-index/suffix-array] (2010) proposes an algorithm for updating the suffix array of a text that has been edited instead of rebuilding a new suffix array from scratch.
[src:suffix-array-index/suffix-array] In practical open source work, a commonly used routine for suffix array construction was qsufsort, based on the 1999 Larsson-Sadakane algorithm.
[src:suffix-array-index/suffix-array] This routine has been superseded by Yuta Mori's DivSufSort, "the fastest known suffix sorting algorithm in main memory" as of 2017.
[src:suffix-array-index/suffix-array] It too can be modified to compute an LCP array.
[src:suffix-array-index/suffix-array] It uses a induced copying combined with Itoh-Tanaka.
[src:suffix-array-index/suffix-array] In 2021 a faster implementation of the algorithm was presented by Ilya Grebnov which in average showed 65% performance improvement over DivSufSort implementation on the Silesia corpus.
[src:suffix-array-index/suffix-array] The concept of a suffix array can be extended to more than one string.
[src:suffix-array-index/suffix-array] This is called a generalized suffix array (or GSA), a suffix array that contains all suffixes for a set of strings (for example, $S=S_{1},S_{2},S_{3},...,S_{k}$ and is lexicographically sorted with all suffixes of each string.
[src:suffix-array-index/suffix-array] The suffix array of a string can be used as an index to quickly locate every occurrence of a substring pattern P within the string S .
[src:suffix-array-index/suffix-array] Finding every occurrence of the pattern is equivalent to finding every suffix that begins with the substring.
[src:suffix-array-index/suffix-array] Thanks to the lexicographical ordering, these suffixes will be grouped together in the suffix array and can be found efficiently with two binary searches.
[src:suffix-array-index/suffix-array] Finding the substring pattern P of length m in the string S of length n takes ${\mathcal {O}}(m\log n)$ time, given that a single suffix comparison needs to compare m characters.
[src:suffix-array-index/suffix-array] Manber & Myers (1990) describe how this bound can be improved to ${\mathcal {O}}(m+\log n)$ time using LCP information.
[src:suffix-array-index/suffix-array] The idea is that a pattern comparison does not need to re-compare certain characters, when it is already known that these are part of the longest common prefix of the pattern and the current search interval.
[src:suffix-array-index/suffix-array] Abouelhoda, Kurtz & Ohlebusch (2004) improve the bound even further and achieve a search time of ${\mathcal {O}}(m)$ for constant alphabet size, as known from suffix trees.
[src:suffix-array-index/suffix-array] Suffix sorting algorithms can be used to compute the Burrows–Wheeler transform (BWT).
[src:suffix-array-index/suffix-array] The BWT requires sorting of all cyclic permutations of a string.
[src:suffix-array-index/suffix-array] If this string ends in a special end-of-string character that is lexicographically smaller than all other character (i.e., $), then the order of the sorted rotated BWT matrix corresponds to the order of suffixes in a suffix array.
[src:suffix-array-index/suffix-array] The BWT can therefore be computed in linear time by first constructing a suffix array of the text and then deducing the BWT string: $BWT[i]=S[A[i]-1]$ .
[src:suffix-array-index/suffix-array] Suffix arrays can also be used to look up substrings in example-based machine translation, demanding much less storage than a full phrase table as used in Statistical machine translation.
[src:suffix-array-index/suffix-array] Many additional applications of the suffix array require the LCP array.
[src:suffix-array-index/suffix-array] Some of these are detailed in the application section of the latter.
[src:suffix-array-index/suffix-array] Suffix trees are powerful data structures that have wide application in areas of pattern and string matching, indexing and textual statistics.
[src:suffix-array-index/suffix-array] However, it occupies a significant amount of space and thus has a drawback in many real-time applications that require processing a considerably large amount of data like genome analysis.
[src:suffix-array-index/suffix-array] To overcome this drawback, the Enhanced Suffix Array was developed.
[src:suffix-array-index/suffix-array] It is a data structure consisting of a suffix array and an additional table called the child table that contains the information about the parent-child relationship between the nodes in the suffix tree.
[src:suffix-array-index/suffix-array] The node branching data structure for this tree is a linked list.
[src:suffix-array-index/suffix-array] Enhanced suffix arrays are superior to suffix trees in terms of both space efficiency and time complexity and are easy to implement.
[src:suffix-array-index/suffix-array] Moreover, they can be applied to any algorithm that uses a suffix tree by using an abstract concept called LCP-interval trees.
[src:suffix-array-index/suffix-array] The time complexity for searching a pattern of length m in an enhanced suffix array is ${\mathcal {O}}(m|\Sigma |)$ .
[src:suffix-array-index/suffix-array] 2. lcp array lcp[1,...n]: It is an array of n integers that maintains the lengths of the longest common prefix of two consecutive suffixes stored in the pos array.
[src:suffix-array-index/suffix-array] The length of the longest common prefix of pos[i − 1] and pos[i] is stored in lcp[i],where 2 ≤ i ≤ n.
[src:suffix-array-index/suffix-array] The lcp-interval portrays the same parent-child relationship as that among the associated nodes in the suffix tree of S.This shows that if the corresponding node of [i..j] is a child of the corresponding node of [k..l], a lcp-interval [i..j] is a child interval of another lcp-interval [k..l].
[src:suffix-array-index/suffix-array] If [k..l] is a child interval of [i..j], a lcp-interval [i..j] is the parent interval of a lcp-interval [k..l].
[src:suffix-array-index/suffix-array] The child table *cldtab* is composed of three n arrays, *up*, *down* and *nextlIndex*.
[src:suffix-array-index/suffix-array] The information about the edges of the corresponding suffix tree is stored and maintained by the *up* and *down* arrays.
[src:suffix-array-index/suffix-array] The *nextlIndex* array stores the links in the linked list used for node branching the suffix tree.
[src:suffix-array-index/suffix-array] The initial index of the second child interval of the longest lcp-interval, starting at index *i* is stored in the element *down[i]*.
[src:suffix-array-index/suffix-array] If and only if the interval is neither the first child nor the final child of its parent, the element *nextlIndex[i]* contains the first index of the next sibling interval of the longest lcp-interval, starting at index *i*.
[src:suffix-array-index/suffix-array] By performing a bottom-up traversal of the lcp-interval of the tree, the child table can be constructed in linear time.
[src:suffix-array-index/suffix-array] The *up/down* values and the *nextlIndex* values can be computed separately by using two distinct algorithms.
[src:suffix-array-index/suffix-array] The suffix links for an enhanced suffix array can be computed by generating the suffix link interval [*1,..,r*] for each [i,..j] interval during the preprocessing.
[src:suffix-array-index/suffix-array] The left and right elements l and r of the interval are maintained in the first index of [i,..,j].
[src:suffix-array-index/suffix-array] The table for this interval ranges from 0 to n.
[src:suffix-array-index/suffix-array] The suffix link table is constructed by the left-to-right breadth-first traversal of the lcp-interval tree.
[src:suffix-array-index/suffix-array] Every time an *l*-interval is computed, it is added to the list of l-intervals, which is referred to as the l-list.
[src:suffix-array-index/suffix-array] When the lcp-value > 0, for every *l*-interval[i,..,j] in the list, link[i] is calculated.
[src:suffix-array-index/suffix-array] The interval [*l*,..,*r*] is computed by a binary search in(*l*-1)-list, where *l* is the largest left boundary amongst all the *l*-1 intervals.
[src:suffix-array-index/suffix-array] The suffix link interval of [i,..j] is represented by this interval[*l,..,r*].
[src:suffix-array-index/suffix-array] The values *l* and *r* are ultimately stored in the first index of [i,..,j].
[src:suffix-array-index/suffix-tree] In computer science, a **suffix tree** (also called **PAT tree** or, in an earlier form, **position tree**) is a compressed trie containing all the suffixes of the given text as their keys and positions in the text as their values.
[src:suffix-array-index/suffix-tree] Suffix trees allow particularly fast implementations of many important string operations.
[src:suffix-array-index/suffix-tree] The construction of such a tree for the string S takes time and space linear in the length of S .
[src:suffix-array-index/suffix-tree] Once constructed, several operations can be performed quickly, such as locating a substring in S , locating a substring if a certain number of mistakes are allowed, and locating matches for a regular expression pattern.
[src:suffix-array-index/suffix-tree] Suffix trees also provided one of the first linear-time solutions for the longest common substring problem.
[src:suffix-array-index/suffix-tree] These speedups come at a cost: storing a string's suffix tree typically requires significantly more space than storing the string itself.
[src:suffix-array-index/suffix-tree] The concept was first introduced by Weiner (1973).
[src:suffix-array-index/suffix-tree] Rather than the suffix $S[i..n]$ , Weiner stored in his trie the *prefix identifier* for each position, that is, the shortest string starting at i and occurring only once in S .
[src:suffix-array-index/suffix-tree] His *Algorithm D* takes an uncompressed trie for $S[k+1..n]$ and extends it into a trie for $S[k..n]$ .
[src:suffix-array-index/suffix-tree] This way, starting from the trivial trie for $S[n..n]$ , a trie for $S[1..n]$ can be built by $n-1$ successive calls to Algorithm D; however, the overall run time is $O(n^{2})$ .
[src:suffix-array-index/suffix-tree] Weiner's *Algorithm B* maintains several auxiliary data structures, to achieve an overall run time linear in the size of the constructed trie.
[src:suffix-array-index/suffix-tree] The latter can still be $O(n^{2})$ nodes, e.g. for $S=a^{n}b^{n}a^{n}b^{n}\$.$ Weiner's *Algorithm C* finally uses compressed tries to achieve linear overall storage size and run time.
[src:suffix-array-index/suffix-tree] Donald Knuth subsequently characterized the latter as "Algorithm of the Year 1973" according to his student Vaughan Pratt.
[src:suffix-array-index/suffix-tree] The text book Aho, Hopcroft & Ullman (1974, Sect.9.5) reproduced Weiner's results in a simplified and more elegant form, introducing the term *position tree*.
[src:suffix-array-index/suffix-tree] McCreight (1976) was the first to build a (compressed) trie of all suffixes of S .
[src:suffix-array-index/suffix-tree] Although the suffix starting at i is usually longer than the prefix identifier, their path representations in a compressed trie do not differ in size.
[src:suffix-array-index/suffix-tree] On the other hand, McCreight could dispense with most of Weiner's auxiliary data structures; only suffix links remained.
[src:suffix-array-index/suffix-tree] Ukkonen (1995) further simplified the construction.
[src:suffix-array-index/suffix-tree] He provided the first online-construction of suffix trees, now known as Ukkonen's algorithm, with running time that matched the then fastest algorithms.
[src:suffix-array-index/suffix-tree] These algorithms are all linear-time for a constant-size alphabet, and have worst-case running time of $O(n\log n)$ in general.
[src:suffix-array-index/suffix-tree] Farach (1997) gave the first suffix tree construction algorithm that is optimal for all alphabets.
[src:suffix-array-index/suffix-tree] In particular, this is the first linear-time algorithm for strings drawn from an alphabet of integers in a polynomial range.
[src:suffix-array-index/suffix-tree] Farach's algorithm has become the basis for new algorithms for constructing both suffix trees and suffix arrays, for example, in external memory, compressed, succinct, etc.
[src:suffix-array-index/suffix-tree] Except for the root, every internal node has at least two children.
[src:suffix-array-index/suffix-tree] Each edge is labelled with a non-empty substring of S .
[src:suffix-array-index/suffix-tree] No two edges starting out of a node can have string-labels beginning with the same character.
[src:suffix-array-index/suffix-tree] The string obtained by concatenating all the string-labels found on the path from the root to leaf i spells out suffix $S[i..n]$ , for i from 1 to n .
[src:suffix-array-index/suffix-tree] If a suffix of S is also the prefix of another suffix, such a tree does not exist for the string.
[src:suffix-array-index/suffix-tree] For example, in the string *abcbc*, the suffix *bc* is also a prefix of the suffix *bcbc*.
[src:suffix-array-index/suffix-tree] In such a case, the path spelling out *bc* will not end in a leaf, violating the fifth rule.
[src:suffix-array-index/suffix-tree] To fix this problem, S is padded with a terminal symbol not seen in the string (usually denoted `$`).
[src:suffix-array-index/suffix-tree] This ensures that no suffix is a prefix of another, and that there will be n leaf nodes, one for each of the n suffixes of S .
[src:suffix-array-index/suffix-tree] Since all internal non-root nodes are branching, there can be at most $n-1$ such nodes, and $n+(n-1)+1=2n$ nodes in total ( n leaves, $n-1$ internal non-root nodes, 1 root).
[src:suffix-array-index/suffix-tree] A generalized suffix tree is a suffix tree made for a set of strings instead of a single string.
[src:suffix-array-index/suffix-tree] It represents all suffixes from this set of strings.
[src:suffix-array-index/suffix-tree] Each string must be terminated by a different termination symbol.
[src:suffix-array-index/suffix-tree] A suffix tree for a string S of length n can be built in $\Theta (n)$ time, if the letters come from an alphabet of integers in a polynomial range (in particular, this is true for constant-sized alphabets).
[src:suffix-array-index/suffix-tree] For larger alphabets, the running time is dominated by first sorting the letters to bring them into a range of size $O(n)$ ; in general, this takes $O(n\log n)$ time.
[src:suffix-array-index/suffix-tree] The costs below are given under the assumption that the alphabet is constant.
[src:suffix-array-index/suffix-tree] Assume that a suffix tree has been built for the string S of length n , or that a generalised suffix tree has been built for the set of strings $D=\{S_{1},S_{2},\dots ,S_{K}\}$ of total length $n=n_{1}+n_{2}+\cdots +n_{K}$ .
[src:suffix-array-index/suffix-tree] The suffix tree can be prepared for constant time lowest common ancestor retrieval between nodes in $\Theta (n)$ time.
[src:suffix-array-index/suffix-tree] Suffix trees can be used to solve a large number of string problems that occur in text-editing, free-text search, computational biology and other application areas.
[src:suffix-array-index/suffix-tree] Suffix trees are often used in bioinformatics applications, searching for patterns in DNA or protein sequences (which can be viewed as long strings of characters).
[src:suffix-array-index/suffix-tree] The ability to search efficiently with mismatches might be considered their greatest strength.
[src:suffix-array-index/suffix-tree] Suffix trees are also used in data compression; they can be used to find repeated data, and can be used for the sorting stage of the Burrows–Wheeler transform.
[src:suffix-array-index/suffix-tree] Variants of the LZW compression schemes use suffix trees (LZSS).
[src:suffix-array-index/suffix-tree] A suffix tree is also used in suffix tree clustering, a data clustering algorithm used in some search engines.
[src:suffix-array-index/suffix-tree] If each node and edge can be represented in $\Theta (1)$ space, the entire tree can be represented in $\Theta (n)$ space.
[src:suffix-array-index/suffix-tree] The total length of all the strings on all of the edges in the tree is $O(n^{2})$ , but each edge can be stored as the position and length of a substring of S, giving a total space usage of $\Theta (n)$ computer words.
[src:suffix-array-index/suffix-tree] The worst-case space usage of a suffix tree is seen with a fibonacci word, giving the full $2n$ nodes.
[src:suffix-array-index/suffix-tree] An important choice when making a suffix tree implementation is the parent-child relationships between nodes.
[src:suffix-array-index/suffix-tree] The most common is using linked lists called **sibling lists**.
[src:suffix-array-index/suffix-tree] Each node has a pointer to its first child, and to the next node in the child list it is a part of.
[src:suffix-array-index/suffix-tree] Other implementations with efficient running time properties use hash maps, sorted or unsorted arrays (with array doubling), or balanced search trees.
[src:suffix-array-index/suffix-tree] The insertion cost is amortised, and that the costs for hashing are given for perfect hashing.
[src:suffix-array-index/suffix-tree] The large amount of information in each edge and node makes the suffix tree very expensive, consuming about 10 to 20 times the memory size of the source text in good implementations.
[src:suffix-array-index/suffix-tree] Researchers have continued to find smaller indexing structures.
[src:suffix-array-index/suffix-tree] Various parallel algorithms to speed up suffix tree construction have been proposed.
[src:suffix-array-index/suffix-tree] Recently, a practical parallel algorithm for suffix tree construction with $O(n)$ work (sequential time) and $O(\log ^{2}n)$ span has been developed.
[src:suffix-array-index/suffix-tree] The algorithm achieves good parallel scalability on shared-memory multicore machines and can index the human genome – approximately 3GB – in under 3 minutes using a 40-core machine.
[src:suffix-array-index/suffix-tree] Though linear, the memory usage of a suffix tree is significantly higher than the actual size of the sequence collection.
[src:suffix-array-index/suffix-tree] For a large text, construction may require external memory approaches.
[src:suffix-array-index/suffix-tree] There are theoretical results for constructing suffix trees in external memory.
[src:suffix-array-index/suffix-tree] The algorithm by Farach-Colton, Ferragina & Muthukrishnan (2000) is theoretically optimal, with an I/O complexity equal to that of sorting.
[src:suffix-array-index/suffix-tree] However the overall intricacy of this algorithm has prevented, so far, its practical implementation.
[src:suffix-array-index/suffix-tree] On the other hand, there have been practical works for constructing disk-based suffix trees which scale to (few) GB/hours.
[src:suffix-array-index/suffix-tree] The state of the art methods are TDD, TRELLIS, DiGeST, and B2ST.
[src:suffix-array-index/suffix-tree] TDD and TRELLIS scale up to the entire human genome resulting in a disk-based suffix tree of a size in the tens of gigabytes.
[src:suffix-array-index/suffix-tree] However, these methods cannot handle efficiently collections of sequences exceeding 3 GB.
[src:suffix-array-index/suffix-tree] DiGeST performs significantly better and is able to handle collections of sequences in the order of 6 GB in about 6 hours.
[src:suffix-array-index/suffix-tree] All these methods can efficiently build suffix trees for the case when the tree does not fit in main memory, but the input does.
[src:suffix-array-index/suffix-tree] The most recent method, B2ST, scales to handle inputs that do not fit in main memory.
[src:suffix-array-index/suffix-tree] ERA is a recent parallel suffix tree construction method that is significantly faster.
[src:suffix-array-index/suffix-tree] ERA can index the entire human genome in 19 minutes on an 8-core desktop computer with 16 GB RAM.
[src:suffix-array-index/suffix-tree] On a simple Linux cluster with 16 nodes (4 GB RAM per node), ERA can index the entire human genome in less than 9 minutes.
[src:suffix-array-index/wavelet-tree] The **wavelet tree** is a succinct data structure to store strings in compressed space.
[src:suffix-array-index/wavelet-tree] It generalizes the $\mathbf {rank} _{q}$ and $\mathbf {select} _{q}$ operations defined on bitvectors to arbitrary alphabets.
[src:suffix-array-index/wavelet-tree] Originally introduced to represent compressed suffix arrays, it has found application in several contexts.
[src:suffix-array-index/wavelet-tree] The tree is defined by recursively partitioning the alphabet into pairs of subsets; the leaves correspond to individual symbols of the alphabet, and at each node a bitvector stores whether a symbol of the string belongs to one subset or the other.
[src:suffix-array-index/wavelet-tree] The name derives from an analogy with the wavelet transform for signals, which recursively decomposes a signal into low-frequency and high-frequency components.
[src:suffix-array-index/wavelet-tree] Let $\Sigma$ be a finite alphabet with $\sigma ={|\Sigma |}$ .
[src:suffix-array-index/wavelet-tree] By using succinct dictionaries in the nodes, a string $s\in \Sigma ^{*}$ can be stored in ${|s|}H_{0}(s)+o({|s|}\log \sigma )$ , where $H_{0}(s)$ is the order-0 empirical entropy of s .
[src:suffix-array-index/wavelet-tree] If the tree is balanced, the operations $\mathbf {access}$ , $\mathbf {rank} _{q}$ , and $\mathbf {select} _{q}$ can be supported in $O(\log \sigma )$ time.
[src:suffix-array-index/wavelet-tree] A wavelet tree contains a bitmap representation of a string.
[src:suffix-array-index/wavelet-tree] If we know the alphabet set, then the exact string can be inferred by tracking bits down the tree.
[src:suffix-array-index/wavelet-tree] In this context, the rank of a position i in a bitvector b is the number of ones that appear in the first i positions of b .
[src:suffix-array-index/wavelet-tree] Because the rank can be calculated in O(1) by using succinct dictionaries, any S[i] in string S can be accessed in $O(\log \sigma )$ time, as long as the tree is balanced.
[src:suffix-array-index/wavelet-tree] Several extensions to the basic structure have been presented in the literature.
[src:suffix-array-index/wavelet-tree] To reduce the height of the tree, multiary nodes can be used instead of binary.
[src:suffix-array-index/wavelet-tree] The data structure can be made dynamic, supporting insertions and deletions at arbitrary points of the string; this feature enables the implementation of dynamic FM-indexes.
[src:suffix-array-index/wavelet-tree] This can be further generalized, allowing the update operations to change the underlying alphabet: the Wavelet Trie exploits the trie structure on an alphabet of strings to enable dynamic tree modifications.
