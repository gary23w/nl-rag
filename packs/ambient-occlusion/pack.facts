# nl-rag distilled facts — domain: ambient-occlusion
# one declarative sentence per line; load with:  neuron --db app.db import pack.facts --scope knowledge
[src:ambient-occlusion/ambient-occlusion] In 3D computer graphics, modeling, and animation, **ambient occlusion** is a shading and rendering technique used to calculate how exposed each point in a scene is to ambient lighting.
[src:ambient-occlusion/ambient-occlusion] For example, the interior of a tube is typically more occluded (and hence darker) than the exposed outer surfaces, and becomes darker the deeper inside the tube one goes.
[src:ambient-occlusion/ambient-occlusion] Ambient occlusion can be seen as an accessibility value that is calculated for each surface point.
[src:ambient-occlusion/ambient-occlusion] In scenes with open sky this is done by estimating the amount of visible sky for each point, while in indoor environments only objects within a certain radius are taken into account and the walls are assumed to be the origin of the ambient light.
[src:ambient-occlusion/ambient-occlusion] The result is a diffuse, non-directional shading effect that casts no clear shadows, but that darkens enclosed and sheltered areas and can affect the rendered image's overall tone.
[src:ambient-occlusion/ambient-occlusion] It is often used as a post-processing effect.
[src:ambient-occlusion/ambient-occlusion] Unlike local methods such as Phong shading, ambient occlusion is a global method, meaning that the illumination at each point is a function of other geometry in the scene.
[src:ambient-occlusion/ambient-occlusion] However, it is a very crude approximation to full global illumination.
[src:ambient-occlusion/ambient-occlusion] The appearance achieved by ambient occlusion alone is similar to the way an object might appear on an overcast day.
[src:ambient-occlusion/ambient-occlusion] The first method that allowed simulating ambient occlusion in real time was developed by the research and development department of Crytek (CryEngine 2).
[src:ambient-occlusion/ambient-occlusion] With the release of hardware capable of real time ray tracing (GeForce 20 series) by Nvidia in 2018, ray traced ambient occlusion (RTAO) became possible in games and other real time applications.
[src:ambient-occlusion/ambient-occlusion] This feature was added to the Unreal Engine with version 4.22.
[src:ambient-occlusion/ambient-occlusion] Ambient occlusion is related to accessibility shading, which determines appearance based on how easy it is for a surface to be touched by various elements (e.g., dirt, light, etc.).
[src:ambient-occlusion/ambient-occlusion] It has been popularized in production animation due to its relative simplicity and efficiency.
[src:ambient-occlusion/ambient-occlusion] The ambient occlusion shading model offers a better perception of the 3D shape of the displayed objects.
[src:ambient-occlusion/ambient-occlusion] This was shown in a paper where the authors report the results of perceptual experiments showing that depth discrimination under diffuse uniform sky lighting is superior to that predicted by a direct lighting model.
[src:ambient-occlusion/ambient-occlusion] A variety of techniques are used to approximate this integral in practice: perhaps the most straightforward way is to use the Monte Carlo method by casting rays from the point ${\bar {p}}$ and testing for intersection with other scene geometry (i.e., ray casting).
[src:ambient-occlusion/ambient-occlusion] Another approach (more suited to hardware acceleration) is to render the view from ${\bar {p}}$ by rasterizing black geometry against a white background and taking the (cosine-weighted) average of rasterized fragments.
[src:ambient-occlusion/ambient-occlusion] This approach is an example of a "gathering" or "inside-out" approach, whereas other algorithms (such as depth-map ambient occlusion) employ "scattering" or "outside-in" techniques.
[src:ambient-occlusion/ambient-occlusion] In addition to the ambient occlusion value, a "bent normal" vector ${\hat {n}}_{b}$ is often generated, which points in the average direction of occluded samples.
[src:ambient-occlusion/ambient-occlusion] The bent normal can be used to look up incident radiance from an environment map to approximate image-based lighting.
[src:ambient-occlusion/ambient-occlusion] In this example, light may reach the point p only from the left or right sides, but the bent normal points to the average of those two sources, which is directly toward the obstruction.
[src:ambient-occlusion/ambient-occlusion] In 2010, Hayden Landis, Ken McGaugh and Hilmar Koch were awarded a Scientific and Technical Academy Award for their work on ambient occlusion rendering.
[src:ambient-occlusion/depth-map] In 3D computer graphics and computer vision, a **depth map** is an image or image channel that contains information relating to the distance of the surfaces of scene objects from a viewpoint.
[src:ambient-occlusion/depth-map] The term is related (and may be analogous) to *depth buffer*, *Z-buffer*, *Z-buffering*, and *Z-depth*.
[src:ambient-occlusion/depth-map] The "Z" in these latter terms relates to a convention that the central axis of view of a camera is in the direction of the camera's Z axis, and not to the absolute Z axis of a scene.
[src:ambient-occlusion/depth-map] Two different depth maps can be seen here, together with the original model from which they are derived.
[src:ambient-occlusion/depth-map] The first depth map shows luminance in proportion to the distance from the camera.
[src:ambient-occlusion/depth-map] Nearer surfaces are darker; further surfaces are lighter.
[src:ambient-occlusion/depth-map] The second depth map shows luminance in relation to the distances from a nominal focal plane.
[src:ambient-occlusion/depth-map] Surfaces closer to the focal plane are darker; surfaces further from the focal plane are lighter, (both closer to and also further away from the viewpoint).
[src:ambient-occlusion/global-illumination] Areas that lie outside of the ceiling lamp's direct light lack definition.
[src:ambient-occlusion/global-illumination] For example, the lamp's housing appears completely uniform.
[src:ambient-occlusion/global-illumination] Without the ambient light added into the render, it would appear uniformly black.
[src:ambient-occlusion/global-illumination] Light is reflected by surfaces, and colored light transfers from one surface to another.
[src:ambient-occlusion/global-illumination] Notice how color from the red wall and green wall (not visible) reflects onto other surfaces in the scene.
[src:ambient-occlusion/global-illumination] projected onto the red wall from light passing through the glass sphere.
[src:ambient-occlusion/global-illumination] Theoretically, reflections, refractions, transparency, and shadows are all examples of global illumination, because when simulating them, one object affects the rendering of another (as opposed to an object being affected only by a direct source of light).
[src:ambient-occlusion/global-illumination] In practice, however, only the simulation of diffuse inter-reflection or caustics is called global illumination, especially in real-time settings.
[src:ambient-occlusion/global-illumination] Global illumination is a key aspect to the realism of a 3D scene.
[src:ambient-occlusion/global-illumination] Naive 3D lighting will only take into account direct light, meaning any light which radiates off a light source and bounces directly into the virtual camera.
[src:ambient-occlusion/global-illumination] Shadows will appear completely dark, due to light not interacting with any other surface before it reaches the camera.
[src:ambient-occlusion/global-illumination] As this is not what occurs in real life, we perceive the resulting image as incomplete.
[src:ambient-occlusion/global-illumination] Applying full global illumination allows for the missing effects that makes an image feel more natural.
[src:ambient-occlusion/global-illumination] However, global illumination is computationally more expensive and consequently much slower to generate.
[src:ambient-occlusion/global-illumination] The algorithms used to calculate the distribution of light energy between surfaces of a scene are closely related to heat transfer simulations performed using finite-element methods in engineering design.
[src:ambient-occlusion/global-illumination] Achieving accurate computation of global illumination in real-time remains difficult.
[src:ambient-occlusion/global-illumination] On one end, the diffuse inter-reflection component of global illumination is sometimes approximated by an "ambient" term in the lighting equation, which is also called "ambient lighting" or "ambient color" in 3D software.
[src:ambient-occlusion/global-illumination] Though this method is one of the cheapest ways to simulate indirect lighting, when used alone it does not provide an adequately realistic effect.
[src:ambient-occlusion/global-illumination] Ambient lighting is known to "flatten" shadows in 3D scenes, making the overall visual effect more bland.
[src:ambient-occlusion/global-illumination] Beyond ambient lighting, techniques which trace the path of light accurately have historically been either too slow for consumer hardware or limited to static and precomputed environments.
[src:ambient-occlusion/global-illumination] This proves problematic, as most applications allow for input from a user that can affect their surroundings, and the precalculation steps may introduce constraints upon the artists.
[src:ambient-occlusion/global-illumination] Consequently, research has been dedicated to finding a balance between adequate performance, accurate visual results, and interactivity.
[src:ambient-occlusion/global-illumination] Starting with Nvidia's RTX 20 series, consumer graphics hardware has been extended to allow for ray tracing computations to be performed in real time through hardware acceleration.
[src:ambient-occlusion/global-illumination] This has allowed for further improvements, as applications can now harness the power of this acceleration to provide not only precise lighting results, but the ability to affect said lighting dynamically.
[src:ambient-occlusion/global-illumination] Some content that has taken advantage of this capability includes Cyberpunk 2077, Indiana Jones and the Great Circle, and Alan Wake 2, among others.
[src:ambient-occlusion/global-illumination] For an overview of the current state of real-time global illumination, see or.
[src:ambient-occlusion/global-illumination] Algorithms which attempt to simulate global illumination are numerical approximations of the rendering equation.
[src:ambient-occlusion/global-illumination] Well-known algorithms for computing global illumination include path tracing, photon mapping and radiosity.
[src:ambient-occlusion/screen-space-ambient-occlusion] The algorithm is implemented as a pixel shader, analyzing the scene depth buffer which is stored in a texture.
[src:ambient-occlusion/screen-space-ambient-occlusion] For every pixel on the screen, the pixel shader samples the depth values around the current pixel and tries to compute the amount of occlusion from each of the sampled points.
[src:ambient-occlusion/screen-space-ambient-occlusion] In its simplest implementation, the occlusion factor depends only on the depth difference between sampled point and current point.
[src:ambient-occlusion/screen-space-ambient-occlusion] Without additional smart solutions, such a brute force method would require about 200 texture reads per pixel for good visual quality.
[src:ambient-occlusion/screen-space-ambient-occlusion] This is not acceptable for real-time rendering on current graphics hardware.
[src:ambient-occlusion/screen-space-ambient-occlusion] In order to get high quality results with far fewer reads, sampling is performed using a randomly rotated kernel.
[src:ambient-occlusion/screen-space-ambient-occlusion] The kernel orientation is repeated every *N* screen pixels in order to have only high-frequency noise in the final picture.
[src:ambient-occlusion/screen-space-ambient-occlusion] In the end this high frequency noise is greatly removed by a *N*x*N* post-process blurring step taking into account depth discontinuities (using methods such as comparing adjacent normals and depths).
[src:ambient-occlusion/screen-space-ambient-occlusion] Such a solution allows a reduction in the number of depth samples per pixel to about 16 or fewer while maintaining a high quality result, and allows the use of SSAO in soft real-time applications like computer games.
[src:ambient-occlusion/screen-space-ambient-occlusion] Because SSAO operates only on the current depth buffer, it can miss occluding geometry that is not rasterized into the z-buffer and may produce undersampling-related artifacts.
[src:ambient-occlusion/shader] In computer graphics, a **shader** is a programmable operation which is applied to data as it moves through the rendering pipeline.
[src:ambient-occlusion/shader] Shaders act on data such as vertices and primitives, generate or morph geometries and fragments, and calculate the colors in a rendered image.
[src:ambient-occlusion/shader] Shaders can execute a wide variety of operations.
[src:ambient-occlusion/shader] In modern real-time computer graphics, shaders are run on graphics processing units (GPUs) - dedicated hardware which provide parallel execution of programs.
[src:ambient-occlusion/shader] Because image rendering is an embarrassingly parallel problem (having an Amdahl's index of one), shaders benefit greatly from parallel computing, such as SIMD hardware.
[src:ambient-occlusion/shader] The drive for faster rendering has produced highly parallel processors, like GPUs, which are now used for general-purpose computing.
[src:ambient-occlusion/shader] Shaders used for general purposes are commonly called *compute shaders*.
[src:ambient-occlusion/shader] The term "shader" was first introduced to the public by Pixar with version 3.0 of their RenderMan Interface Specification, originally published in May 1988.
[src:ambient-occlusion/shader] As graphics processing units evolved, major graphics software libraries such as OpenGL and Direct3D began to support shaders.
[src:ambient-occlusion/shader] The first shader-capable GPUs only supported pixel shading, but vertex shaders were quickly introduced once developers realized the power of shaders.
[src:ambient-occlusion/shader] The first video card with a programmable pixel shader was the Nvidia GeForce 3 (NV20), released in 2001.
[src:ambient-occlusion/shader] Geometry shaders were introduced with Direct3D 10 and OpenGL 3.2.
[src:ambient-occlusion/shader] Graphics hardware evolved toward a unified shader model, making all shaders equal.
[src:ambient-occlusion/shader] The traditional use of shaders is to operate on data in the graphics pipeline to control the rendering of an image.
[src:ambient-occlusion/shader] Graphics shaders are classified according to their position in the pipeline, the data being manipulated, and the graphics API being used.
[src:ambient-occlusion/shader] Vertex shaders are run once for each 3D vertex given to the graphics processor.
[src:ambient-occlusion/shader] The purpose is to transform each vertex's 3D position in virtual space to the 2D coordinate at which it appears on the screen (as well as a depth value for the Z-buffer).
[src:ambient-occlusion/shader] Vertex shaders can manipulate properties such as position, color and texture coordinates, but cannot create new vertices.
[src:ambient-occlusion/shader] The output of the vertex shader goes to the next stage in the pipeline, which is either a geometry shader if present, or the rasterizer.
[src:ambient-occlusion/shader] Vertex shaders can enable powerful control over the details of position, movement, lighting, and color in any scene involving 3D models.
[src:ambient-occlusion/shader] Geometry shaders were introduced in Direct3D 10 and OpenGL 3.2; formerly available in OpenGL 2.0+ with the use of extensions.
[src:ambient-occlusion/shader] This type of shader can generate new graphics primitives, such as points, lines, and triangles, from those primitives that were sent to the beginning of the graphics pipeline.
[src:ambient-occlusion/shader] Geometry shader programs are executed after vertex shaders.
[src:ambient-occlusion/shader] They take as input a whole primitive, possibly with adjacency information.
[src:ambient-occlusion/shader] For example, when operating on triangles, the three vertices are the geometry shader's input.
[src:ambient-occlusion/shader] The shader can then emit zero or more primitives, which are rasterized and their fragments ultimately passed to a pixel shader.
[src:ambient-occlusion/shader] Typical uses of a geometry shader include point sprite generation, geometry tessellation, shadow volume extrusion, and single pass rendering to a cube map.
[src:ambient-occlusion/shader] A typical real-world example of the benefits of geometry shaders would be automatic mesh complexity modification.
[src:ambient-occlusion/shader] A series of line strips representing control points for a curve are passed to the geometry shader and depending on the complexity required the shader can automatically generate extra lines each of which provides a better approximation of a curve.
[src:ambient-occlusion/shader] model rendered using fragment shaders with different outputs: solid white, applied texture, physically based shading.
[src:ambient-occlusion/shader] Fragment shaders, also known as **pixel shaders**, compute color and other attributes of each "fragment": a unit of rendering work affecting at most a single output pixel.
[src:ambient-occlusion/shader] The simplest kinds of pixel shaders output one screen pixel as a color value; more complex shaders with multiple inputs/outputs are also possible.
[src:ambient-occlusion/shader] Pixel shaders range from simply always outputting the same color, to applying a lighting value, to doing bump mapping, shadows, specular highlights, translucency and other phenomena.
[src:ambient-occlusion/shader] They can alter the depth of the fragment (for Z-buffering), or output more than one color if multiple render targets are active.
[src:ambient-occlusion/shader] In 3D graphics, a pixel shader alone cannot produce some kinds of complex effects because it operates only on a single fragment, without knowledge of a scene's geometry (i.e. vertex data).
[src:ambient-occlusion/shader] However, pixel shaders do have knowledge of the screen coordinate being drawn, and can sample the screen and nearby pixels if the contents of the entire screen are passed as a texture to the shader.
[src:ambient-occlusion/shader] This technique can enable a wide variety of two-dimensional postprocessing effects such as blur, or edge detection/enhancement for cartoon/cel shaders.
[src:ambient-occlusion/shader] Pixel shaders may also be applied in *intermediate* stages to any two-dimensional images—sprites or textures—in the pipeline, whereas vertex shaders always require a 3D scene.
[src:ambient-occlusion/shader] For instance, a pixel shader is the only kind of shader that can act as a postprocessor or filter for a video stream after it has been rasterized.
[src:ambient-occlusion/shader] OpenGL 4.0 and Direct3D 11 introduced a new shader class called a tessellation shader.
[src:ambient-occlusion/shader] The function can be related to a variety of variables, most notably the distance from the viewing camera to allow active level-of-detail scaling.
[src:ambient-occlusion/shader] This allows objects close to the camera to have fine detail, while further away ones can have more coarse meshes, yet seem comparable in quality.
[src:ambient-occlusion/shader] It also can drastically reduce required mesh bandwidth by allowing meshes to be refined once inside the shader units instead of down-sampling very complex ones from memory.
[src:ambient-occlusion/shader] Some algorithms can up-sample any arbitrary mesh, while others allow for "hinting" in meshes to dictate the most characteristic vertices and edges.
[src:ambient-occlusion/shader] Circa 2017, the AMD Vega microarchitecture added support for a new shader stage—primitive shaders—somewhat akin to compute shaders with access to the data necessary to process geometry.
[src:ambient-occlusion/shader] Nvidia introduced mesh and task shaders with its Turing microarchitecture in 2018 which are also modelled after compute shaders.
[src:ambient-occlusion/shader] Nvidia Turing is the world's first GPU microarchitecture that supports mesh shading through DirectX 12 Ultimate API, several months before Ampere RTX 30 series was released.
[src:ambient-occlusion/shader] In 2020, AMD and Nvidia released RDNA 2 and Ampere microarchitectures which both support mesh shading through DirectX 12 Ultimate.
[src:ambient-occlusion/shader] These mesh shaders allow the GPU to handle more complex algorithms, offloading more work from the CPU to the GPU, and in algorithm intense rendering, increasing the frame rate of or number of triangles in a scene by an order of magnitude.
[src:ambient-occlusion/shader] Intel announced that Intel Arc Alchemist GPUs shipping in Q1 2022 will support mesh shaders.
[src:ambient-occlusion/shader] Ray tracing shaders are supported by Microsoft via DirectX Raytracing, by Khronos Group via Vulkan, GLSL, and SPIR-V, by Apple via Metal.
[src:ambient-occlusion/shader] Nvidia and AMD called the parts of the hardware responsible for executing these shaders "ray tracing cores".
[src:ambient-occlusion/shader] Compute kernels are routines compiled for high throughput accelerators (such as graphics processing units (GPUs), digital signal processors (DSPs), or field-programmable gate arrays (FPGAs)), separate from but used by a main program (typically running on a central processing unit).
[src:ambient-occlusion/shader] They may be specified by a separate programming language such as "OpenCL C", as "compute shaders" written in a shading language, or embedded directly in application code written in a high level language.
[src:ambient-occlusion/shader] Compute kernels roughly correspond to inner loops when implementing algorithms in traditional languages (except there is no implied sequential operation), or to code passed to internal iterators.
[src:ambient-occlusion/shader] Microsoft support this as DirectCompute.
[src:ambient-occlusion/shader] This single program, multiple data programming paradigm maps well to vector processors: there is an assumption that each invocation of a kernel within a batch is independent, allowing for data parallel execution.
[src:ambient-occlusion/shader] However, atomic operations may sometimes be used for synchronization between elements (for interdependent work), in some scenarios.
[src:ambient-occlusion/shader] Individual invocations are given indices (in 1 or more dimensions) from which arbitrary addressing of buffer data may be performed (including scatter gather operations), so long as the non-overlapping assumption is respected.
[src:ambient-occlusion/shader] The Vulkan API provides the intermediate SPIR-V representation to describe *both* graphical shaders, and compute kernels in a language independent and machine independent manner.
[src:ambient-occlusion/shader] The intention is to facilitate language evolution and provide a more natural ability to leverage GPU compute capabilities, in line with hardware developments such as Unified Memory Architecture and Heterogeneous System Architecture.
[src:ambient-occlusion/shader] This allows closer cooperation between a CPU and GPU.
[src:ambient-occlusion/shader] Much work has been done in the field of Kernel generation through LLMs as a means of optimizing code.
[src:ambient-occlusion/shader] KernelBench, created by the Scaling Intelligence Lab at Stanford, provides a framework to evaluate the ability of LLMs to generate efficient GPU kernels.
[src:ambient-occlusion/shader] Cognition has created Kevin 32-B to create efficient CUDA kernels which is currently the highest performing model on KernelBench.
[src:ambient-occlusion/shader] Tensor shaders are accelerator programs that deal with matrix operations on large arrays, a type of operation commonly used in neural network-based AI.
[src:ambient-occlusion/shader] Tensor shaders are supported by Microsoft via DirectML, by Khronos Group via OpenVX, by Apple via Core ML, by Google via TensorFlow, by Linux Foundation via ONNX.
[src:ambient-occlusion/shader] Nvidia and AMD called the parts of the hardware responsible for executing these shaders "tensor cores".
[src:ambient-occlusion/shader] Several programming languages exist specifically for writing shaders, and which is used can depend on the target environment.
[src:ambient-occlusion/shader] The shading language for OpenGL is GLSL, and Direct3D uses HLSL.
[src:ambient-occlusion/shader] The Metal framework, used by Apple devices, has its own shading language called Metal Shading Language.
[src:ambient-occlusion/shader] Increasingly in modern graphics APIs, shaders are compiled into SPIR-V, an intermediate language, before they are distributed to the end user.
[src:ambient-occlusion/shader] This standard allows more flexible choice of shading language, regardless of target platform.
[src:ambient-occlusion/shader] First supported by Vulkan and OpenGL, SPIR-V is also being adopted by Direct3D.
[src:ambient-occlusion/shader] The graph is then compiled into a shader.
[src:ambient-occlusion/shader] In web development contexts, visual and component-based shader tools have also emerged.
[src:ambient-occlusion/shader] Platforms such as shaders.com provide component-driven composition of shaders for integration into frontend frameworks including Vue, React, and Svelte, while tools such as Shadertoy and FragCoord.xyz focus on lower-level shader authoring and debugging.
