# nl-rag distilled facts — domain: kernel-hardening
# one declarative sentence per line; load with:  neuron --db app.db import pack.facts --scope knowledge
[src:kernel-hardening/address-space-layout-randomization] The Linux PaX project first coined the term "ASLR", and published the first design and implementation of ASLR in July 2001 as a patch for the Linux kernel.
[src:kernel-hardening/address-space-layout-randomization] It is seen as a complete implementation, providing a patch for kernel stack randomization since October 2002.
[src:kernel-hardening/address-space-layout-randomization] The first mainstream operating system to support ASLR by default was OpenBSD version 3.4 in 2003, followed by Linux in 2005.
[src:kernel-hardening/address-space-layout-randomization] Address space randomization hinders some types of security attacks by making it more difficult for an attacker to predict target addresses.
[src:kernel-hardening/address-space-layout-randomization] For example, attackers trying to execute return-to-libc attacks must locate the code to be executed, while other attackers trying to execute shellcode injected on the stack have to find the stack first.
[src:kernel-hardening/address-space-layout-randomization] In both cases, the system makes related memory-addresses unpredictable from the attackers' point of view.
[src:kernel-hardening/address-space-layout-randomization] These values have to be guessed, and a mistaken guess is not usually recoverable due to the application crashing.
[src:kernel-hardening/address-space-layout-randomization] Address space layout randomization is based upon the low chance of an attacker guessing the locations of randomly placed areas.
[src:kernel-hardening/address-space-layout-randomization] Security is increased by increasing the search space.
[src:kernel-hardening/address-space-layout-randomization] Thus, address space randomization is more effective when more entropy is present in the random offsets.
[src:kernel-hardening/address-space-layout-randomization] Entropy is increased by either raising the amount of virtual memory area space over which the randomization occurs or reducing the period over which the randomization occurs.
[src:kernel-hardening/address-space-layout-randomization] The period is typically implemented as small as possible, so most systems must increase VMA space randomization.
[src:kernel-hardening/address-space-layout-randomization] To defeat the randomization, attackers must successfully guess the positions of all areas they wish to attack.
[src:kernel-hardening/address-space-layout-randomization] For data areas such as stack and heap, where custom code or useful data can be loaded, more than one state can be attacked by using NOP slides for code or repeated copies of data.
[src:kernel-hardening/address-space-layout-randomization] This allows an attack to succeed if the area is randomized to one of a handful of values.
[src:kernel-hardening/address-space-layout-randomization] In contrast, code areas such as library base and main executable need to be discovered exactly.
[src:kernel-hardening/address-space-layout-randomization] Often these areas are mixed, for example stack frames are injected onto the stack and a library is returned into.
[src:kernel-hardening/address-space-layout-randomization] To calculate the probability of an attacker succeeding, a number of attempts α carried out without being interrupted by a signature-based IPS, law enforcement, or other factor must be assumed; in the case of brute forcing, the daemon cannot be restarted.
[src:kernel-hardening/address-space-layout-randomization] The number of relevant bits and how many are being attacked in each attempt must also be calculated, leaving however many bits the attacker has to defeat.
[src:kernel-hardening/address-space-layout-randomization] The following formulas represent the probability of success for a given set of α attempts on N bits of entropy.
[src:kernel-hardening/address-space-layout-randomization] In many systems, $2^{N}$ can be in the thousands or millions.
[src:kernel-hardening/address-space-layout-randomization] On 32-bit systems, a typical amount of entropy *N* is 8 bits.
[src:kernel-hardening/address-space-layout-randomization] For 2004 computer speeds, Shacham and co-workers state "...
[src:kernel-hardening/address-space-layout-randomization] 16 bits of address randomization can be defeated by a brute force attack within minutes." (The authors' statement depends on the ability to attack the same application multiple times without any delay.
[src:kernel-hardening/address-space-layout-randomization] Proper implementations of ASLR, like that included in grsecurity, provide several methods to make such brute force attacks infeasible.
[src:kernel-hardening/address-space-layout-randomization] One method involves preventing an executable from executing for a configurable amount of time if it has crashed a certain number of times.) On modern 64-bit systems, these numbers typically reach the millions at least.
[src:kernel-hardening/address-space-layout-randomization] Android, and possibly other systems, implement *Library Load Order Randomization*, a form of ASLR which randomizes the order in which libraries are loaded.
[src:kernel-hardening/address-space-layout-randomization] An approximation of the number of bits of entropy supplied per needed library appears below; this does not yet account for varied library sizes, so the actual entropy gained is really somewhat higher.
[src:kernel-hardening/address-space-layout-randomization] Attackers usually need only one library; the math is more complex with multiple libraries, and shown below as well.
[src:kernel-hardening/address-space-layout-randomization] The case of an attacker using only one library is a simplification of the more complex formula for $l=1$ .
[src:kernel-hardening/address-space-layout-randomization] These values tend to be low even for large values of l, most importantly since attackers typically can use only the C standard library and thus one can often assume that $\beta \,=1$ .
[src:kernel-hardening/address-space-layout-randomization] However, even for a small number of libraries there are a few bits of entropy gained here; it is thus potentially interesting to combine library load order randomization with VMA address randomization to gain a few extra bits of entropy.
[src:kernel-hardening/address-space-layout-randomization] These extra bits of entropy will not apply to other mmap() segments, only libraries.
[src:kernel-hardening/address-space-layout-randomization] Attackers may make use of several methods to reduce the entropy present in a randomized address space, ranging from simple information leaks to attacking multiple bits of entropy per attack (such as by heap spraying).
[src:kernel-hardening/address-space-layout-randomization] There is little that can be done about this.
[src:kernel-hardening/address-space-layout-randomization] It is possible to leak information about memory layout using format string vulnerabilities.
[src:kernel-hardening/address-space-layout-randomization] Format string functions such as printf use a variable argument list to do their job; format specifiers describe what the argument list looks like.
[src:kernel-hardening/address-space-layout-randomization] Because of the way arguments are typically passed, each format specifier moves closer to the top of the stack frame.
[src:kernel-hardening/address-space-layout-randomization] Eventually, the return pointer and stack frame pointer can be extracted, revealing the address of a vulnerable library and the address of a known stack frame; this can eliminate library and stack randomization as an obstacle to an attacker.
[src:kernel-hardening/address-space-layout-randomization] One can also decrease entropy in the stack or heap.
[src:kernel-hardening/address-space-layout-randomization] The stack typically must be aligned to 16 bytes, and so this is the smallest possible randomization interval; while the heap must be page-aligned, typically 4096 bytes.
[src:kernel-hardening/address-space-layout-randomization] When attempting an attack, it is possible to align duplicate attacks with these intervals; a NOP slide may be used with shellcode injection, and the string '`/bin/sh`' can be replaced with '`////////bin/sh`' for an arbitrary number of slashes when attempting to return to *system*.
[src:kernel-hardening/address-space-layout-randomization] The number of bits removed is exactly $\log _{2}\!\left(n\right)$ for n intervals attacked.
[src:kernel-hardening/address-space-layout-randomization] Such decreases are limited due to the amount of data in the stack or heap.
[src:kernel-hardening/address-space-layout-randomization] The stack, for example, is typically limited to 8 MB and grows to much less; this allows for at most 19 bits, although a more conservative estimate would be around 8–10 bits corresponding to 4–16 KB of stack stuffing.
[src:kernel-hardening/address-space-layout-randomization] The heap on the other hand is limited by the behavior of the memory allocator; in the case of glibc, allocations above 128 KB are created using mmap, limiting attackers to 5 bits of reduction.
[src:kernel-hardening/address-space-layout-randomization] This is also a limiting factor when brute forcing; although the number of attacks to perform can be reduced, the size of the attacks is increased enough that the behavior could in some circumstances become apparent to intrusion detection systems.
[src:kernel-hardening/address-space-layout-randomization] ASLR-protected addresses can be leaked by various side channels, removing mitigation utility.
[src:kernel-hardening/address-space-layout-randomization] Recent attacks have used information leaked by the CPU branch target predictor buffer (BTB) or memory management unit (MMU) walking page tables.
[src:kernel-hardening/address-space-layout-randomization] It is not clear if this class of ASLR attack can be mitigated.
[src:kernel-hardening/address-space-layout-randomization] If they cannot, the benefit of ASLR is reduced or eliminated.
[src:kernel-hardening/address-space-layout-randomization] In August 2024 a paper was published with an empirical analysis of major desktop platforms, including Linux, macOS, and Windows, by examining the variability in the placement of memory objects across various processes, threads, and system restarts.
[src:kernel-hardening/address-space-layout-randomization] The results show that while some systems as of 2024, like Linux distributions, provide robust randomization, others, like Windows and macOS, often fail to adequately randomize key areas like executable code and libraries.
[src:kernel-hardening/address-space-layout-randomization] Moreover, they found a significant reduction in the entropy of libraries after the Linux 5.18 version and identify correlation paths that an attacker could leverage to reduce exploitation complexity significantly.
[src:kernel-hardening/address-space-layout-randomization] Several mainstream, general-purpose operating systems implement ASLR.
[src:kernel-hardening/address-space-layout-randomization] Android 4.0 Ice Cream Sandwich provides address space layout randomization (ASLR) to help protect system and third-party applications from exploits due to memory-management issues.
[src:kernel-hardening/address-space-layout-randomization] Position-independent executable support was added in Android 4.1.
[src:kernel-hardening/address-space-layout-randomization] Android 5.0 dropped non-PIE support and requires all dynamically linked binaries to be position independent.
[src:kernel-hardening/address-space-layout-randomization] Library load ordering randomization was accepted into the Android open-source project on 26 October 2015, and was included in the Android 7.0 release.
[src:kernel-hardening/address-space-layout-randomization] DragonFly BSD has an implementation of ASLR based upon OpenBSD's model, added in 2010.
[src:kernel-hardening/address-space-layout-randomization] It is off by default, and can be enabled by setting the sysctl vm.randomize_mmap to 1.
[src:kernel-hardening/address-space-layout-randomization] Support for ASLR appeared in FreeBSD 13.0.
[src:kernel-hardening/address-space-layout-randomization] Apple introduced ASLR in iOS 4.3 (released March 2011).
[src:kernel-hardening/address-space-layout-randomization] The randomized kernel base is `0x01000000 + ((1+0xRR) * 0x00200000)`, where `0xRR` is a random byte from SHA1 (random data) generated by iBoot (the 2nd-stage iOS Boot Loader).
[src:kernel-hardening/address-space-layout-randomization] The Linux kernel enabled a weak form of ASLR by default since the kernel version 2.6.12, released in June 2005.
[src:kernel-hardening/address-space-layout-randomization] The PaX and Exec Shield patchsets to the Linux kernel provide more complete implementations.
[src:kernel-hardening/address-space-layout-randomization] The Exec Shield patch for Linux supplies 19 bits of stack entropy on a period of 16 bytes, and 8 bits of mmap base randomization on a period of 1 page of 4096 bytes.
[src:kernel-hardening/address-space-layout-randomization] This places the stack base in an area 8 MB wide containing 524,288 possible positions, and the mmap base in an area 1 MB wide containing 256 possible positions.
[src:kernel-hardening/address-space-layout-randomization] ASLR can be disabled for a specific process by changing its execution domain, using `personality(2)`.
[src:kernel-hardening/address-space-layout-randomization] A number of sysctl options control the behavior of mainline ASLR.
[src:kernel-hardening/address-space-layout-randomization] For example, `kernel.randomize_va_space` controls *what* to randomize; the strongest option is 2.
[src:kernel-hardening/address-space-layout-randomization] `vm.mmap_rnd_bits` controls how many bits to randomize for mmap.
[src:kernel-hardening/address-space-layout-randomization] Position-independent executable (PIE) implements a random base address for the main executable binary and has been in place since April 18, 2004.
[src:kernel-hardening/address-space-layout-randomization] It provides the same address randomness to the main executable as being used for the shared libraries.
[src:kernel-hardening/address-space-layout-randomization] The PIE feature cannot be used together with the prelink feature for the same executable.
[src:kernel-hardening/address-space-layout-randomization] The prelink tool implements randomization at prelink time rather than runtime, because by design prelink aims to handle relocating libraries before the dynamic linker has to, which allows the relocation to occur once for many runs of the program.
[src:kernel-hardening/address-space-layout-randomization] As a result, real address space randomization would defeat the purpose of prelinking.
[src:kernel-hardening/address-space-layout-randomization] In 2014, Marco-Gisbert and Ripoll disclosed *offset2lib* technique that weakens Linux ASLR for PIE executables.
[src:kernel-hardening/address-space-layout-randomization] Linux kernels load PIE executables right after their libraries; as a result, there is a fixed offset between the executable and the library functions.
[src:kernel-hardening/address-space-layout-randomization] If an attacker finds a way to find the address of a function in the executable, the library addresses are also known.
[src:kernel-hardening/address-space-layout-randomization] They demonstrated an attack that finds the address in fewer than 400 tries.
[src:kernel-hardening/address-space-layout-randomization] They proposed a new `randomize_va_space=3` option to randomize the placement of the executable relative to the library, but it is yet to be incorporated into the upstream as of 2024.
[src:kernel-hardening/address-space-layout-randomization] The Linux kernel 5.18 released May 2022 reduced the effectiveness of both 32-bit and 64-bit implementations.
[src:kernel-hardening/address-space-layout-randomization] Linux filesystems call `thp_get_unmapped_area` to respond to a file-backed mmap.
[src:kernel-hardening/address-space-layout-randomization] With a change in 5.18, files greater than 2 MiB are made to return 2 MiB-aligned addresses, so they can be potentially backed by huge pages.
[src:kernel-hardening/address-space-layout-randomization] For 32-bit Linux, many distributions show no randomization *at all* in the placement of the libc.
[src:kernel-hardening/address-space-layout-randomization] For 64-bit Linux, the 28 bits of entropy is reduced to 19 bits.
[src:kernel-hardening/address-space-layout-randomization] In response, Ubuntu has increased its `mmap_rnd_bits` setting.
[src:kernel-hardening/address-space-layout-randomization] Martin Doucha added a Linux Test Project testcase to detect this issue.
[src:kernel-hardening/address-space-layout-randomization] Kernel address space layout randomization (KASLR) enables address space randomization for the Linux kernel image by randomizing where the kernel code is placed at boot time.
[src:kernel-hardening/address-space-layout-randomization] KASLR was merged into the Linux kernel mainline in kernel version 3.14, released on 30 March 2014.
[src:kernel-hardening/address-space-layout-randomization] When compiled in, it can be disabled at boot time by specifying nokaslr as one of the kernel's boot parameters.
[src:kernel-hardening/address-space-layout-randomization] There are several side-channel attacks in x86 processors that could leak kernel addresses.
[src:kernel-hardening/address-space-layout-randomization] In late 2017, kernel page-table isolation (KPTI aka KAISER) was developed to defeat these attacks.
[src:kernel-hardening/address-space-layout-randomization] However, this method cannot protect against side-channel attacks utilizing collisions in branch predictor structures.
[src:kernel-hardening/address-space-layout-randomization] As of 2021, finer grained kernel address space layout randomization (or function granular KASLR, FGKASLR) is a planned extension of KASLR to randomize down to the function level by placing functions in separate sections and reordering them at boot time.
[src:kernel-hardening/address-space-layout-randomization] Microsoft's Windows Vista (released *to manufacturing* November 2006, generally available January 2007) and later have ASLR enabled only for executables and dynamic link libraries that are specifically linked to be ASLR-enabled.
[src:kernel-hardening/address-space-layout-randomization] For compatibility, it is not enabled by default for other applications.
[src:kernel-hardening/address-space-layout-randomization] Typically, only older software is incompatible and ASLR can be fully enabled by editing a registry entry `HKLM\SYSTEM\CurrentControlSet\Control\Session Manager\Memory Management\MoveImages`, or by installing Microsoft's Enhanced Mitigation Experience Toolkit.
[src:kernel-hardening/address-space-layout-randomization] The locations of the heap, stack, Process Environment Block, and Thread Environment Block are also randomized.
[src:kernel-hardening/address-space-layout-randomization] A security whitepaper from Symantec noted that ASLR in 32-bit Windows Vista may not be as robust as expected, and Microsoft has acknowledged a weakness in its implementation.
[src:kernel-hardening/address-space-layout-randomization] Host-based intrusion prevention systems such as *WehnTrust* and *Ozone* also offer ASLR for Windows XP and Windows Server 2003 operating systems.
[src:kernel-hardening/address-space-layout-randomization] Complete details of Ozone's implementation are not available.
[src:kernel-hardening/address-space-layout-randomization] It was noted in February 2012 that ASLR on 32-bit Windows systems prior to Windows 8 can have its effectiveness reduced in low memory situations.
[src:kernel-hardening/address-space-layout-randomization] A similar effect also had been achieved on Linux in the same research.
[src:kernel-hardening/address-space-layout-randomization] The test code caused the Mac OS X 10.7.3 system to kernel panic, so it was left unclear about its ASLR behavior in this scenario.
[src:kernel-hardening/address-space-layout-randomization] Support for ASLR in userland appeared in NetBSD 5.0 (released April 2009), and was enabled by default in NetBSD-current in April 2016.
[src:kernel-hardening/address-space-layout-randomization] Kernel ASLR support on amd64 was added in NetBSD-current in October 2017, making NetBSD the first BSD system to support KASLR.
[src:kernel-hardening/address-space-layout-randomization] In 2003, OpenBSD became the first mainstream operating system to support a strong form of ASLR and to activate it by default.
[src:kernel-hardening/address-space-layout-randomization] OpenBSD completed its ASLR support in 2008 when it added support for PIE binaries.
[src:kernel-hardening/address-space-layout-randomization] OpenBSD 4.4's malloc(3) was designed to improve security by taking advantage of ASLR and gap page features implemented as part of OpenBSD's `mmap` system call, and to detect use-after-free bugs.
[src:kernel-hardening/address-space-layout-randomization] Released in 2013, OpenBSD 5.3 was the first mainstream operating system to enable position-independent executables by default on multiple hardware platforms, and OpenBSD 5.7 activated position-independent static binaries (Static-PIE) by default.
[src:kernel-hardening/address-space-layout-randomization] In Mac OS X Leopard 10.5 (released October 2007), Apple introduced randomization for system libraries.
[src:kernel-hardening/address-space-layout-randomization] In Mac OS X Lion 10.7 (released July 2011), Apple expanded their implementation to cover all applications, stating "address space layout randomization (ASLR) has been improved for all applications.
[src:kernel-hardening/address-space-layout-randomization] As of OS X Mountain Lion 10.8 (released July 2012) and later, the entire system including the kernel as well as kexts and zones are randomly relocated during system boot.
[src:kernel-hardening/address-space-layout-randomization] ASLR has been introduced in Solaris beginning with Solaris 11.1 (released October 2012).
[src:kernel-hardening/address-space-layout-randomization] ASLR in Solaris 11.1 can be set system-wide, per zone, or on a per-binary basis.
[src:kernel-hardening/address-space-layout-randomization] A side-channel attack utilizing branch target buffer was demonstrated to bypass ASLR protection.
[src:kernel-hardening/address-space-layout-randomization] In 2017, an attack named "ASLR⊕Cache" was demonstrated which could defeat ASLR in a web browser using JavaScript.
[src:kernel-hardening/attack-surface] The **attack surface** of a software environment is the sum of the different points (for "attack vectors") where an unauthorized user (the "attacker") can try to enter data to, extract data from, or control a device or critical software in an environment.
[src:kernel-hardening/attack-surface] Keeping the attack surface as small as possible is a basic security measure.
[src:kernel-hardening/attack-surface] Worldwide digital change has accelerated the size, scope, and composition of an organization's attack surface.
[src:kernel-hardening/attack-surface] The size of an attack surface may fluctuate over time, adding and subtracting assets and digital systems (e.g. websites, hosts, cloud and mobile apps, etc.).
[src:kernel-hardening/attack-surface] Attack surface sizes can change rapidly as well.
[src:kernel-hardening/attack-surface] Digital assets eschew the physical requirements of traditional network devices, servers, data centers, and on-premise networks.
[src:kernel-hardening/attack-surface] This leads to attack surfaces changing rapidly, based on the organization's needs and the availability of digital services to accomplish it.
[src:kernel-hardening/attack-surface] Attack surface scope also varies from organization to organization.
[src:kernel-hardening/attack-surface] With the rise of digital supply chains, interdependencies, and globalization, an organization's attack surface has a broader scope of concern (viz. vectors for cyberattacks).
[src:kernel-hardening/attack-surface] Due to the increase in the countless potential vulnerable points each enterprise has, there has been increasing advantage for hackers and attackers as they only need to find one vulnerable point to succeed in their attack.
[src:kernel-hardening/attack-surface] One approach to improving information security is to reduce the attack surface of a system or software.
[src:kernel-hardening/attack-surface] The basic strategies of attack surface reduction include the following: reduce the amount of code running, reduce entry points available to untrusted users, and eliminate services requested by relatively few users.
[src:kernel-hardening/attack-surface] By having less code available to unauthorized actors, there tend to be fewer failures.
[src:kernel-hardening/attack-surface] By turning off unnecessary functionality, there are fewer security risks.
[src:kernel-hardening/attack-surface] Although attack surface reduction helps prevent security failures, it does not mitigate the amount of damage an attacker could inflict once a vulnerability is found.
[src:kernel-hardening/hardening-computing] In computer security, **hardening** or **system hardening** is usually the process of securing a system by making it a 'hard target' by reducing its attack surface vulnerabilities.
[src:kernel-hardening/hardening-computing] The attack surface is larger when a system performs more functions; in principle a single-function system is more secure than a multipurpose one.
[src:kernel-hardening/hardening-computing] Hardening is considered an important component of cybersecurity.
[src:kernel-hardening/hardening-computing] Reducing available ways of attack typically includes changing default passwords, the removal of unnecessary software, unnecessary usernames or logins, and the disabling or removal of unnecessary services.
[src:kernel-hardening/hardening-computing] It may also involve patching vulnerabilities and switching off ancillary services that are not essential.
[src:kernel-hardening/hardening-computing] Hardening measures can also include setting up intrusion prevention systems, disabling or restricting accounts, reducing file system permissions, using encrypted network connections and enabling host-based network security.
[src:kernel-hardening/hardening-computing] Binary hardening is a security technique in which binary executables are analyzed and modified to protect against common exploits.
[src:kernel-hardening/hardening-computing] Binary hardening is independent of compilers and involves the entire toolchain.
[src:kernel-hardening/hardening-computing] For example, one binary hardening technique is to detect potential buffer overflows and to substitute the existing code with safer code.
[src:kernel-hardening/hardening-computing] The advantage of manipulating binaries is that vulnerabilities in legacy code can be fixed automatically without the need for source code, which may be unavailable or obfuscated.
[src:kernel-hardening/hardening-computing] Secondly, the same techniques can be applied to binaries from multiple compilers, some of which may be less secure than others.
[src:kernel-hardening/hardening-computing] Binary hardening often involves the non-deterministic modification of control flow and instruction addresses so as to prevent attackers from successfully reusing program code to perform exploits.
[src:kernel-hardening/kernel-operating-system-p01] A **kernel** is a computer program at the core of a computer's operating system that always has complete control over everything in the system.
[src:kernel-hardening/kernel-operating-system-p01] The kernel is also responsible for preventing and mitigating conflicts between different processes.
[src:kernel-hardening/kernel-operating-system-p01] It is the portion of the operating system that is always resident in memory and facilitates interactions between hardware and software components.
[src:kernel-hardening/kernel-operating-system-p01] A full kernel controls all hardware resources (e.g.
[src:kernel-hardening/kernel-operating-system-p01] I/O, memory, cryptography) via device drivers, arbitrates conflicts between processes concerning such resources, and optimizes the use of common resources, such as CPU, cache, file systems, and network sockets.
[src:kernel-hardening/kernel-operating-system-p01] On most systems, the kernel is one of the first programs loaded on startup (after the bootloader).
[src:kernel-hardening/kernel-operating-system-p01] It handles the rest of startup as well as memory, peripherals, and input/output (I/O) requests from software, translating them into data-processing instructions for the central processing unit.
[src:kernel-hardening/kernel-operating-system-p01] The critical code of the kernel is usually loaded into a separate area of memory, which is protected from access by application software or other less critical parts of the operating system.
[src:kernel-hardening/kernel-operating-system-p01] The kernel performs its tasks, such as scheduling processes, managing hardware devices such as the hard disk, and handling interrupts, in this protected kernel space.
[src:kernel-hardening/kernel-operating-system-p01] In contrast, application programs such as browsers, word processors, or audio or video players use a separate area of memory, user space.
[src:kernel-hardening/kernel-operating-system-p01] This prevents user data and kernel data from interfering with each other and causing instability and slowness, as well as preventing malfunctioning applications from affecting other applications or crashing the entire operating system.
[src:kernel-hardening/kernel-operating-system-p01] Even in systems where the kernel is included in application address spaces, memory protection is used to prevent unauthorized applications from modifying the kernel.
[src:kernel-hardening/kernel-operating-system-p01] The kernel's interface is a low-level abstraction layer.
[src:kernel-hardening/kernel-operating-system-p01] When a process requests a service from the kernel, it must invoke a system call, usually through a wrapper function.
[src:kernel-hardening/kernel-operating-system-p01] There are different kernel architecture designs.
[src:kernel-hardening/kernel-operating-system-p01] Monolithic kernels run entirely in a single address space with the CPU executing in supervisor mode, mainly for speed.
[src:kernel-hardening/kernel-operating-system-p01] Microkernels run most but not all of their services in user space, like user processes do, mainly for resilience and modularity.
[src:kernel-hardening/kernel-operating-system-p01] MINIX 3 is a notable example of microkernel design.
[src:kernel-hardening/kernel-operating-system-p01] Some kernels, such as the Linux kernel, are both monolithic and modular, since they can insert and remove loadable kernel modules at runtime.
[src:kernel-hardening/kernel-operating-system-p01] This central component of a computer system is responsible for executing programs.
[src:kernel-hardening/kernel-operating-system-p01] The kernel takes responsibility for deciding at any time which of the many running programs should be allocated to the processor or processors.
[src:kernel-hardening/kernel-operating-system-p01] Random-access memory (RAM) is used to store both program instructions and data.
[src:kernel-hardening/kernel-operating-system-p01] Typically, both need to be present in memory for a program to execute.
[src:kernel-hardening/kernel-operating-system-p01] Often, multiple programs will want memory access, frequently demanding more memory than the computer has available.
[src:kernel-hardening/kernel-operating-system-p01] The kernel is responsible for deciding which memory each process can use, and determining what to do when insufficient memory is available.
[src:kernel-hardening/kernel-operating-system-p01] I/O devices include, but are not limited to, peripherals such as keyboards, mice, disk drives, printers, USB devices, network adapters, and display devices.
[src:kernel-hardening/kernel-operating-system-p01] The kernel provides convenient methods for applications to use these devices, which are typically abstracted by the kernel so that applications do not need to know their implementation details.
[src:kernel-hardening/kernel-operating-system-p01] Key aspects necessary in resource management are defining the execution domain (address space) and the protection mechanism used to mediate access to the resources within a domain.
[src:kernel-hardening/kernel-operating-system-p01] Kernels also provide methods for synchronization and inter-process communication (IPC).
[src:kernel-hardening/kernel-operating-system-p01] These implementations may be located within the kernel itself or the kernel can also rely on other processes it is running.
[src:kernel-hardening/kernel-operating-system-p01] Although the kernel must provide IPC in order to provide access to the facilities provided by each other, kernels must also provide running programs with a method to make requests to access these facilities.
[src:kernel-hardening/kernel-operating-system-p01] The kernel is also responsible for context switching between processes or threads.
[src:kernel-hardening/kernel-operating-system-p01] The kernel has full access to the system's memory and must allow processes to safely access this memory as they require it.
[src:kernel-hardening/kernel-operating-system-p01] Often the first step in doing this is virtual addressing, usually achieved by paging and/or segmentation.
[src:kernel-hardening/kernel-operating-system-p01] Virtual addressing allows the kernel to make a given physical address appear to be another address, the virtual address.
[src:kernel-hardening/kernel-operating-system-p01] Virtual address spaces may be different for different processes; the memory that one process accesses at a particular (virtual) address may be different memory from what another process accesses at the same address.
[src:kernel-hardening/kernel-operating-system-p01] This allows every program to behave as if it is the only one (apart from the kernel) running and thus prevents applications from crashing each other.
[src:kernel-hardening/kernel-operating-system-p01] On many systems, a program's virtual address may refer to data that is not currently in memory.
[src:kernel-hardening/kernel-operating-system-p01] The layer of indirection provided by virtual addressing allows the operating system to use other data stores, like a hard drive, to store what would otherwise have to remain in main memory (RAM).
[src:kernel-hardening/kernel-operating-system-p01] As a result, operating systems can allow programs to use more memory than the system has physically available.
[src:kernel-hardening/kernel-operating-system-p01] When a program needs data that is not currently in RAM, the CPU signals to the kernel that this has happened, and the kernel responds by writing the contents of an inactive memory block to disk (if necessary) and replacing it with the data requested by the program.
[src:kernel-hardening/kernel-operating-system-p01] The program can then be resumed from the point where it was stopped.
[src:kernel-hardening/kernel-operating-system-p01] This scheme is generally known as demand paging.
[src:kernel-hardening/kernel-operating-system-p01] Virtual addressing also allows creation of virtual partitions of memory in two disjoint areas, one being reserved for the kernel (kernel space) and the other for the applications (user space).
[src:kernel-hardening/kernel-operating-system-p01] The applications are not permitted by the processor to address kernel memory, thus preventing an application from damaging the running kernel.
[src:kernel-hardening/kernel-operating-system-p01] This fundamental partition of memory space has contributed much to the current designs of actual general-purpose kernels and is almost universal in such systems, although some research kernels (e.g., Singularity) take other approaches.
[src:kernel-hardening/kernel-operating-system-p01] To perform useful functions, processes need access to the peripherals connected to the computer, which are controlled by the kernel through device drivers.
[src:kernel-hardening/kernel-operating-system-p01] A device driver is a computer program encapsulating, monitoring and controlling a hardware device (via its hardware/software interface (HSI)) on behalf of the OS.
[src:kernel-hardening/kernel-operating-system-p01] It provides the operating system with an API, procedures and information about how to control and communicate with a certain piece of hardware.
[src:kernel-hardening/kernel-operating-system-p01] Device drivers are an important and vital dependency for all OS and their applications.
[src:kernel-hardening/kernel-operating-system-p01] The design goal of a driver is abstraction; the function of the driver is to translate the OS-mandated abstract function calls (programming calls) into device-specific calls.
[src:kernel-hardening/kernel-operating-system-p01] In theory, a device should work correctly with a suitable driver.
[src:kernel-hardening/kernel-operating-system-p01] Device drivers are used for e.g. host adapters, video cards, sound cards, printers, scanners, modems, and network interface controllers.
[src:kernel-hardening/kernel-operating-system-p01] For example, to show the user something on the screen, an application would make a request to the kernel, which would forward the request to its display driver, which is then responsible for actually plotting the character/pixel.
[src:kernel-hardening/kernel-operating-system-p01] A kernel must maintain a list of available devices.
[src:kernel-hardening/kernel-operating-system-p01] In plug-and-play systems, a device manager first performs a scan on different peripheral buses, such as Peripheral Component Interconnect (PCI) or Universal Serial Bus (USB), to detect installed devices, then searches for the appropriate drivers.
[src:kernel-hardening/kernel-operating-system-p01] As device management is a very OS-specific topic, these drivers are handled differently by each kind of kernel design, but in every case, the kernel has to provide the I/O to allow drivers to physically access their devices through some port or memory location.
[src:kernel-hardening/kernel-operating-system-p01] Important decisions have to be made when designing the device management system, as in some designs accesses may involve context switches, making the operation very CPU-intensive and easily causing a significant performance overhead.
[src:kernel-hardening/kernel-operating-system-p01] In computing, a system call is how a process requests a service from an operating system's kernel that it does not normally have permission to run.
[src:kernel-hardening/kernel-operating-system-p01] System calls provide the interface between a process and the operating system.
[src:kernel-hardening/kernel-operating-system-p01] Most operations interacting with the system require permissions not available to a user-level process, e.g., I/O performed with a device present on the system, or any form of communication with other processes requires the use of system calls.
[src:kernel-hardening/kernel-operating-system-p01] A system call is a mechanism that is used by the application program to request a service from the operating system.
[src:kernel-hardening/kernel-operating-system-p01] They use a machine-code instruction that causes the processor to change mode.
[src:kernel-hardening/kernel-operating-system-p01] An example would be from supervisor mode to protected mode.
[src:kernel-hardening/kernel-operating-system-p01] This is where the operating system performs actions like accessing hardware devices or the memory management unit.
[src:kernel-hardening/kernel-operating-system-p01] Generally the operating system provides a library that sits between the operating system and normal user programs.
[src:kernel-hardening/kernel-operating-system-p01] Usually it is a C library such as Glibc or Windows API.
[src:kernel-hardening/kernel-operating-system-p01] The library handles the low-level details of passing information to the kernel and switching to supervisor mode.
[src:kernel-hardening/kernel-operating-system-p01] System calls include close, open, read, wait and write.
[src:kernel-hardening/kernel-operating-system-p01] To actually perform useful work, a process must be able to access the services provided by the kernel.
[src:kernel-hardening/kernel-operating-system-p01] This is implemented differently by each kernel, but most provide a C library or an API, which in turn invokes the related kernel functions.
[src:kernel-hardening/kernel-operating-system-p01] The method of invoking the kernel function varies from kernel to kernel.
[src:kernel-hardening/kernel-operating-system-p01] If memory isolation is in use, it is impossible for a user process to call the kernel directly, because that would be a violation of the processor's access control rules.
[src:kernel-hardening/kernel-operating-system-p01] An important consideration in the design of a kernel is the support it provides for protection from faults (fault tolerance) and from malicious behaviours (security).
[src:kernel-hardening/kernel-operating-system-p01] These two aspects are usually not clearly distinguished, and the adoption of this distinction in the kernel design leads to the rejection of a hierarchical structure for protection.
[src:kernel-hardening/kernel-operating-system-p01] Support for hierarchical protection domains is typically implemented using CPU modes.
[src:kernel-hardening/kernel-operating-system-p01] Many kernels implement "capabilities", i.e., objects provided to user code that allow limited access to an underlying object managed by the kernel.
[src:kernel-hardening/kernel-operating-system-p01] A common example is file handling: a file is a representation of information stored on a permanent storage device.
[src:kernel-hardening/kernel-operating-system-p01] The kernel may be able to perform many different operations, including read, write, delete or execute, but a user-level application may only be permitted to perform some of these operations (e.g., it may only be allowed to read the file).
[src:kernel-hardening/kernel-operating-system-p01] A common implementation of this is for the kernel to provide an object to the application (typically called a "file handle") that the application may then invoke operations on, the validity of which the kernel checks at the time the operation is requested.
[src:kernel-hardening/kernel-operating-system-p01] Such a system may be extended to cover all objects that the kernel manages, and indeed to objects provided by other user applications.
[src:kernel-hardening/kernel-operating-system-p01] An efficient and simple way to provide hardware support of capabilities is to delegate to the memory management unit (MMU) the responsibility of checking access-rights for every memory access, a mechanism called capability-based addressing.
[src:kernel-hardening/kernel-operating-system-p01] Most commercial computer architectures lack such MMU support for capabilities; the Capability Hardware Enhanced RISC Instructions (CHERI) project is developing capability-based mechanisms for several architectures.
[src:kernel-hardening/kernel-operating-system-p01] An alternative approach is to simulate capabilities using commonly supported hierarchical domains.
[src:kernel-hardening/kernel-operating-system-p01] In this approach, each protected object must reside in an address space that the application does not have access to; the kernel also maintains a list of capabilities in such memory.
[src:kernel-hardening/kernel-operating-system-p01] The performance cost of address space switching limits the practicality of this approach in systems with complex interactions between objects, but it is used in current operating systems for objects that are not accessed frequently or that are not expected to perform quickly.
[src:kernel-hardening/kernel-operating-system-p01] If the firmware does not support protection mechanisms, it is possible to simulate protection at a higher level, for example by simulating capabilities by manipulating page tables, but there are performance implications.
[src:kernel-hardening/kernel-operating-system-p01] Lack of hardware support may not be an issue, however, for systems that choose to use language-based protection.
[src:kernel-hardening/kernel-operating-system-p01] An important kernel design decision is the choice of the abstraction levels where the security mechanisms and policies should be implemented.
[src:kernel-hardening/kernel-operating-system-p01] Kernel security mechanisms play a critical role in supporting security at higher levels.
[src:kernel-hardening/kernel-operating-system-p01] One approach is to use firmware and kernel support for fault tolerance (see above), and build the security policy for malicious behavior on top of that (adding features such as cryptography mechanisms where necessary), delegating some responsibility to the compiler.
[src:kernel-hardening/kernel-operating-system-p01] Approaches that delegate enforcement of security policy to the compiler and/or the application level are often called *language-based security*.
[src:kernel-hardening/kernel-operating-system-p01] The lack of many critical security mechanisms in current mainstream operating systems impedes the implementation of adequate security policies at the application abstraction level.
[src:kernel-hardening/kernel-operating-system-p01] In fact, a common misconception in computer security is that any security policy can be implemented in an application regardless of kernel support.
[src:kernel-hardening/kernel-operating-system-p01] According to Mars Research Group developers, a lack of isolation is one of the main factors undermining kernel security.
[src:kernel-hardening/kernel-operating-system-p01] They propose their driver isolation framework for protection, primarily in the Linux kernel.
[src:kernel-hardening/kernel-operating-system-p01] Typical computer systems today use hardware-enforced rules about what programs are allowed to access what data.
[src:kernel-hardening/kernel-operating-system-p01] The processor monitors the execution and stops a program that violates a rule, such as a user process that tries to write to kernel memory.
[src:kernel-hardening/kernel-operating-system-p01] In systems that lack support for capabilities, processes are isolated from each other by using separate address spaces.
[src:kernel-hardening/kernel-operating-system-p01] Calls from user processes into the kernel are regulated by requiring them to use one of the above-described system call methods.
[src:kernel-hardening/kernel-operating-system-p01] An alternative approach is to use language-based protection.
[src:kernel-hardening/kernel-operating-system-p01] In a language-based protection system, the kernel will only allow code to execute that has been produced by a trusted language compiler.
[src:kernel-hardening/kernel-operating-system-p01] The language may then be designed such that it is impossible for the programmer to instruct it to do something that will violate a security requirement.
[src:kernel-hardening/kernel-operating-system-p01] Examples of systems with language-based protection include JX and Microsoft's Singularity.
[src:kernel-hardening/kernel-operating-system-p01] Edsger Dijkstra proved that from a logical point of view, atomic lock and unlock operations operating on binary semaphores are sufficient primitives to express any functionality of process cooperation.
[src:kernel-hardening/kernel-operating-system-p01] Nonetheless, this approach is generally held to be lacking in terms of safety and efficiency, whereas a message passing approach is more flexible.
[src:kernel-hardening/kernel-operating-system-p01] A number of other approaches (either lower- or higher-level) are available as well, with many modern kernels providing support for systems such as shared memory and remote procedure calls.
[src:kernel-hardening/kernel-operating-system-p01] The idea of a kernel where I/O devices are handled uniformly with other processes, as parallel co-operating processes, was first proposed and implemented by Brinch Hansen (although similar ideas were suggested in 1967).
[src:kernel-hardening/kernel-operating-system-p01] In Hansen's description of this, the "common" processes are called *internal processes*, while the I/O devices are called *external processes*.
[src:kernel-hardening/kernel-operating-system-p01] Similar to physical memory, allowing applications direct access to controller ports and registers can cause the controller to malfunction, or system to crash.
[src:kernel-hardening/kernel-operating-system-p01] With this, depending on the complexity of the device, some devices can get surprisingly complex to program, and use several different controllers.
[src:kernel-hardening/kernel-operating-system-p01] Because of this, providing a more abstract interface to manage the device is important.
[src:kernel-hardening/kernel-operating-system-p01] This interface is normally done by a device driver or hardware abstraction layer.
[src:kernel-hardening/kernel-operating-system-p01] Frequently, applications will require access to these devices.
[src:kernel-hardening/kernel-operating-system-p01] The kernel must maintain the list of these devices by querying the system for them in some way.
[src:kernel-hardening/kernel-operating-system-p01] This can be done through the BIOS, or through one of the various system buses (such as PCI/PCIE, or USB).
[src:kernel-hardening/kernel-operating-system-p01] Using an example of a video driver, when an application requests an operation on a device, such as displaying a character, the kernel needs to send this request to the current active video driver.
[src:kernel-hardening/kernel-operating-system-p01] The video driver, in turn, needs to carry out this request.
[src:kernel-hardening/kernel-operating-system-p01] This is an example of inter-process communication (IPC).
[src:kernel-hardening/kernel-operating-system-p01] The above listed tasks and features can be provided in many ways that differ from each other in design and implementation.
[src:kernel-hardening/kernel-operating-system-p01] The principle of *separation of mechanism and policy* is the substantial difference between the two main philosophies of microkernels and monolithic kernels.
[src:kernel-hardening/kernel-operating-system-p01] Here a *mechanism* is the support that allows the implementation of many different policies, while a *policy* is a particular "mode of operation".
[src:kernel-hardening/kernel-operating-system-p01] Because the mechanism and policy are separated, the policy can be easily changed to, e.g., require the use of a security token.
[src:kernel-hardening/kernel-operating-system-p01] A monolithic kernel instead tends to include many policies, therefore restricting the rest of the system to rely on them.
[src:kernel-hardening/kernel-operating-system-p01] Computer scientist Per Brinch Hansen argued in favour of separation of mechanism and policy.
[src:kernel-hardening/kernel-operating-system-p01] The failure to properly fulfill this separation is one of the major causes of the lack of substantial innovation in existing operating systems, a problem common in computer architecture.
[src:kernel-hardening/kernel-operating-system-p01] The monolithic design is induced by the "kernel mode"/"user mode" architectural approach to protection (technically called hierarchical protection domains), which is common in conventional commercial systems; in fact, every module needing protection is therefore preferably included into the kernel.
[src:kernel-hardening/kernel-operating-system-p01] While monolithic kernels execute all of their code in the same address space (kernel space), microkernels try to run most of their services in user space, aiming to improve maintainability and modularity of their codebase.
[src:kernel-hardening/kernel-operating-system-p01] Most kernels do not fit exactly into one of these categories, but are rather found in between these two designs.
[src:kernel-hardening/kernel-operating-system-p01] More exotic designs such as nanokernels and exokernels are available, but are seldom used for production systems.
[src:kernel-hardening/kernel-operating-system-p01] The Xen hypervisor, for example, is an exokernel.
[src:kernel-hardening/kernel-operating-system-p01] In a monolithic kernel, all OS services are part of the kernel and run in kernel mode, thus also residing in the same memory area.
[src:kernel-hardening/kernel-operating-system-p01] This approach provides rich and powerful hardware access.
[src:kernel-hardening/kernel-operating-system-p01] UNIX developer Ken Thompson stated that "it is in [his] opinion easier to implement a monolithic kernel".
[src:kernel-hardening/kernel-operating-system-p01] The main disadvantages of monolithic kernels are the dependencies between system components – a bug in a device driver might crash the entire system, for example – and the fact that large kernels can become very difficult to maintain.
[src:kernel-hardening/kernel-operating-system-p01] Thompson also stated that "It is also easier for [a monolithic kernel] to turn into a mess in a hurry as it is modified".
[src:kernel-hardening/kernel-operating-system-p01] Monolithic kernels, which have traditionally been used by Unix-like operating systems, contain all the operating system core functions and the device drivers.
[src:kernel-hardening/kernel-operating-system-p01] A monolithic kernel is one single program that contains all of the code necessary to perform every kernel-related task.
[src:kernel-hardening/kernel-operating-system-p01] Every part that is to be accessed by a program that cannot be put in a library is in the kernel space, including drivers, schedulers, memory management, file systems, and network stacks.
[src:kernel-hardening/kernel-operating-system-p01] Many system calls are provided to applications, to allow them to access all those services.
[src:kernel-hardening/kernel-operating-system-p01] A monolithic kernel, while initially loaded with subsystems that may not be needed, can be tuned to a point where it is as fast as or faster than one that was specifically designed for the hardware, although more relevant in a general sense.
[src:kernel-hardening/kernel-operating-system-p01] Most work in the monolithic kernel is done via system calls.
[src:kernel-hardening/kernel-operating-system-p01] These are interfaces, usually kept in a tabular structure, that access some subsystem within the kernel, such as disk operations.
[src:kernel-hardening/kernel-operating-system-p01] Essentially, calls are made within programs and a checked copy of the request is passed through the system call.
[src:kernel-hardening/kernel-operating-system-p01] The monolithic Linux kernel can be made extremely small not only because of its ability to dynamically load modules but also because of its ease of customization.
[src:kernel-hardening/kernel-operating-system-p01] In fact, there are some versions that are small enough to fit together with a large number of utilities and other programs on a single floppy disk and still provide a fully functional operating system (one of the most popular of which is muLinux).
[src:kernel-hardening/kernel-operating-system-p01] This ability to miniaturize its kernel has also led to a rapid growth in the use of Linux in embedded systems.
[src:kernel-hardening/kernel-operating-system-p01] These types of kernels consist of the core functions of the operating system and the device drivers with the ability to load modules at runtime.
[src:kernel-hardening/kernel-operating-system-p01] They provide rich and powerful abstractions of the underlying hardware.
[src:kernel-hardening/kernel-operating-system-p01] They provide a small set of simple hardware abstractions and use applications called servers to provide more functionality.
[src:kernel-hardening/kernel-operating-system-p01] This particular approach defines a high-level virtual interface over the hardware, with a set of system calls to implement operating system services such as process management, concurrency and memory management in several modules that run in supervisor mode.
[src:kernel-hardening/kernel-operating-system-p01] A microkernel that is designed for a specific platform or device is only ever going to have what it needs to operate.
[src:kernel-hardening/kernel-operating-system-p01] The microkernel approach consists of defining a simple abstraction over the hardware, with a set of primitives or system calls to implement minimal OS services such as memory management, multitasking, and inter-process communication.
[src:kernel-hardening/kernel-operating-system-p01] Other services, including those normally provided by the kernel, such as networking, are implemented in user-space programs, referred to as *servers*.
[src:kernel-hardening/kernel-operating-system-p01] Microkernels are easier to maintain than monolithic kernels, but the large number of system calls and context switches might slow down the system because they typically generate more overhead than plain function calls.
[src:kernel-hardening/kernel-operating-system-p01] Only parts that really require being in a privileged mode are in kernel space, such as IPC (Inter-Process Communication), a basic scheduler or scheduling primitives, basic memory handling, and basic I/O primitives.
[src:kernel-hardening/kernel-operating-system-p01] Many critical parts are now running in user space, including the complete scheduler, memory handling, file systems, and network stacks.
[src:kernel-hardening/kernel-operating-system-p01] Microkernels were invented as a reaction to traditional "monolithic" kernel design, whereby all system functionality was put in one static program running in a special "system" mode of the processor.
[src:kernel-hardening/kernel-operating-system-p01] In the microkernel, only the most fundamental of tasks are performed at this level, such as being able to access some (not necessarily all) of the hardware, manage memory and coordinate message passing between the processes.
[src:kernel-hardening/kernel-operating-system-p01] Some systems that use microkernels are QNX and GNU Hurd.
[src:kernel-hardening/kernel-operating-system-p01] In the case of QNX and GNU Hurd, user sessions can be entire snapshots of the system itself, or "views" as it is referred to.
[src:kernel-hardening/kernel-operating-system-p01] Most microkernels use a message passing system to handle requests from one server to another.
[src:kernel-hardening/kernel-operating-system-p01] The message passing system generally operates on a port basis with the microkernel.
[src:kernel-hardening/kernel-operating-system-p01] As an example, if a request for more memory is sent, a port is opened with the microkernel and the request sent through.
[src:kernel-hardening/kernel-operating-system-p01] Once within the microkernel, the steps are similar to system calls.
[src:kernel-hardening/kernel-operating-system-p01] The rationale was that it would bring modularity in the system architecture, which would entail a cleaner system that is easier to debug or dynamically modify, customizable to users' needs, and higher performing.
[src:kernel-hardening/kernel-operating-system-p01] Microkernels are part of operating systems like GNU Hurd, MINIX, MkLinux, QNX and Redox OS.
[src:kernel-hardening/kernel-operating-system-p01] Although microkernels are very small by themselves, in combination with all their required auxiliary code they are, in fact, often larger than monolithic kernels.
[src:kernel-hardening/kernel-operating-system-p01] Advocates of monolithic kernels also point out that the two-tiered structure of microkernel systems, in which most of the operating system does not interact directly with the hardware, creates a not-insignificant cost in terms of system efficiency.
[src:kernel-hardening/kernel-operating-system-p01] These types of kernels normally provide only the minimal services such as defining memory address spaces, inter-process communication (IPC) and process management.
[src:kernel-hardening/kernel-operating-system-p01] Other functions, such as running hardware processes, are not handled directly by microkernels.
[src:kernel-hardening/kernel-operating-system-p01] Proponents of microkernels point out those monolithic kernels have the disadvantage that an error in the kernel can cause the entire system to crash.
[src:kernel-hardening/kernel-operating-system-p01] With a microkernel, however, if a kernel process crashes, it is still possible to prevent a crash of the system as a whole by merely restarting the service that caused the error.
[src:kernel-hardening/kernel-operating-system-p01] Other services provided by the kernel such as networking are implemented in user-space programs referred to as *servers*.
[src:kernel-hardening/kernel-operating-system-p01] Servers allow the operating system to be modified by simply starting and stopping programs.
[src:kernel-hardening/kernel-operating-system-p01] For a machine without networking support, for instance, the networking server is not started.
[src:kernel-hardening/kernel-operating-system-p01] The task of moving in and out of the kernel to move data between the various applications and servers creates overhead that is detrimental to the efficiency of microkernels in comparison with monolithic kernels.
[src:kernel-hardening/kernel-operating-system-p01] Disadvantages in the microkernel exist, however.
[src:kernel-hardening/kernel-operating-system-p01] The disadvantages for microkernels are extremely context-based.
[src:kernel-hardening/kernel-operating-system-p01] As an example, they work well for small single-purpose (and critical) systems because if not many processes need to run, then the complications of process management are effectively mitigated.
[src:kernel-hardening/kernel-operating-system-p01] A microkernel allows the implementation of the remaining part of the operating system as programs running in user mode, and the use of different operating systems on top of the same unchanged kernel.
[src:kernel-hardening/kernel-operating-system-p01] It is also possible to dynamically switch among operating systems and to have more than one active simultaneously.
[src:kernel-hardening/kernel-operating-system-p01] As the computer kernel grows, so grows the size and vulnerability of its trusted computing base, in addition to its memory footprint.
[src:kernel-hardening/kernel-operating-system-p01] This is mitigated to some degree by perfecting a virtual memory system, but not all computer architectures have virtual memory support.
[src:kernel-hardening/kernel-operating-system-p01] To reduce the kernel's footprint, extensive editing has to be performed to carefully remove unneeded code, which can be very difficult with non-obvious interdependencies between parts of a kernel with millions of lines of code.
[src:kernel-hardening/kernel-operating-system-p01] By the early 1990s, due to the various shortcomings of monolithic kernels versus microkernels, monolithic kernels were considered obsolete by virtually all operating system researchers.
[src:kernel-hardening/kernel-operating-system-p01] As a result, the design of Linux as a monolithic kernel rather than a microkernel was the topic of a famous debate between Linus Torvalds and Andrew Tanenbaum.
[src:kernel-hardening/kernel-operating-system-p01] There is merit on both sides of the argument presented in the Tanenbaum–Torvalds debate.
[src:kernel-hardening/kernel-operating-system-p01] Monolithic kernels are designed to have all of their code in the same address space (kernel space), which some developers argue is necessary to increase the performance of the system.
[src:kernel-hardening/kernel-operating-system-p01] Some developers also maintain that monolithic systems are extremely efficient if well written.
[src:kernel-hardening/kernel-operating-system-p01] The monolithic model tends to be more efficient through the use of shared kernel memory, rather than the slower IPC system of microkernel designs, which is typically based on message passing.
[src:kernel-hardening/kernel-operating-system-p01] The performance of microkernels was poor in both the 1980s and early 1990s.
[src:kernel-hardening/kernel-operating-system-p01] Studies that empirically measured the performance of these microkernels did not analyze the reasons of such inefficiency.
[src:kernel-hardening/kernel-operating-system-p01] The explanations of this data were left to "folklore", with the assumption that they were due to the increased frequency of switches from "kernel-mode" to "user-mode", to the increased frequency of inter-process communication and to the increased frequency of context switches.
[src:kernel-hardening/kernel-operating-system-p01] In fact, as guessed in 1995, the reasons for the poor performance of microkernels might as well have been: (1) an actual inefficiency of the whole microkernel *approach*, (2) the particular *concepts* implemented in those microkernels, and (3) the particular *implementation* of those concepts.
[src:kernel-hardening/kernel-operating-system-p01] Therefore it remained to be studied if the solution to build an efficient microkernel was, unlike previous attempts, to apply the correct construction techniques.
[src:kernel-hardening/kernel-operating-system-p01] Hybrid kernels are used in some commercial operating systems, including most versions of Microsoft Windows to date (NT 3.1, NT 3.5, NT 3.51, NT 4.0, 2000, XP, Vista, 7, 8, 8.1, 10 and 11).
[src:kernel-hardening/kernel-operating-system-p01] Apple's macOS uses a hybrid kernel called XNU, which is based upon code from OSF/1's Mach kernel (OSFMK 7.3) and FreeBSD's monolithic kernel.
[src:kernel-hardening/kernel-operating-system-p01] Hybrid kernels are similar to microkernels, except they include some additional code in kernel-space to increase performance.
[src:kernel-hardening/kernel-operating-system-p01] These kernels represent a compromise that was implemented by some developers to accommodate the major advantages of both monolithic and microkernels.
[src:kernel-hardening/kernel-operating-system-p01] These types of kernels are extensions of microkernels with some properties of monolithic kernels.
[src:kernel-hardening/kernel-operating-system-p01] Unlike monolithic kernels, these types of kernels are unable to load modules at runtime on their own.
[src:kernel-hardening/kernel-operating-system-p01] This implies running some services (such as the network stack or the filesystem) in kernel space to reduce the performance overhead of a traditional microkernel, but still running kernel code (such as device drivers) as servers in user space.
[src:kernel-hardening/kernel-operating-system-p01] Many traditionally monolithic kernels support loadable kernel modules.
[src:kernel-hardening/kernel-operating-system-p01] The most well known of these kernels is the Linux kernel.
[src:kernel-hardening/kernel-operating-system-p01] The modular kernel essentially can have parts of it that are built into the core kernel binary or binaries that load into memory on demand.
[src:kernel-hardening/kernel-operating-system-p01] A code-tainted module has the potential to destabilize a running kernel.
[src:kernel-hardening/kernel-operating-system-p01] By contrast, it is possible to write a driver for a microkernel in a completely separate memory space and test it before "going" live.
[src:kernel-hardening/kernel-operating-system-p01] When a kernel module is loaded, it accesses the monolithic portion's memory space by adding to it what it needs, therefore opening the doorway to possible pollution.
[src:kernel-hardening/kernel-operating-system-p01] Modules, generally, communicate with the kernel using a module interface of some sort.
[src:kernel-hardening/kernel-operating-system-p01] The interface is generalized (although particular to a given operating system) so it is not always possible to use modules.
[src:kernel-hardening/kernel-operating-system-p01] Often the device drivers may need more flexibility than the module interface affords.
[src:kernel-hardening/kernel-operating-system-p01] Essentially, it is two system calls and often the safety checks that only have to be done once in the monolithic kernel now may be done twice.
[src:kernel-hardening/kernel-operating-system-p01] A nanokernel delegates virtually all services – including even the most basic ones like interrupt controllers or the timer – to device drivers to make the kernel memory requirement even smaller than a traditional microkernel.
[src:kernel-hardening/kernel-operating-system-p01] Exokernels are a still-experimental approach to operating system design.
[src:kernel-hardening/kernel-operating-system-p01] They differ from other types of kernels in limiting their functionality to the protection of and multiplexing of access to the raw hardware, providing no hardware abstractions on top of which to develop applications.
[src:kernel-hardening/kernel-operating-system-p01] This separation of hardware protection from hardware management enables application developers to determine how to make the most efficient use of the available hardware for each specific program.
[src:kernel-hardening/kernel-operating-system-p01] Exokernels in themselves are extremely small.
[src:kernel-hardening/kernel-operating-system-p01] Nonetheless, they are accompanied by library operating systems (see also unikernel), providing application developers with the functionalities of a conventional operating system.
[src:kernel-hardening/kernel-operating-system-p01] A major advantage of exokernel-based systems is that they can incorporate multiple library operating systems, each exporting a different API, for example one for high level UI development and one for real-time control.
[src:kernel-hardening/kernel-operating-system-p01] A multikernel operating system treats a multi-core machine as a network of independent cores, as if it were a distributed system.
[src:kernel-hardening/kernel-operating-system-p01] It does not assume shared memory but rather implements inter-process communications as message passing.
[src:kernel-hardening/kernel-operating-system-p01] Barrelfish was the first operating system to be described as a multikernel.
[src:kernel-hardening/kernel-operating-system-p02] Strictly speaking, an operating system (and thus, a kernel) is not *required* to run a computer.
[src:kernel-hardening/kernel-operating-system-p02] Programs can be directly loaded and executed on the "bare metal" machine, provided that the authors of those programs are willing to work without any hardware abstraction or operating system support.
[src:kernel-hardening/kernel-operating-system-p02] Most early computers operated this way during the 1950s and early 1960s, which were reset and reloaded between the execution of different programs.
[src:kernel-hardening/kernel-operating-system-p02] Eventually, small ancillary programs such as program loaders and debuggers were left in memory between runs, or loaded from ROM.
[src:kernel-hardening/kernel-operating-system-p02] As these were developed, they formed the basis of what became early operating system kernels.
[src:kernel-hardening/kernel-operating-system-p02] The "bare metal" approach is still used today on some video game consoles and embedded systems, but in general, newer computers use modern operating systems and kernels.
[src:kernel-hardening/kernel-operating-system-p02] In 1969, the RC 4000 Multiprogramming System introduced the system design philosophy of a small nucleus "upon which operating systems for different purposes could be built in an orderly manner", what would be called the microkernel approach.
[src:kernel-hardening/kernel-operating-system-p02] In the decade preceding Unix, computers had grown enormously in power – to the point where computer operators were looking for new ways to get people to use their spare time on their machines.
[src:kernel-hardening/kernel-operating-system-p02] One of the major developments during this era was time-sharing, whereby a number of users would get small slices of computer time, at a rate at which it appeared they were each connected to their own, slower, machine.
[src:kernel-hardening/kernel-operating-system-p02] The development of time-sharing systems led to a number of problems.
[src:kernel-hardening/kernel-operating-system-p02] One was that users, particularly at universities where the systems were being developed, wanted to hack the system to get more CPU time.
[src:kernel-hardening/kernel-operating-system-p02] For this reason, security and access control became a major focus of the Multics project in 1965.
[src:kernel-hardening/kernel-operating-system-p02] Another ongoing issue was properly handling computing resources: users spent most of their time staring at the terminal and thinking about what to input instead of actually using the resources of the computer, and a time-sharing system should give the CPU time to an active user during these periods.
[src:kernel-hardening/kernel-operating-system-p02] Finally, the systems typically offered a memory hierarchy several layers deep, and partitioning this expensive resource led to major developments in virtual memory systems.
[src:kernel-hardening/kernel-operating-system-p02] The Commodore Amiga was released in 1985, and was among the first – and certainly most successful – home computers to feature an advanced kernel architecture.
[src:kernel-hardening/kernel-operating-system-p02] The AmigaOS kernel's executive component, *exec.library*, uses a microkernel message-passing design, but there are other kernel components, like *graphics.library*, that have direct access to the hardware.
[src:kernel-hardening/kernel-operating-system-p02] There is no memory protection, and the kernel is almost always running in user mode.
[src:kernel-hardening/kernel-operating-system-p02] Only special actions are executed in kernel mode, and user-mode applications can ask the operating system to execute their code in kernel mode.
[src:kernel-hardening/kernel-operating-system-p02] During the design phase of Unix, programmers decided to model every high-level device as a file, because they believed the purpose of computation was data transformation.
[src:kernel-hardening/kernel-operating-system-p02] For instance, printers were represented as a "file" at a known location – when data was copied to the file, it printed out.
[src:kernel-hardening/kernel-operating-system-p02] Other systems, to provide a similar functionality, tended to virtualize devices at a lower level – that is, both devices *and* files would be instances of some lower level concept.
[src:kernel-hardening/kernel-operating-system-p02] Virtualizing the system at the file level allowed users to manipulate the entire system using their existing file management utilities and concepts, dramatically simplifying operation.
[src:kernel-hardening/kernel-operating-system-p02] As an extension of the same paradigm, Unix allows programmers to manipulate files using a series of small programs, using the concept of pipes, which allowed users to complete operations in stages, feeding a file through a chain of single-purpose tools.
[src:kernel-hardening/kernel-operating-system-p02] Although the end result was the same, using smaller programs in this way dramatically increased flexibility as well as ease of development and use, allowing the user to modify their workflow by adding or removing a program from the chain.
[src:kernel-hardening/kernel-operating-system-p02] In the Unix model, the *operating system* consists of two parts: first, the huge collection of utility programs that drive most operations; second, the kernel that runs the programs.
[src:kernel-hardening/kernel-operating-system-p02] Over the years the computing model changed, and Unix's treatment of everything as a file or byte stream no longer was as universally applicable as it was before.
[src:kernel-hardening/kernel-operating-system-p02] Although a terminal could be treated as a file or a byte stream, which is printed to or read from, the same did not seem to be true for a graphical user interface.
[src:kernel-hardening/kernel-operating-system-p02] Even if network communication can be compared to file access, the low-level packet-oriented architecture dealt with discrete chunks of data and not with whole files.
[src:kernel-hardening/kernel-operating-system-p02] As the capability of computers grew, Unix became increasingly cluttered with code.
[src:kernel-hardening/kernel-operating-system-p02] It is also because the modularity of the Unix kernel is extensively scalable.
[src:kernel-hardening/kernel-operating-system-p02] While kernels might have had 100,000 lines of code in the seventies and eighties, kernels like Linux, of modern Unix successors like GNU, have more than 13 million lines.
[src:kernel-hardening/kernel-operating-system-p02] Modern Unix-derivatives are generally based on module-loading monolithic kernels.
[src:kernel-hardening/kernel-operating-system-p02] Examples of this are the Linux kernel in the many distributions of GNU, IBM AIX, as well as the Berkeley Software Distribution variant kernels such as FreeBSD, DragonFly BSD, OpenBSD, NetBSD, and macOS.
[src:kernel-hardening/kernel-operating-system-p02] Apart from these alternatives, amateur developers maintain an active operating system development community, populated by self-written hobby kernels that mostly end up sharing many features with Linux, FreeBSD, DragonflyBSD, OpenBSD, or NetBSD kernels and/or being compatible with them.
[src:kernel-hardening/kernel-operating-system-p02] Apple first launched its classic Mac OS in 1984, bundled with its Macintosh personal computer.
[src:kernel-hardening/kernel-operating-system-p02] Apple moved to a nanokernel design in Mac OS 8.6.
[src:kernel-hardening/kernel-operating-system-p02] Against this, the modern macOS (originally named Mac OS X) is based on Darwin, which uses a hybrid kernel called XNU, which was created by combining the 4.3BSD kernel and the Mach kernel.
[src:kernel-hardening/kernel-operating-system-p02] Microsoft Windows was first released in 1985 as an add-on to MS-DOS.
[src:kernel-hardening/kernel-operating-system-p02] Because of its dependence on another operating system, initial releases of Windows, prior to Windows 95, were considered an operating environment (not to be confused with an operating system).
[src:kernel-hardening/kernel-operating-system-p02] This product line continued to evolve through the 1980s and 1990s, with the Windows 9x series adding 32-bit addressing and pre-emptive multitasking; but ended with the release of Windows Me in 2000.
[src:kernel-hardening/kernel-operating-system-p02] Microsoft also developed Windows NT, an operating system with a very similar interface, but intended for high-end and business users.
[src:kernel-hardening/kernel-operating-system-p02] This line started with the release of Windows NT 3.1 in 1993, and was introduced to general users with the release of Windows XP in October 2001—replacing Windows 9x with a completely different, much more sophisticated operating system.
[src:kernel-hardening/kernel-operating-system-p02] This is the line that continues with Windows 11.
[src:kernel-hardening/kernel-operating-system-p02] The architecture of Windows NT's kernel is considered a hybrid kernel because the kernel itself contains tasks such as the Window Manager and the IPC Managers, with a client/server layered subsystem model.
[src:kernel-hardening/kernel-operating-system-p02] It was designed as a modified microkernel, as the Windows NT kernel was influenced by the Mach microkernel but does not meet all of the criteria of a pure microkernel.
[src:kernel-hardening/kernel-operating-system-p02] Supervisory program or supervisor is a computer program, usually part of an operating system, that controls the execution of other routines and regulates work scheduling, input/output operations, error actions, and similar functions and regulates the flow of work in a data processing system.
[src:kernel-hardening/kernel-operating-system-p02] Historically, this term was essentially associated with IBM's line of mainframe operating systems starting with OS/360.
[src:kernel-hardening/kernel-operating-system-p02] In other operating systems, the supervisor is generally called the kernel.
[src:kernel-hardening/kernel-operating-system-p02] In the 1970s, IBM further abstracted the supervisor state from the hardware, resulting in a hypervisor that enabled full virtualization, i.e. the capacity to run multiple operating systems on the same machine totally independent of each other.
[src:kernel-hardening/kernel-operating-system-p02] Hence the first such system was called *Virtual Machine* or *VM*.
[src:kernel-hardening/kernel-operating-system-p02] Although Mach, developed by Richard Rashid at Carnegie Mellon University, is the best-known general-purpose microkernel, other microkernels have been developed with more specific aims.
[src:kernel-hardening/kernel-operating-system-p02] The L4 microkernel family (mainly the L3 and the L4 kernel) was created to demonstrate that microkernels are not necessarily slow.
[src:kernel-hardening/kernel-operating-system-p02] Newer implementations such as Fiasco and Pistachio are able to run Linux next to other L4 processes in separate address spaces.
[src:kernel-hardening/kernel-operating-system-p02] Additionally, QNX is a microkernel that is principally used in embedded systems, and the open-source software MINIX, while originally created for educational purposes, is now focused on being a highly reliable and self-healing microkernel OS.
[src:kernel-hardening/kernel-page-table-isolation] The KPTI patches were based on KAISER (short for *Kernel Address Isolation to have Side-channels Efficiently Removed*), a technique conceived in 2016 and published in June 2017 back when Meltdown was not known yet.
[src:kernel-hardening/kernel-page-table-isolation] KAISER makes it harder to defeat KASLR, a 2014 mitigation for a much less severe issue.
[src:kernel-hardening/kernel-page-table-isolation] In 2014, the Linux kernel adopted kernel address space layout randomization (KASLR), which makes it more difficult to exploit other kernel vulnerabilities, which relies on kernel address mappings remaining hidden from user space.
[src:kernel-hardening/kernel-page-table-isolation] Despite prohibiting access to these kernel mappings, it turns out that there are several side-channel attacks in modern processors that can leak the location of this memory, making it possible to work around KASLR.
[src:kernel-hardening/kernel-page-table-isolation] KAISER addressed these problems in KASLR by eliminating some sources of address leakage.
[src:kernel-hardening/kernel-page-table-isolation] Whereas KASLR merely prevents address mappings from leaking, KAISER also prevents the data from leaking, thereby covering the Meltdown case.
[src:kernel-hardening/kernel-page-table-isolation] Without KPTI enabled, whenever executing user-space code (applications), Linux would also keep its entire kernel memory mapped in page tables, although protected from access.
[src:kernel-hardening/kernel-page-table-isolation] The advantage is that when the application makes a system call into the kernel or an interrupt is received, kernel page tables are always present, so most context switching-related overheads (TLB flush, page-table swapping, etc.) can be avoided.
[src:kernel-hardening/kernel-page-table-isolation] In January 2018, the Meltdown vulnerability was published, known to affect Intel's x86 CPUs and ARM Cortex-A75.
[src:kernel-hardening/kernel-page-table-isolation] It was a far more severe vulnerability than the KASLR bypass that KAISER originally intended to fix: It was found that *contents* of kernel memory could also be leaked, not just the locations of memory mappings, as previously thought.
[src:kernel-hardening/kernel-page-table-isolation] KPTI (conceptually based on KAISER) prevents Meltdown by preventing most protected locations from being mapped to user space.
[src:kernel-hardening/kernel-page-table-isolation] AMD x86 processors are not currently known to be affected by Meltdown and don't need KPTI to mitigate them.
[src:kernel-hardening/kernel-page-table-isolation] However, AMD processors are still susceptible to KASLR bypass when KPTI is disabled.
[src:kernel-hardening/kernel-page-table-isolation] KPTI fixes these leaks by separating user-space and kernel-space page tables entirely.
[src:kernel-hardening/kernel-page-table-isolation] One set of page tables includes both kernel-space and user-space addresses same as before, but it is only used when the system is running in kernel mode.
[src:kernel-hardening/kernel-page-table-isolation] The second set of page tables for use in user mode contains a copy of user-space and a minimal set of kernel-space mappings that provides the information needed to enter or exit system calls, interrupts and exceptions.
[src:kernel-hardening/kernel-page-table-isolation] On processors that support the process-context identifiers (PCID), a translation lookaside buffer (TLB) flush can be avoided, but even then it comes at a significant performance cost, particularly in syscall-heavy and interrupt-heavy workloads.
[src:kernel-hardening/kernel-page-table-isolation] Many benchmarks have been done by Phoronix, Redis slowed by 6–7%.
[src:kernel-hardening/kernel-page-table-isolation] Linux kernel compilation slowed down by 5% on Haswell.
[src:kernel-hardening/kernel-page-table-isolation] KPTI can partially be disabled with the "nopti" kernel boot option.
[src:kernel-hardening/kernel-page-table-isolation] Also provisions were created to disable KPTI if newer processors fix the information leaks.
[src:kernel-hardening/memory-protection] Segmentation refers to dividing a computer's memory into segments.
[src:kernel-hardening/memory-protection] A reference to a memory location includes a value that identifies a segment and an offset within that segment.
[src:kernel-hardening/memory-protection] A segment descriptor may limit access rights, e.g., read only, only from certain rings.
[src:kernel-hardening/memory-protection] The x86 architecture has multiple segmentation features, which are helpful for using protected memory on this architecture.
[src:kernel-hardening/memory-protection] On the x86 architecture, the Global Descriptor Table and Local Descriptor Tables can be used to reference segments in the computer's memory.
[src:kernel-hardening/memory-protection] Pointers to memory segments on x86 processors can also be stored in the processor's segment registers.
[src:kernel-hardening/memory-protection] Initially x86 processors had 4 segment registers, CS (code segment), SS (stack segment), DS (data segment) and ES (extra segment); later another two segment registers were added – FS and GS.
[src:kernel-hardening/memory-protection] In paging the memory address space or segment is divided into equal-sized blocks called pages.
[src:kernel-hardening/memory-protection] Using virtual memory hardware, each page can reside in any location at a suitable boundary of the computer's physical memory, or be flagged as being protected.
[src:kernel-hardening/memory-protection] Virtual memory makes it possible to have a linear virtual memory address space and to use it to access blocks fragmented over physical memory address space.
[src:kernel-hardening/memory-protection] Most computer architectures which support paging also use pages as the basis for memory protection.
[src:kernel-hardening/memory-protection] A *page table* maps virtual memory to physical memory.
[src:kernel-hardening/memory-protection] There may be a single page table, a page table for each process, a page table for each segment, or a hierarchy of page tables, depending on the architecture and the OS.
[src:kernel-hardening/memory-protection] The page tables are usually invisible to the process.
[src:kernel-hardening/memory-protection] Page tables make it easier to allocate additional memory, as each new page can be allocated from anywhere in physical memory.
[src:kernel-hardening/memory-protection] On some systems a page table entry can also designate a page as read-only.
[src:kernel-hardening/memory-protection] Some operating systems set up a different address space for each process, which provides hard memory protection boundaries.
[src:kernel-hardening/memory-protection] It is impossible for an unprivileged application to access a page that has not been explicitly allocated to it, because every memory address either points to a page allocated to that application, or generates an interrupt called a *page fault*.
[src:kernel-hardening/memory-protection] Unallocated pages, and pages allocated to any other application, do not have any addresses from the application point of view.
[src:kernel-hardening/memory-protection] A page fault may not necessarily indicate an error.
[src:kernel-hardening/memory-protection] Page faults are not only used for memory protection.
[src:kernel-hardening/memory-protection] The operating system may manage the page table in such a way that a reference to a page that has been previously paged out to secondary storage causes a page fault.
[src:kernel-hardening/memory-protection] The operating system intercepts the page fault, loads the required memory page, and the application continues as if no fault had occurred.
[src:kernel-hardening/memory-protection] This scheme, a type of virtual memory, allows in-memory data not currently in use to be moved to secondary storage and back in a way which is transparent to applications, to increase overall memory capacity.
[src:kernel-hardening/memory-protection] On some systems, a request for virtual storage may allocate a block of virtual addresses for which no page frames have been assigned, and the system will only assign and initialize page frames when page faults occur.
[src:kernel-hardening/memory-protection] On some systems a guard page may be used, either for error detection or to automatically grow data structures.
[src:kernel-hardening/memory-protection] On some systems, the page fault mechanism is also used for executable-space protection such as W^X.
[src:kernel-hardening/memory-protection] A memory protection key (MPK) mechanism divides physical memory into blocks of a particular size (e.g., 4 KiB), each of which has an associated numerical value called a protection key.
[src:kernel-hardening/memory-protection] Each process also has a protection key value associated with it.
[src:kernel-hardening/memory-protection] On a memory access the hardware checks that the current process's protection key matches the value associated with the memory block being accessed; if not, an exception occurs.
[src:kernel-hardening/memory-protection] This mechanism was introduced in the System/360 architecture.
[src:kernel-hardening/memory-protection] It is available on today's System z mainframes and heavily used by System z operating systems and their subsystems.
[src:kernel-hardening/memory-protection] The System/360 protection keys described above are associated with physical addresses.
[src:kernel-hardening/memory-protection] This is different from the protection key mechanism used by architectures such as the Hewlett-Packard/Intel IA-64 and Hewlett-Packard PA-RISC, which are associated with virtual addresses, and which allow multiple keys per process.
[src:kernel-hardening/memory-protection] In the Itanium and PA-RISC architectures, translations (TLB entries) have *keys* (Itanium) or *access ids* (PA-RISC) associated with them.
[src:kernel-hardening/memory-protection] A running process has several protection key registers (16 for Itanium, 4 for PA-RISC).
[src:kernel-hardening/memory-protection] A translation selected by the virtual address has its key compared to each of the protection key registers.
[src:kernel-hardening/memory-protection] If any of them match (plus other possible checks), the access is permitted.
[src:kernel-hardening/memory-protection] If none match, a fault or exception is generated.
[src:kernel-hardening/memory-protection] The software fault handler can, if desired, check the missing key against a larger list of keys maintained by software; thus, the protection key registers inside the processor may be treated as a software-managed cache of a larger list of keys associated with a process.
[src:kernel-hardening/memory-protection] PA-RISC has 15–18 bits of key; Itanium mandates at least 18.
[src:kernel-hardening/memory-protection] Keys are usually associated with *protection domains*, such as libraries, modules, etc.
[src:kernel-hardening/memory-protection] In the x86, the protection keys architecture allows tagging virtual addresses for user pages with any of 16 protection keys.
[src:kernel-hardening/memory-protection] All the pages tagged with the same protection key constitute a protection domain.
[src:kernel-hardening/memory-protection] A new register contains the permissions associated with each of the protection domain.
[src:kernel-hardening/memory-protection] Load and store operations are checked against both the page table permissions and the protection key permissions associated with the protection domain of the virtual address, and only allowed if both permissions allow the access.
[src:kernel-hardening/memory-protection] The protection key permissions can be set from user space, allowing applications to directly restrict access to the application data without OS intervention.
[src:kernel-hardening/memory-protection] Since the protection keys are associated with a virtual address, the protection domains are per address space, so processes running in different address spaces can each use all 16 domains.
[src:kernel-hardening/memory-protection] In Multics and systems derived from it, each segment has a protection ring for reading, writing and execution; an attempt by a process with a higher ring number than the ring number for the segment causes a fault.
[src:kernel-hardening/memory-protection] There is a mechanism for safely calling procedures that run in a lower ring and returning to the higher ring.
[src:kernel-hardening/memory-protection] There are mechanisms for a routine running with a low ring number to access a parameter with the larger of its own ring and the caller's ring.
[src:kernel-hardening/memory-protection] Simulation is the use of a monitoring program to interpret the machine code instructions of some computer architectures.
[src:kernel-hardening/memory-protection] Such an instruction set simulator can provide memory protection by using a segmentation-like scheme and validating the target address and length of each instruction in real time before actually executing them.
[src:kernel-hardening/memory-protection] The simulator must calculate the target address and length and compare this against a list of valid address ranges that it holds concerning the thread's environment, such as any dynamic memory blocks acquired since the thread's inception, plus any valid shared static memory slots.
[src:kernel-hardening/memory-protection] The meaning of "valid" may change throughout the thread's life depending upon context.
[src:kernel-hardening/memory-protection] It may sometimes be allowed to alter a static block of storage, and sometimes not, depending upon the current mode of execution, which may or may not depend on a storage key or supervisor state.
[src:kernel-hardening/memory-protection] It is generally not advisable to use this method of memory protection where adequate facilities exist on a CPU, as this takes valuable processing power from the computer.
[src:kernel-hardening/memory-protection] Capability-based addressing is a method of memory protection that is unused in modern commercial computers.
[src:kernel-hardening/memory-protection] In this method, pointers are replaced by protected objects (called *capabilities*) that can only be created using privileged instructions which may only be executed by the kernel, or some other process authorized to do so.
[src:kernel-hardening/memory-protection] This effectively lets the kernel control which processes may access which objects in memory, with no need to use separate address spaces or context switches.
[src:kernel-hardening/memory-protection] Only a few commercial products used capability based security: Plessey System 250, IBM System/38, Intel iAPX 432 architecture and KeyKOS.
[src:kernel-hardening/memory-protection] Capability approaches are widely used in research systems such as EROS and Combex DARPA browser.
[src:kernel-hardening/memory-protection] They are used conceptually as the basis for some virtual machines, most notably Smalltalk and Java.
[src:kernel-hardening/memory-protection] Currently, the DARPA-funded CHERI project at University of Cambridge is working to create a modern capability machine that also supports legacy software.
[src:kernel-hardening/memory-protection] Dynamic tainting is a technique for protecting programs from illegal memory accesses.
[src:kernel-hardening/memory-protection] When memory is allocated, at runtime, this technique taints both the memory and the corresponding pointer using the same taint mark.
[src:kernel-hardening/memory-protection] Taint marks are then suitably propagated while the program executes and are checked every time a memory address *m* is accessed through a pointer *p*; if the taint marks associated with *m* and *p* differ, the execution is stopped and the illegal access is reported.
[src:kernel-hardening/memory-protection] SPARC M7 processors (and higher) implement dynamic tainting in hardware.
[src:kernel-hardening/memory-protection] Oracle markets this feature as Silicon Secured Memory (SSM) (previously branded as Application Data Integrity (ADI)).
[src:kernel-hardening/memory-protection] The lowRISC CPU design includes dynamic tainting under the name Tagged Memory.
[src:kernel-hardening/memory-protection] The protection level of a particular implementation may be measured by how closely it adheres to the principle of minimum privilege.
[src:kernel-hardening/memory-protection] Different operating systems use different forms of memory protection or separation.
[src:kernel-hardening/memory-protection] Although memory protection was common on most mainframes and many minicomputer systems from the 1960s, true memory separation was not used in home computer operating systems until OS/2 (and in RISC OS) was released in 1987.
[src:kernel-hardening/memory-protection] On prior systems, such lack of protection was even used as a form of interprocess communication, by sending a pointer between processes.
[src:kernel-hardening/memory-protection] It is possible for processes to access System Memory in the Windows 9x family of operating systems.
[src:kernel-hardening/memory-protection] On Unix-like systems, the `mprotect` system call is used to control memory protection.
