# nl-rag distilled facts — domain: epoll-kqueue
# one declarative sentence per line; load with:  neuron --db app.db import pack.facts --scope knowledge
[src:epoll-kqueue/epoll-create] epoll_create() creates a new epoll(7) instance.
[src:epoll-kqueue/epoll-create] subsequent calls to the epoll interface.
[src:epoll-kqueue/epoll-create] releases the associated resources for reuse.
[src:epoll-kqueue/epoll-create] open(2) for reasons why this may be useful.
[src:epoll-kqueue/epoll-create] EINVAL (epoll_create1()) Invalid value specified in flags.
[src:epoll-kqueue/epoll-create] ENOMEM There was insufficient memory to create the kernel object.
[src:epoll-kqueue/epoll-create] caller expected to add to the epoll instance.
[src:epoll-kqueue/epoll-create] allocate in internal data structures describing events.
[src:epoll-kqueue/epoll-create] new epoll applications are run on older kernels.
[src:epoll-kqueue/epoll-create] caused epoll_create() to fail with EMFILE on overrun.
[src:epoll-kqueue/epoll-ctl] The associated file is available for read(2) operations.
[src:epoll-kqueue/epoll-ctl] The associated file is available for write(2) operations.
[src:epoll-kqueue/epoll-ctl] There is an exceptional condition on the file descriptor.
[src:epoll-kqueue/epoll-ctl] See the discussion of POLLPRI in poll(2).
[src:epoll-kqueue/epoll-ctl] Error condition happened on the associated file descriptor.
[src:epoll-kqueue/epoll-ctl] necessary to set it in events when calling epoll_ctl().
[src:epoll-kqueue/epoll-ctl] Hang up happened on the associated file descriptor.
[src:epoll-kqueue/epoll-ctl] outstanding data in the channel has been consumed.
[src:epoll-kqueue/epoll-ctl] about edge-triggered and level-triggered notification.
[src:epoll-kqueue/epoll-ctl] event file descriptor with EPOLL_CTL_MOD.
[src:epoll-kqueue/epoll-ctl] that is being attached to the target file descriptor, fd.
[src:epoll-kqueue/epoll-ctl] will receive an event with epoll_wait(2).
[src:epoll-kqueue/epoll-ctl] epoll file descriptors to receive an event.
[src:epoll-kqueue/epoll-ctl] instances that did specify EPOLLEXCLUSIVE.
[src:epoll-kqueue/epoll-ctl] file descriptor fd as an epoll instance will likewise fail.
[src:epoll-kqueue/epoll-ctl] The error in all of these cases is EINVAL.
[src:epoll-kqueue/epoll-ctl] When successful, epoll_ctl() returns zero.
[src:epoll-kqueue/epoll-ctl] epoll_ctl() returns -1 and errno is set to indicate the error.
[src:epoll-kqueue/epoll-ctl] EBADF epfd or fd is not a valid file descriptor.
[src:epoll-kqueue/epoll-ctl] is already registered with this epoll instance.
[src:epoll-kqueue/epoll-ctl] EINVAL op was EPOLL_CTL_MOD and events included EPOLLEXCLUSIVE.
[src:epoll-kqueue/epoll-ctl] previously been applied to this epfd, fd pair.
[src:epoll-kqueue/epoll-ctl] new file descriptor on an epoll instance.
[src:epoll-kqueue/epoll-ctl] EPERM The target file fd does not support epoll.
[src:epoll-kqueue/epoll-ctl] null pointer in event, even though this argument is ignored.
[src:epoll-kqueue/epoll-ctl] before Linux 2.6.9 should specify a non-null pointer in event.
[src:epoll-kqueue/epoll-ctl] capability if attempting to use the EPOLLWAKEUP flag.
[src:epoll-kqueue/epoll] `epoll` is similar to FreeBSD's `kqueue`, in that it consists of a set of user-space functions, each taking a file descriptor argument denoting the configurable kernel object, against which they cooperatively operate.
[src:epoll-kqueue/epoll] `epoll` uses a red–black tree (RB-tree) data structure to keep track of all file descriptors that are currently being monitored.
[src:epoll-kqueue/epoll] Creates an `epoll` object and returns its file descriptor.
[src:epoll-kqueue/epoll] The `flags` parameter allows epoll behavior to be modified.
[src:epoll-kqueue/epoll] It has only one valid value, `EPOLL_CLOEXEC`.
[src:epoll-kqueue/epoll] `epoll_create()` is an older variant of `epoll_create1()` and is deprecated as of Linux kernel version 2.6.27 and glibc version 2.9.
[src:epoll-kqueue/epoll] Controls (configures) which file descriptors are watched by this object, and for which events.
[src:epoll-kqueue/epoll] Waits for any of the events registered for with `epoll_ctl`, until at least one occurs or the timeout elapses.
[src:epoll-kqueue/epoll] Returns the occurred events in `events`, up to `maxevents` at once.
[src:epoll-kqueue/epoll] `maxevents` is the maximum number of `epoll_event`/file descriptors to be monitored.
[src:epoll-kqueue/epoll] In most case, `maxevents` is set to the value of the size of `*events` argument (`struct epoll_event* events` array).
[src:epoll-kqueue/epoll] `epoll` provides both edge-triggered and level-triggered modes.
[src:epoll-kqueue/epoll] In edge-triggered mode, a call to `epoll_wait` will return only when a new event is enqueued with the `epoll` object, while in level-triggered mode, `epoll_wait` will return as long as the condition holds.
[src:epoll-kqueue/epoll] For instance, if a pipe registered with `epoll` has received data, a call to `epoll_wait` will return, signaling the presence of data to be read.
[src:epoll-kqueue/epoll] Suppose, the reader only consumed part of data from the buffer.
[src:epoll-kqueue/epoll] In level-triggered mode, further calls to `epoll_wait` will return immediately, as long as the pipe's buffer contains data to be read.
[src:epoll-kqueue/epoll] In edge-triggered mode, however, `epoll_wait` will return only once new data is written to the pipe.
[src:epoll-kqueue/epoll] Bryan Cantrill pointed out that `epoll` had mistakes that could have been avoided, had it learned from its predecessors: input/output completion ports, event ports (Solaris) and kqueue.
[src:epoll-kqueue/epoll] However, a large part of his criticism was addressed by `epoll`'s `EPOLLONESHOT` and `EPOLLEXCLUSIVE` options.
[src:epoll-kqueue/epoll] `EPOLLONESHOT` was added in version 2.6.2 of the Linux kernel mainline, released in February 2004.
[src:epoll-kqueue/epoll] `EPOLLEXCLUSIVE` was added in version 4.5, released in March 2016.
[src:epoll-kqueue/kqueue] Kqueue provides efficient input and output event pipelines between the kernel and userland.
[src:epoll-kqueue/kqueue] Thus, it is possible to modify event filters as well as receive pending events while using only a single system call to `kevent(2)` per main event loop iteration.
[src:epoll-kqueue/kqueue] This contrasts with older traditional polling system calls such as `poll(2)` and `select(2)` which are less efficient, especially when polling for events on numerous file descriptors.
[src:epoll-kqueue/kqueue] Kqueue not only handles file descriptor events but is also used for various other notifications such as file modification monitoring, signals, asynchronous I/O events (AIO), child process state change monitoring, and timers which support nanosecond resolution.
[src:epoll-kqueue/kqueue] Furthermore, kqueue provides a way to use user-defined events in addition to the ones provided by the kernel.
[src:epoll-kqueue/kqueue] Some other operating systems which traditionally only supported `select(2)` and `poll(2)` also currently provide more efficient polling alternatives, such as epoll on Linux and I/O completion ports on Windows and Solaris.
[src:epoll-kqueue/kqueue] `libkqueue` is a user space implementation of `kqueue(2)`, which translates calls to an operating system's native backend event mechanism.
[src:epoll-kqueue/kqueue] The function prototypes and types are found in `<sys/event.h>`.
[src:epoll-kqueue/kqueue] Creates a new kernel event queue and returns a descriptor.
[src:epoll-kqueue/kqueue] Used to register events with the queue, then wait for and return any pending events to the user.
[src:epoll-kqueue/kqueue] In contrast to epoll, kqueue uses the same function to register and wait for events, and multiple event sources may be registered and modified using a single call.
[src:epoll-kqueue/kqueue] The `changelist` array can be used to pass modifications (changing the type of events to wait for, register new event sources, etc.) to the event queue, which are applied before waiting for events begins.
[src:epoll-kqueue/kqueue] `nevents` is the size of the user supplied `eventlist` array that is used to receive events from the event queue.
[src:epoll-kqueue/kqueue] A macro that is used for convenient initialization of a `struct kevent` object.
[src:epoll-kqueue/select] modern applications—and this limitation will not change.
[src:epoll-kqueue/select] for some class of I/O operation (e.g., input possible).
[src:epoll-kqueue/select] A structure type that can represent a set of file descriptors.
[src:epoll-kqueue/select] fd_set structure is the value of the macro FD_SETSIZE.
[src:epoll-kqueue/select] This macro clears (removes all file descriptors from) set.
[src:epoll-kqueue/select] This macro adds the file descriptor fd to set.
[src:epoll-kqueue/select] This macro removes the file descriptor fd from set.
[src:epoll-kqueue/select] is a no-op, and does not produce an error.
[src:epoll-kqueue/select] a file descriptor is also ready on end-of-file.
[src:epoll-kqueue/select] writing if a write operation will not block.
[src:epoll-kqueue/select] conditions, see the discussion of POLLPRI in poll(2).
[src:epoll-kqueue/select] descriptor in any of the three sets, plus 1.
[src:epoll-kqueue/select] waiting for a file descriptor to become ready.
[src:epoll-kqueue/select] that the blocking interval may overrun by a small amount.
[src:epoll-kqueue/select] indefinitely waiting for a file descriptor to become ready.
[src:epoll-kqueue/select] time was left. pselect() does not change this argument.
[src:epoll-kqueue/select] and then restores the original signal mask.
[src:epoll-kqueue/select] an atomic test is needed to prevent race conditions.
[src:epoll-kqueue/select] signal handler sets a global flag and returns.
[src:epoll-kqueue/select] not slept; most other implementations do not do this.
[src:epoll-kqueue/select] multiple select()s in a loop without reinitializing it.
[src:epoll-kqueue/select] timeout to be undefined after select() returns.
[src:epoll-kqueue/select] before any file descriptors became ready.
[src:epoll-kqueue/select] EBADF An invalid file descriptor was given in one of the sets.
[src:epoll-kqueue/select] on which an error has occurred.) However, see BUGS.
[src:epoll-kqueue/select] EINTR A signal was caught; see signal(7).
[src:epoll-kqueue/select] EINVAL The value contained within timeout is invalid.
[src:epoll-kqueue/select] ENOMEM Unable to allocate memory for internal tables.
[src:epoll-kqueue/select] check for EAGAIN and loop, just as with EINTR.
[src:epoll-kqueue/select] Prior to this, it was emulated in glibc (but see BUGS).
[src:epoll-kqueue/select] The following header also provides the fd_set type: <sys/time.h>.
[src:epoll-kqueue/select] FD_SETSIZE will result in undefined behavior.
[src:epoll-kqueue/select] requires fd to be a valid file descriptor.
[src:epoll-kqueue/select] signal trapping can be achieved using the self-pipe trick.
[src:epoll-kqueue/select] other end is monitored by select() in the main program.
[src:epoll-kqueue/select] another thread, the result is unspecified.
[src:epoll-kqueue/select] descriptor in another thread has no effect on select().
[src:epoll-kqueue/select] The underlying Linux system call is named pselect6().
[src:epoll-kqueue/select] The Linux pselect6() system call modifies its timeout argument.
[src:epoll-kqueue/select] architectures support a maximum of 6 arguments to a system call.
[src:epoll-kqueue/select] kernel and libc notion of the signal set.
[src:epoll-kqueue/select] order to obtain the declaration of pselect() from <sys/select.h>.
[src:epoll-kqueue/select] descriptors that can be specified in a file descriptor set.
[src:epoll-kqueue/select] and the FD_*() macros operating according to that limit.
[src:epoll-kqueue/select] descriptor number that the process currently has open.
[src:epoll-kqueue/select] that was implemented using sigprocmask(2) and select().
[src:epoll-kqueue/select] for reading", while nevertheless a subsequent read blocks.
[src:epoll-kqueue/select] examination has the wrong checksum and is discarded.
[src:epoll-kqueue/select] interrupted by a signal handler (i.e., the EINTR error return).
[src:epoll-kqueue/select] /* Watch stdin (fd 0) to see when it has input.
[src:epoll-kqueue/select] For a tutorial with discussion and examples, see select_tut(2).
