# nl-rag distilled facts — domain: hopscotch-hashing
# one declarative sentence per line; load with:  neuron --db app.db import pack.facts --scope knowledge
[src:hopscotch-hashing/cuckoo-hashing] Cuckoo hashing was first described by Rasmus Pagh and Flemming Friche Rodler in a 2001 conference paper.
[src:hopscotch-hashing/cuckoo-hashing] The paper was awarded the European Symposium on Algorithms Test-of-Time award in 2020.
[src:hopscotch-hashing/cuckoo-hashing] Cuckoo hashing is a form of open addressing in which each non-empty cell of a hash table contains a key or key–value pair.
[src:hopscotch-hashing/cuckoo-hashing] A hash function is used to determine the location for each key, and its presence in the table (or the value associated with it) can be found by examining that cell of the table.
[src:hopscotch-hashing/cuckoo-hashing] However, open addressing suffers from collisions, which happens when more than one key is mapped to the same cell.
[src:hopscotch-hashing/cuckoo-hashing] The basic idea of cuckoo hashing is to resolve collisions by using two hash functions instead of only one.
[src:hopscotch-hashing/cuckoo-hashing] This provides two possible locations in the hash table for each key.
[src:hopscotch-hashing/cuckoo-hashing] In one of the commonly used variants of the algorithm, the hash table is split into two smaller tables of equal size, and each hash function provides an index into one of these two tables.
[src:hopscotch-hashing/cuckoo-hashing] It is also possible for both hash functions to provide indexes into a single table.
[src:hopscotch-hashing/cuckoo-hashing] Cuckoo hashing uses two hash tables, $T_{1}$ and $T_{2}$ .
[src:hopscotch-hashing/cuckoo-hashing] Assuming r is the length of each table, the hash functions for the two tables is defined as, ${\displaystyle h_{1},\ h_{2}\$ and $\forall x\in S$ where x is the key and S is the set whose keys are stored in $h_{1}(x)$ of $T_{1}$ or $h_{2}(x)$ of $T_{2}$ .
[src:hopscotch-hashing/cuckoo-hashing] The logical or ( $\vee$ ) denotes that, the value of the key x is found in either $T_{1}$ or $T_{2}$ , which is $O(1)$ in worst case.
[src:hopscotch-hashing/cuckoo-hashing] Deletion is performed in $O(1)$ time since probing is not involved.
[src:hopscotch-hashing/cuckoo-hashing] This ignores the cost of the shrinking operation if the table is too sparse.
[src:hopscotch-hashing/cuckoo-hashing] When inserting a new item with key x , the first step involves examining if slot $h_{1}(x)$ of table $T_{1}$ is occupied.
[src:hopscotch-hashing/cuckoo-hashing] If it is not, the item is inserted in that slot.
[src:hopscotch-hashing/cuckoo-hashing] However, if the slot is occupied, the existing item $x'$ is removed and x is inserted at $T_{1}[h_{1}(x)]$ .
[src:hopscotch-hashing/cuckoo-hashing] Then, $x'$ is inserted into table $T_{2}$ by following the same procedure.
[src:hopscotch-hashing/cuckoo-hashing] The process continues until an empty position is found to insert the key.
[src:hopscotch-hashing/cuckoo-hashing] To avoid an infinite loop, a threshold ${\text{Max-Loop}}$ is specified.
[src:hopscotch-hashing/cuckoo-hashing] If the number of iterations exceeds this fixed threshold, both $T_{1}$ and $T_{2}$ are rehashed with new hash functions and the insertion procedure repeats.
[src:hopscotch-hashing/cuckoo-hashing] On lines 10 and 15, the "cuckoo approach" of kicking other keys which occupy $T_{1,2}[h_{1,2}(x)]$ repeats until every key has its own "nest", i.e. item x is inserted into an empty slot in either of the two tables.
[src:hopscotch-hashing/cuckoo-hashing] The notation $x\leftrightarrow y$ expresses swapping x and y .
[src:hopscotch-hashing/cuckoo-hashing] Insertions succeed in expected constant time, even considering the possibility of having to rebuild the table, as long as the number of keys is kept below half of the capacity of the hash table, i.e., the load factor is below 50%.
[src:hopscotch-hashing/cuckoo-hashing] One method of proving this uses the theory of random graphs: one may form an undirected graph called the "cuckoo graph" that has a vertex for each hash table location, and an edge for each hashed value, with the endpoints of the edge being the two possible locations of the value.
[src:hopscotch-hashing/cuckoo-hashing] Then, the greedy insertion algorithm for adding a set of values to a cuckoo hash table succeeds if and only if the cuckoo graph for this set of values is a pseudoforest, a graph with at most one cycle in each of its connected components.
[src:hopscotch-hashing/cuckoo-hashing] Any vertex-induced subgraph with more edges than vertices corresponds to a set of keys for which there are an insufficient number of slots in the hash table.
[src:hopscotch-hashing/cuckoo-hashing] When the hash function is chosen randomly, the cuckoo graph is a random graph in the Erdős–Rényi model.
[src:hopscotch-hashing/cuckoo-hashing] With high probability, for load factor less than 1/2 (corresponding to a random graph in which the ratio of the number of edges to the number of vertices is bounded below 1/2), the graph is a pseudoforest and the cuckoo hashing algorithm succeeds in placing all keys.
[src:hopscotch-hashing/cuckoo-hashing] The same theory also proves that the expected size of a connected component of the cuckoo graph is small, ensuring that each insertion takes constant expected time.
[src:hopscotch-hashing/cuckoo-hashing] However, also with high probability, a load factor greater than 1/2 will lead to a giant component with two or more cycles, causing the data structure to fail and need to be resized.
[src:hopscotch-hashing/cuckoo-hashing] Since a theoretical random hash function requires too much space for practical usage, an important theoretical question is which practical hash functions suffice for Cuckoo hashing.
[src:hopscotch-hashing/cuckoo-hashing] One approach is to use k-independent hashing.
[src:hopscotch-hashing/cuckoo-hashing] In 2009 it was shown that $O(\log n)$ -independence suffices, and at least 6-independence is needed.
[src:hopscotch-hashing/cuckoo-hashing] Another approach is to use tabulation hashing, which is not 6-independent, but was shown in 2012 to have other properties sufficient for Cuckoo hashing.
[src:hopscotch-hashing/cuckoo-hashing] A third approach from 2014 is to slightly modify the cuckoo hashtable with a so-called stash, which makes it possible to use nothing more than 2-independent hash functions.
[src:hopscotch-hashing/cuckoo-hashing] In practice, cuckoo hashing is about 20–30% slower than linear probing, which is the fastest of the common approaches.
[src:hopscotch-hashing/cuckoo-hashing] The reason is that cuckoo hashing often causes two cache misses per search, to check the two locations where a key might be stored, while linear probing usually causes only one cache miss per search.
[src:hopscotch-hashing/cuckoo-hashing] However, because of its worst case guarantees on search time, cuckoo hashing can still be valuable when real-time response rates are required.
[src:hopscotch-hashing/cuckoo-hashing] The following two tables show the insertion of some example elements.
[src:hopscotch-hashing/cuckoo-hashing] Each column corresponds to the state of the two hash tables over time.
[src:hopscotch-hashing/cuckoo-hashing] The possible insertion locations for each new value are highlighted.
[src:hopscotch-hashing/cuckoo-hashing] The last column illustrates a failed insertion due to a cycle, details below.
[src:hopscotch-hashing/cuckoo-hashing] If you attempt to insert the element 45, then you get into a cycle, and fail.
[src:hopscotch-hashing/cuckoo-hashing] In the last row of the table we find the same initial situation as at the beginning again.
[src:hopscotch-hashing/cuckoo-hashing] Several variations of cuckoo hashing have been studied, primarily with the aim of improving its space usage by increasing the load factor that it can tolerate to a number greater than the 50% threshold of the basic algorithm.
[src:hopscotch-hashing/cuckoo-hashing] Some of these methods can also be used to reduce the failure rate of cuckoo hashing, causing rebuilds of the data structure to be much less frequent.
[src:hopscotch-hashing/cuckoo-hashing] Generalizations of cuckoo hashing that use more than two alternative hash functions can be expected to utilize a larger part of the capacity of the hash table efficiently while sacrificing some lookup and insertion speed.
[src:hopscotch-hashing/cuckoo-hashing] Using just three hash functions increases the load to 91%.
[src:hopscotch-hashing/cuckoo-hashing] Another generalization of cuckoo hashing called *blocked cuckoo hashing* uses more than one key per bucket and a balanced allocation scheme.
[src:hopscotch-hashing/cuckoo-hashing] Using just 2 keys per bucket permits a load factor above 80%.
[src:hopscotch-hashing/cuckoo-hashing] Another variation of cuckoo hashing that has been studied is *cuckoo hashing with a stash*.
[src:hopscotch-hashing/cuckoo-hashing] The stash, in this data structure, is an array of a constant number of keys, used to store keys that cannot successfully be inserted into the main hash table of the structure.
[src:hopscotch-hashing/cuckoo-hashing] This modification reduces the failure rate of cuckoo hashing to an inverse-polynomial function with an exponent that can be made arbitrarily large by increasing the stash size.
[src:hopscotch-hashing/cuckoo-hashing] However, larger stashes also mean slower searches for keys that are not present or are in the stash.
[src:hopscotch-hashing/cuckoo-hashing] A stash can be used in combination with more than two hash functions or with blocked cuckoo hashing to achieve both high load factors and small failure rates.
[src:hopscotch-hashing/cuckoo-hashing] The analysis of cuckoo hashing with a stash extends to practical hash functions, not just to the random hash function model commonly used in theoretical analysis of hashing.
[src:hopscotch-hashing/cuckoo-hashing] Some people recommend a simplified generalization of cuckoo hashing called skewed-associative cache in some CPU caches.
[src:hopscotch-hashing/cuckoo-hashing] Another variation of a cuckoo hash table, called a cuckoo filter, replaces the stored keys of a cuckoo hash table with much shorter fingerprints, computed by applying another hash function to the keys.
[src:hopscotch-hashing/cuckoo-hashing] In order to allow these fingerprints to be moved around within the cuckoo filter, without knowing the keys that they came from, the two locations of each fingerprint may be computed from each other by a bitwise exclusive or operation with the fingerprint, or with a hash of the fingerprint.
[src:hopscotch-hashing/cuckoo-hashing] However, it improves on a Bloom filter in multiple respects: its memory usage is smaller by a constant factor, it has better locality of reference, and (unlike Bloom filters) it allows for fast deletion of set elements with no additional storage penalty.
[src:hopscotch-hashing/cuckoo-hashing] A study by Zukowski et al. has shown that cuckoo hashing is much faster than chained hashing for small, cache-resident hash tables on modern processors.
[src:hopscotch-hashing/cuckoo-hashing] Kenneth Ross has shown bucketized versions of cuckoo hashing (variants that use buckets that contain more than one key) to be faster than conventional methods also for large hash tables, when space utilization is high.
[src:hopscotch-hashing/cuckoo-hashing] The performance of the bucketized cuckoo hash table was investigated further by Askitis, with its performance compared against alternative hashing schemes.
[src:hopscotch-hashing/cuckoo-hashing] A survey by Mitzenmacher presents open problems related to cuckoo hashing as of 2009.
[src:hopscotch-hashing/cuckoo-hashing] Cuckoo hashing is used in TikTok's recommendation system to solve the problem of "embedding table collisions", which can result in reduced model quality.
[src:hopscotch-hashing/cuckoo-hashing] The TikTok recommendation system "Monolith" takes advantage of cuckoo hashing's collision resolution to prevent different concepts from being mapped to the same vectors.
[src:hopscotch-hashing/hash-collision] In computer science, a **hash collision** or **hash clash** is when two distinct pieces of data in a hash table share the same hash value.
[src:hopscotch-hashing/hash-collision] The hash value in this case is derived from a hash function which takes a data input and returns a fixed length of bits.
[src:hopscotch-hashing/hash-collision] Although hash algorithms, especially cryptographic hash algorithms, have been created with the intent of being collision resistant, they can still sometimes map different data to the same hash (by virtue of the pigeonhole principle).
[src:hopscotch-hashing/hash-collision] Malicious users can take advantage of this to mimic, access, or alter data.
[src:hopscotch-hashing/hash-collision] Due to the possible negative applications of hash collisions in data management and computer security (in particular, cryptographic hash functions), collision avoidance has become an important topic in computer security.
[src:hopscotch-hashing/hash-collision] Hash collisions can be unavoidable depending on the number of objects in a set and whether or not the bit string they are mapped to is long enough in length.
[src:hopscotch-hashing/hash-collision] When there is a set of n objects, if n is greater than $|R|$ , which in this case R is the set of the hash values, a hash collision is guaranteed to occur.
[src:hopscotch-hashing/hash-collision] Another reason hash collisions are likely at some point in time stems from the idea of the birthday paradox in mathematics.
[src:hopscotch-hashing/hash-collision] This problem looks at the probability of a set of two randomly chosen people having the same birthday out of n number of people.
[src:hopscotch-hashing/hash-collision] This idea has led to what has been called the birthday attack.
[src:hopscotch-hashing/hash-collision] The premise of this attack is that it is difficult to find a birthday that specifically matches your birthday or a specific birthday, but the probability of finding a set of *any* two people with matching birthdays increases the probability greatly.
[src:hopscotch-hashing/hash-collision] Bad actors can use this approach to make it simpler for them to find hash values that collide with any other hash value – rather than searching for a specific value.
[src:hopscotch-hashing/hash-collision] The impact of collisions depends on the application.
[src:hopscotch-hashing/hash-collision] When hash functions and fingerprints are used to identify similar data, such as homologous DNA sequences or similar audio files, the functions are designed so as to *maximize* the probability of collision between distinct but similar data, using techniques like locality-sensitive hashing.
[src:hopscotch-hashing/hash-collision] Checksums, on the other hand, are designed to minimize the probability of collisions between similar inputs, without regard for collisions between very different inputs.
[src:hopscotch-hashing/hash-collision] Instances where bad actors attempt to create or find hash collisions are known as collision attacks.
[src:hopscotch-hashing/hash-collision] In practice, security-related applications use cryptographic hash algorithms, which are designed to be long enough for random matches to be unlikely, fast enough that they can be used anywhere, and safe enough that it would be extremely hard to find collisions.
[src:hopscotch-hashing/hash-collision] In hash tables, since hash collisions are inevitable, hash tables have mechanisms of dealing with them, known as collision resolutions.
[src:hopscotch-hashing/hash-collision] Two of the most common strategies are open addressing and separate chaining.
[src:hopscotch-hashing/hash-collision] The cache-conscious collision resolution is another strategy that has been discussed in the past for string hash tables.
[src:hopscotch-hashing/hash-collision] Cells in the hash table are assigned one of three states in this method – occupied, empty, or deleted.
[src:hopscotch-hashing/hash-collision] If a hash collision occurs, the table will be probed to move the record to an alternate cell that is stated as empty.
[src:hopscotch-hashing/hash-collision] There are different types of probing that take place when a hash collision happens and this method is implemented.
[src:hopscotch-hashing/hash-collision] Some types of probing are linear probing, double hashing, and quadratic probing.
[src:hopscotch-hashing/hash-collision] Open Addressing is also known as closed hashing.
[src:hopscotch-hashing/hash-collision] This strategy allows more than one record to be "chained" to the cells of a hash table.
[src:hopscotch-hashing/hash-collision] If two records are being directed to the same cell, both would go into that cell as a linked list.
[src:hopscotch-hashing/hash-collision] This efficiently prevents a hash collision from occurring since records with the same hash values can go into the same cell, but it has its disadvantages.
[src:hopscotch-hashing/hash-collision] Keeping track of so many lists is difficult and can cause whatever tool that is being used to become very slow.
[src:hopscotch-hashing/hash-collision] Separate chaining is also known as open hashing.
[src:hopscotch-hashing/hash-collision] Although much less used than the previous two, Askitis & Zobel (2005) has proposed the cache-conscious collision resolution method in 2005.
[src:hopscotch-hashing/hash-collision] It is a similar idea to the separate chaining methods, although it does not technically involve the chained lists.
[src:hopscotch-hashing/hash-collision] In this case, instead of chained lists, the hash values are represented in a contiguous list of items.
[src:hopscotch-hashing/hash-collision] This is better suited for string hash tables and the use for numeric values is still unknown.
[src:hopscotch-hashing/hash-table] In computer science, a **hash table** is a data structure that implements an associative array, also called a **dictionary** or simply **map**; an associative array is an abstract data type that maps keys to values.
[src:hopscotch-hashing/hash-table] A hash table uses a hash function to compute an *index*, also called a *hash code*, into an array of *buckets* or *slots*, from which the desired value can be found.
[src:hopscotch-hashing/hash-table] During lookup, the key is hashed and the resulting hash indicates where the corresponding value is stored.
[src:hopscotch-hashing/hash-table] A map implemented by a hash table is called a **hash map**.
[src:hopscotch-hashing/hash-table] Most hash table designs employ an imperfect hash function.
[src:hopscotch-hashing/hash-table] Hash collisions, where the hash function generates the same index for more than one key, therefore typically must be accommodated in some way.
[src:hopscotch-hashing/hash-table] Common strategies to handle hash collisions include chaining, which stores multiple elements in the same slot using linked lists, and open addressing, which searches for the next available slot according to a probing sequence.
[src:hopscotch-hashing/hash-table] In a well-dimensioned hash table, the average time complexity for each lookup is independent of the number of elements stored in the table.
[src:hopscotch-hashing/hash-table] Many hash table designs also allow arbitrary insertions and deletions of key–value pairs, at amortized constant average cost per operation.
[src:hopscotch-hashing/hash-table] Hashing is an example of a space–time tradeoff.
[src:hopscotch-hashing/hash-table] If memory is infinite, the entire key can be used directly as an index to locate its value with a single memory access.
[src:hopscotch-hashing/hash-table] On the other hand, if infinite time is available, values can be stored without regard for their keys, and a binary search or linear search can be used to retrieve the element.
[src:hopscotch-hashing/hash-table] In many situations, hash tables turn out to be on average more efficient than search trees or any other table lookup structure.
[src:hopscotch-hashing/hash-table] For this reason, they are widely used in many kinds of computer software, particularly for associative arrays, database indexing, caches, and sets.
[src:hopscotch-hashing/hash-table] Many programming languages provide built-in hash table structures, such as Python’s dictionaries, Java’s HashMap, C++’s unordered_map, Go maps, which abstract the complexity of hashing from the programmer.
[src:hopscotch-hashing/hash-table] The idea of hashing arose independently in different places.
[src:hopscotch-hashing/hash-table] In January 1953, Hans Peter Luhn wrote an internal IBM memorandum that used hashing with chaining.
[src:hopscotch-hashing/hash-table] The first example of open addressing was proposed by A.
[src:hopscotch-hashing/hash-table] Around the same time, Gene Amdahl, Elaine M.
[src:hopscotch-hashing/hash-table] McGraw, Nathaniel Rochester, and Arthur Samuel of IBM Research implemented hashing for the IBM 701 assembler.
[src:hopscotch-hashing/hash-table] Open addressing with linear probing is credited to Amdahl, although Andrey Ershov independently had the same idea.
[src:hopscotch-hashing/hash-table] The term "open addressing" was coined by W.
[src:hopscotch-hashing/hash-table] Wesley Peterson in his article which discusses the problem of search in large files.
[src:hopscotch-hashing/hash-table] The first published work on hashing with chaining is credited to Arnold Dumey, who discussed the idea of using remainder modulo a prime as a hash function.
[src:hopscotch-hashing/hash-table] The word "hashing" was first published in an article by Robert Morris.
[src:hopscotch-hashing/hash-table] A theoretical analysis of linear probing was submitted originally by Konheim and Weiss.
[src:hopscotch-hashing/hash-table] An associative array stores a set of (key, value) pairs and allows insertion, deletion, and lookup (search), with the constraint of unique keys.
[src:hopscotch-hashing/hash-table] In the hash table implementation of associative arrays, an array A of length m is partially filled with n elements, where $m\geq n$ .
[src:hopscotch-hashing/hash-table] A key **x** is hashed using a hash function h to compute an index location $A[h(x)]$ in the hash table, where $h(x)<m$ .
[src:hopscotch-hashing/hash-table] The efficiency of a hash table depends on the load factor, defined as the ratio of the number of stored elements to the number of available slots, with lower load factors generally yielding faster operations.
[src:hopscotch-hashing/hash-table] At this index, both the key and its associated value are stored.
[src:hopscotch-hashing/hash-table] Storing the key alongside the value ensures that lookups can verify the key at the index to retrieve the correct value, even in the presence of collisions.
[src:hopscotch-hashing/hash-table] Under reasonable assumptions, hash tables have better time complexity bounds on search, delete, and insert operations in comparison to self-balancing binary search trees.
[src:hopscotch-hashing/hash-table] Hash tables are also commonly used to implement sets, by omitting the stored value for each key and merely tracking whether the key is present.
[src:hopscotch-hashing/hash-table] The performance of the hash table deteriorates in relation to the load factor $\alpha$ .
[src:hopscotch-hashing/hash-table] In the limit of large m and n , each bucket statistically has a Poisson distribution with expectation $\lambda =\alpha$ for an ideally random hash function.
[src:hopscotch-hashing/hash-table] The software typically ensures that the load factor $\alpha$ remains below a certain constant, $\alpha _{\max }$ .
[src:hopscotch-hashing/hash-table] Therefore, a common approach is to resize or "rehash" the hash table whenever the load factor $\alpha$ reaches $\alpha _{\max }$ .
[src:hopscotch-hashing/hash-table] Similarly the table may also be resized if the load factor drops below $\alpha _{\max }/4$ .
[src:hopscotch-hashing/hash-table] With separate chaining hash tables, each slot of the bucket array stores a pointer to a list or array of data.
[src:hopscotch-hashing/hash-table] Separate chaining hash tables suffer gradually declining performance as the load factor grows, and no fixed point beyond which resizing is absolutely needed.
[src:hopscotch-hashing/hash-table] With separate chaining, the value of $\alpha _{\max }$ that gives best performance is typically between 1 and 3.
[src:hopscotch-hashing/hash-table] With open addressing, each slot of the bucket array holds exactly one item.
[src:hopscotch-hashing/hash-table] Therefore an open-addressed hash table cannot have a load factor greater than 1.
[src:hopscotch-hashing/hash-table] The performance of open addressing becomes very bad when the load factor approaches 1.
[src:hopscotch-hashing/hash-table] Therefore a hash table that uses open addressing *must* be resized or *rehashed* if the load factor $\alpha$ approaches 1.
[src:hopscotch-hashing/hash-table] With open addressing, acceptable figures of max load factor $\alpha _{\max }$ should range around 0.6 to 0.75.
[src:hopscotch-hashing/hash-table] A hash function $h:U\rightarrow \{0,...,m-1\}$ maps the universe U of keys to indices or slots within the table, that is, $h(x)\in \{0,...,m-1\}$ for $x\in U$ .
[src:hopscotch-hashing/hash-table] The conventional implementations of hash functions are based on the *integer universe assumption* that all elements of the table stem from the universe $U=\{0,...,u-1\}$ , where the bit length of u is confined within the word size of a computer architecture.
[src:hopscotch-hashing/hash-table] A hash function h is said to be perfect for a given set S if it is injective on S , that is, if each element $x\in S$ maps to a different value in ${0,...,m-1}$ .
[src:hopscotch-hashing/hash-table] A perfect hash function can be created if all the keys are known ahead of time.
[src:hopscotch-hashing/hash-table] The schemes of hashing used in *integer universe assumption* include hashing by division, hashing by multiplication, universal hashing, dynamic perfect hashing, and static perfect hashing.
[src:hopscotch-hashing/hash-table] However, hashing by division is the commonly used scheme.
[src:hopscotch-hashing/hash-table] The scheme in hashing by division is as follows: $h(x)\ =\ x\,{\bmod {\,}}m,$ where $h(x)$ is the hash value of $x\in S$ and m is the size of the table.
[src:hopscotch-hashing/hash-table] The scheme in hashing by multiplication is as follows: $h(x)=\lfloor m{\bigl (}(xA){\bmod {1}}{\bigr )}\rfloor$ Where A is a non-integer real-valued constant and m is the size of the table.
[src:hopscotch-hashing/hash-table] An advantage of the hashing by multiplication is that the m is not critical.
[src:hopscotch-hashing/hash-table] Although any value A produces a hash function, Donald Knuth suggests using the golden ratio.
[src:hopscotch-hashing/hash-table] Commonly a string is used as a key to the hash function.
[src:hopscotch-hashing/hash-table] The third edition of *The C++ Programming Language* describes a simple hash function in which an unsigned integer that is initially zero is repeatedly left shifted one bit and then xor'ed with the integer value of the next character.
[src:hopscotch-hashing/hash-table] This hash value is then taken modulo the table size.
[src:hopscotch-hashing/hash-table] If the left shift is not circular, then the string length should be at least eight bits less than the size of the unsigned integer in bits.
[src:hopscotch-hashing/hash-table] Another common way to hash a string to an integer is with a polynomial rolling hash function.
[src:hopscotch-hashing/hash-table] Uniform distribution of the hash values is a fundamental requirement of a hash function.
[src:hopscotch-hashing/hash-table] A non-uniform distribution increases the number of collisions and the cost of resolving them.
[src:hopscotch-hashing/hash-table] Uniformity is sometimes difficult to ensure by design, but may be evaluated empirically using statistical tests, e.g., a Pearson's chi-squared test for discrete uniform distributions.
[src:hopscotch-hashing/hash-table] The distribution needs to be uniform only for table sizes that occur in the application.
[src:hopscotch-hashing/hash-table] In particular, if one uses dynamic resizing with exact doubling and halving of the table size, then the hash function needs to be uniform only when the size is a power of two.
[src:hopscotch-hashing/hash-table] Here the index can be computed as some range of bits of the hash function.
[src:hopscotch-hashing/hash-table] On the other hand, some hashing algorithms prefer to have the size be a prime number.
[src:hopscotch-hashing/hash-table] For open addressing schemes, the hash function should also avoid *runs*, the mapping of two or more keys to consecutive slots.
[src:hopscotch-hashing/hash-table] Such runs may cause the lookup cost to skyrocket, even if the load factor is low and collisions are infrequent.
[src:hopscotch-hashing/hash-table] The popular multiplicative hash is claimed to have particularly poor run behavior.
[src:hopscotch-hashing/hash-table] K-independent hashing offers a way to prove a certain hash function does not have bad keysets for a given type of hashtable.
[src:hopscotch-hashing/hash-table] A number of K-independence results are known for collision resolution schemes such as linear probing and cuckoo hashing.
[src:hopscotch-hashing/hash-table] Since K-independence can prove a hash function works, one can then focus on finding the fastest possible such hash function.
[src:hopscotch-hashing/hash-table] A search algorithm that uses hashing consists of two parts.
[src:hopscotch-hashing/hash-table] The first part is computing a hash function which transforms the search key into an array index.
[src:hopscotch-hashing/hash-table] The ideal case is such that no two search keys hash to the same array index.
[src:hopscotch-hashing/hash-table] However, this is not always the case and impossible to guarantee for unseen given data.
[src:hopscotch-hashing/hash-table] Hence the second part of the algorithm is collision resolution.
[src:hopscotch-hashing/hash-table] The two common methods for collision resolution are separate chaining and open addressing.
[src:hopscotch-hashing/hash-table] In separate chaining, the process involves building a linked list with key–value pair for each search array index.
[src:hopscotch-hashing/hash-table] The collided items are chained together through a single linked list, which can be traversed to access the item with a unique search key.
[src:hopscotch-hashing/hash-table] Collision resolution through chaining with linked list is a common method of implementation of hash tables.
[src:hopscotch-hashing/hash-table] If the element is comparable either numerically or lexically, and inserted into the list by maintaining the total order, it results in faster termination of the unsuccessful searches.
[src:hopscotch-hashing/hash-table] If the keys are ordered, it could be efficient to use "self-organizing" concepts such as using a self-balancing binary search tree, through which the theoretical worst case could be brought down to $O(\log {n})$ , although it introduces additional complexities.
[src:hopscotch-hashing/hash-table] In dynamic perfect hashing, two-level hash tables are used to reduce the look-up complexity to be a guaranteed $O(1)$ in the worst case.
[src:hopscotch-hashing/hash-table] In this technique, the buckets of k entries are organized as perfect hash tables with $k^{2}$ slots providing constant worst-case lookup time, and low amortized time for insertion.
[src:hopscotch-hashing/hash-table] A study shows array-based separate chaining to be 97% more performant when compared to the standard linked list method under heavy load.
[src:hopscotch-hashing/hash-table] Techniques such as using fusion tree for each buckets also result in constant time for all operations with high probability.
[src:hopscotch-hashing/hash-table] The linked list of separate chaining implementation may not be cache-conscious due to spatial locality—locality of reference—when the nodes of the linked list are scattered across memory, thus the list traversal during insert and search may entail CPU cache inefficiencies.
[src:hopscotch-hashing/hash-table] Open addressing is another collision resolution technique in which every entry record is stored in the bucket array itself, and the hash resolution is performed through **probing**.
[src:hopscotch-hashing/hash-table] When a new entry has to be inserted, the buckets are examined, starting with the hashed-to slot and proceeding in some *probe sequence*, until an unoccupied slot is found.
[src:hopscotch-hashing/hash-table] When searching for an entry, the buckets are scanned in the same sequence, until either the target record is found, or an unused array slot is found, which indicates an unsuccessful search.
[src:hopscotch-hashing/hash-table] The performance of open addressing may be slower compared to separate chaining since the probe sequence increases when the load factor $\alpha$ approaches 1.
[src:hopscotch-hashing/hash-table] The probing results in an infinite loop if the load factor reaches 1, in the case of a completely filled table.
[src:hopscotch-hashing/hash-table] The average cost of linear probing depends on the hash function's ability to distribute the elements uniformly throughout the table to avoid runs, since formation of runs would result in increased search time.
[src:hopscotch-hashing/hash-table] Since the slots are located in successive locations, linear probing could lead to better utilization of CPU cache due to locality of references resulting in reduced memory latency.
[src:hopscotch-hashing/hash-table] Coalesced hashing is a hybrid of both separate chaining and open addressing in which the buckets or nodes link within the table.
[src:hopscotch-hashing/hash-table] The algorithm is ideally suited for fixed memory allocation.
[src:hopscotch-hashing/hash-table] The collision in coalesced hashing is resolved by identifying the largest-indexed empty slot on the hash table, then the colliding value is inserted into that slot.
[src:hopscotch-hashing/hash-table] The bucket is also linked to the inserted node's slot which contains its colliding hash address.
[src:hopscotch-hashing/hash-table] Cuckoo hashing is a form of open addressing collision resolution technique which guarantees $O(1)$ worst-case lookup complexity and constant amortized time for insertions.
[src:hopscotch-hashing/hash-table] The collision is resolved through maintaining two hash tables, each having its own hashing function, and collided slot gets replaced with the given item, and the preoccupied element of the slot gets displaced into the other hash table.
[src:hopscotch-hashing/hash-table] The process continues until every key has its own spot in the empty buckets of the tables; if the procedure enters into infinite loop—which is identified through maintaining a threshold loop counter—both hash tables get rehashed with newer hash functions and the procedure continues.
[src:hopscotch-hashing/hash-table] Hopscotch hashing is an open addressing based algorithm which combines the elements of cuckoo hashing, linear probing and chaining through the notion of a *neighbourhood* of buckets—the subsequent buckets around any given occupied bucket, also called a "virtual" bucket.
[src:hopscotch-hashing/hash-table] The algorithm is designed to deliver better performance when the load factor of the hash table grows beyond 90%; it also provides high throughput in concurrent settings, thus well suited for implementing resizable concurrent hash table.
[src:hopscotch-hashing/hash-table] Each bucket within the hash table includes an additional "hop-information"—an *H*-bit bit array for indicating the relative distance of the item which was originally hashed into the current virtual bucket within *H* − 1 entries.
[src:hopscotch-hashing/hash-table] *H* − 1, subsequent swap and hop-info bit array manipulation of each bucket is performed in accordance with its neighbourhood invariant properties.
[src:hopscotch-hashing/hash-table] Robin Hood hashing is an open addressing based collision resolution algorithm; the collisions are resolved through favouring the displacement of the element that is farthest—or longest *probe sequence length* (PSL)—from its "home location" i.e. the bucket to which the item was hashed into.
[src:hopscotch-hashing/hash-table] It is named after Robin Hood, a mythical heroic outlaw who stole from the rich to give to the poor.
[src:hopscotch-hashing/hash-table] Although Robin Hood hashing does not change the theoretical search cost, it significantly affects the variance of the distribution of the items on the buckets, i.e. dealing with long run formation in the hash table.
[src:hopscotch-hashing/hash-table] Each node within the hash table that uses Robin Hood hashing should be augmented to store an extra PSL value.
[src:hopscotch-hashing/hash-table] If a hash table becomes "too empty" after deleting some elements, resizing may be performed to avoid excessive memory usage.
[src:hopscotch-hashing/hash-table] Generally, a new hash table with a size double that of the original hash table gets allocated privately and every item in the original hash table gets moved to the newly allocated one by computing the hash values of the items followed by the insertion operation.
[src:hopscotch-hashing/hash-table] Rehashing is simple, but computationally expensive.
[src:hopscotch-hashing/hash-table] Some hash table implementations, notably in real-time systems, cannot pay the price of enlarging the hash table all at once, because it may interrupt time-critical operations.
[src:hopscotch-hashing/hash-table] If one cannot avoid dynamic resizing, a solution is to perform the resizing gradually to avoid storage blip—typically at 50% of new table's size—during rehashing and to avoid memory fragmentation that triggers heap compaction due to deallocation of large memory blocks caused by the old hash table.
[src:hopscotch-hashing/hash-table] In such case, the rehashing operation is done incrementally through extending prior memory block allocated for the old hash table such that the buckets of the hash table remain unaltered.
[src:hopscotch-hashing/hash-table] A common approach for amortized rehashing involves maintaining two hash functions $h_{\text{old}}$ and $h_{\text{new}}$ .
[src:hopscotch-hashing/hash-table] Linear hashing is an implementation of the hash table which enables dynamic growths or shrinks of the table one bucket at a time.
[src:hopscotch-hashing/hash-table] The performance of a hash table is dependent on the hash function's ability in generating quasi-random numbers ( $\sigma$ ) for entries in the hash table where K , n and $h(x)$ denotes the key, number of buckets and the hash function such that $\sigma \ =\ h(K)\ \%\ n$ .
[src:hopscotch-hashing/hash-table] If the hash function generates the same $\sigma$ for distinct keys ( $K_{1}\neq K_{2},\ h(K_{1})\ =\ h(K_{2})$ ), this results in *collision*, which is dealt with in a variety of ways.
[src:hopscotch-hashing/hash-table] The constant time complexity ( $O(1)$ ) of the operation in a hash table is presupposed on the condition that the hash function doesn't generate colliding indices; thus, the performance of the hash table is directly proportional to the chosen hash function's ability to disperse the indices.
[src:hopscotch-hashing/hash-table] However, construction of such a hash function is practically infeasible, that being so, implementations depend on case-specific collision resolution techniques in achieving higher performance.
[src:hopscotch-hashing/hash-table] The best performance is obtained in the case that the hash function distributes the elements of the universe uniformaly, and the elements stored at the table are drawn at random from the universe.
[src:hopscotch-hashing/hash-table] In this case, in hashing with chaining, the expected time for a successful search is ${\textstyle 1+{\frac {\alpha }{2}}+\Theta \left({\frac {1}{m}}\right)}$ , and the expected time for an unsuccessful search is ${\textstyle e^{-\alpha }+\alpha +\Theta \left({\frac {1}{m}}\right)}$ .
[src:hopscotch-hashing/hash-table] Hash tables are commonly used to implement many types of in-memory tables.
[src:hopscotch-hashing/hash-table] They are used to implement associative arrays.
[src:hopscotch-hashing/hash-table] Hash tables may also be used as disk-based data structures and database indices (such as in dbm) although B-trees are more popular in these applications.
[src:hopscotch-hashing/hash-table] Hash tables can be used to implement caches, auxiliary data tables that are used to speed up the access to data that is primarily stored in slower media.
[src:hopscotch-hashing/hash-table] In this application, hash collisions can be handled by discarding one of the two colliding entries—usually erasing the old item that is currently stored in the table and overwriting it with the new item, so every item in the table has a unique hash value.
[src:hopscotch-hashing/hash-table] Hash tables can be used in the implementation of set data structure, which can store unique values without any particular order; set is typically used in testing the membership of a value in the collection, rather than element retrieval.
[src:hopscotch-hashing/hash-table] A transposition table to a complex Hash Table which stores information about each section that has been searched.
[src:hopscotch-hashing/hash-table] Many programming languages provide hash table functionality, either as built-in associative arrays or as standard library modules.
[src:hopscotch-hashing/hopscotch-hashing] The algorithm uses a single array of *n* buckets.
[src:hopscotch-hashing/hopscotch-hashing] For each bucket, its *neighborhood* is a small collection of *H* consecutive buckets (i.e. ones with indices close to the original hashed bucket).
[src:hopscotch-hashing/hopscotch-hashing] The desired property of the neighborhood is that the cost of finding an item in the buckets of the neighborhood is close to the cost of finding it in the bucket itself (for example, by having buckets in the neighborhood fall within the same cache line).
[src:hopscotch-hashing/hopscotch-hashing] The size of the neighborhood must be sufficient to accommodate a logarithmic number of items in the worst case (i.e. it must accommodate log(*n*) items), but only a constant number on average.
[src:hopscotch-hashing/hopscotch-hashing] If some bucket's neighborhood is filled, the table is resized.
[src:hopscotch-hashing/hopscotch-hashing] In hopscotch hashing, as in cuckoo hashing, and unlike in linear probing, a given item will always be inserted-into and found-in the neighborhood of its hashed bucket.
[src:hopscotch-hashing/hopscotch-hashing] In other words, it will always be found either in its original hashed array entry, or in one of the next *H*−1 neighboring entries.
[src:hopscotch-hashing/hopscotch-hashing] *H* could, for example, be 32, a common machine word size.
[src:hopscotch-hashing/hopscotch-hashing] The neighborhood is thus a "virtual" bucket that has fixed size and overlaps with the following *H*−1 buckets.
[src:hopscotch-hashing/hopscotch-hashing] To speed the search, each bucket (array entry) includes a "hop-information" word, an *H*-bit bitmap that indicates which of the next *H*−1 entries contain items that hashed to the current entry's virtual bucket.
[src:hopscotch-hashing/hopscotch-hashing] In this way, an item can be found quickly by looking at the word to see which entries belong to the bucket, and then scanning through the constant number of entries (most modern processors support special bit manipulation operations that make the lookup in the "hop-information" bitmap very fast).
[src:hopscotch-hashing/hopscotch-hashing] Starting at entry *i*, use a linear probe to find an empty entry at index *j*.
[src:hopscotch-hashing/hopscotch-hashing] If no such item *y* exists within the range, the table is full.
[src:hopscotch-hashing/hopscotch-hashing] Move *y* to *j*, creating a new empty slot closer to *i*.
[src:hopscotch-hashing/hopscotch-hashing] Set *j* to the empty slot vacated by *y* and repeat.
[src:hopscotch-hashing/hopscotch-hashing] The idea is that hopscotch hashing "moves the empty slot towards the desired bucket".
[src:hopscotch-hashing/hopscotch-hashing] To remove an item from the table, one simply removes it from the table entry.
[src:hopscotch-hashing/hopscotch-hashing] If the neighborhood buckets are cache aligned, then one could apply a reorganization operation in which items are moved into the now vacant location in order to improve alignment.
[src:hopscotch-hashing/hopscotch-hashing] One advantage of hopscotch hashing is that it provides good performance at very high table load factors, even ones exceeding 0.9.
[src:hopscotch-hashing/hopscotch-hashing] Part of this efficiency is due to using a linear probe only to find an empty slot during insertion, not for every lookup as in the original linear probing hash table algorithm.
[src:hopscotch-hashing/hopscotch-hashing] Another advantage is that one can use any hash function, in particular simple ones that are close to universal.
[src:hopscotch-hashing/hopscotch-hashing] The paper also introduces several variants of the hopscotch hashing scheme.
[src:hopscotch-hashing/hopscotch-hashing] An advanced approach uses a pointer scheme to implement the hop information word (in the basic case this is the hop information bit-map).
[src:hopscotch-hashing/hopscotch-hashing] This allows for the hop information word to be of arbitrary (but fixed) size.
[src:hopscotch-hashing/hopscotch-hashing] While the basic case and the advanced approach are designed to be sequential, there also is a concurrent variant for each of them.
[src:hopscotch-hashing/hopscotch-hashing] A lock-free variant was introduced by Robert Kelly, Barak A.
[src:hopscotch-hashing/linear-probing] Along with quadratic probing and double hashing, linear probing is a form of open addressing.
[src:hopscotch-hashing/linear-probing] In these schemes, each cell of a hash table stores a single key–value pair.
[src:hopscotch-hashing/linear-probing] When the hash function causes a collision by mapping a new key to a cell of the hash table that is already occupied by another key, linear probing searches the table for the closest following free location and inserts the new key there.
[src:hopscotch-hashing/linear-probing] Lookups are performed in the same way, by searching the table sequentially starting at the position given by the hash function, until finding a cell with a matching key or an empty cell.
[src:hopscotch-hashing/linear-probing] It takes constant expected time per search, insertion, or deletion when implemented using a random hash function, a 5-independent hash function, or tabulation hashing.
[src:hopscotch-hashing/linear-probing] Good results can also be achieved in practice with other hash functions such as MurmurHash.
[src:hopscotch-hashing/linear-probing] Linear probing is a component of open addressing schemes for using a hash table to solve the dictionary problem.
[src:hopscotch-hashing/linear-probing] In the dictionary problem, a data structure should maintain a collection of key–value pairs subject to operations that insert or delete pairs from the collection or that search for the value associated with a given key.
[src:hopscotch-hashing/linear-probing] In open addressing solutions to this problem, the data structure is an array T (the hash table) whose cells *T*[*i*] (when nonempty) each store a single key–value pair.
[src:hopscotch-hashing/linear-probing] A hash function is used to map each key into the cell of T where that key should be stored, typically scrambling the keys so that keys with similar values are not placed near each other in the table.
[src:hopscotch-hashing/linear-probing] A hash collision occurs when the hash function maps a key into a cell that is already occupied by a different key.
[src:hopscotch-hashing/linear-probing] Linear probing is a strategy for resolving collisions, by placing the new key into the closest following empty cell.
[src:hopscotch-hashing/linear-probing] To search for a given key x, the cells of T are examined, beginning with the cell at index *h*(*x*) (where h is the hash function) and continuing to the adjacent cells *h*(*x*) + 1, *h*(*x*) + 2, ..., until finding either an empty cell or a cell whose stored key is x.
[src:hopscotch-hashing/linear-probing] If a cell containing the key is found, the search returns the value from that cell.
[src:hopscotch-hashing/linear-probing] Otherwise, if an empty cell is found, the key cannot be in the table, because it would have been placed in that cell in preference to any later cell that has not yet been searched.
[src:hopscotch-hashing/linear-probing] In this case, the search returns as its result that the key is not present in the dictionary.
[src:hopscotch-hashing/linear-probing] To insert a key–value pair (*x*,*v*) into the table (possibly replacing any existing pair with the same key), the insertion algorithm follows the same sequence of cells that would be followed for a search, until finding either an empty cell or a cell whose stored key is x.
[src:hopscotch-hashing/linear-probing] The new key–value pair is then placed into that cell.
[src:hopscotch-hashing/linear-probing] If the insertion would cause the load factor of the table (its fraction of occupied cells) to grow above some preset threshold, the whole table may be replaced by a new table, larger by a constant factor, with a new hash function, as in a dynamic array.
[src:hopscotch-hashing/linear-probing] Setting this threshold close to zero and using a high growth rate for the table size leads to faster hash table operations but greater memory usage than threshold values close to one and low growth rates.
[src:hopscotch-hashing/linear-probing] A common choice would be to double the table size when the load factor would exceed 1/2, causing the load factor to stay between 1/4 and 1/2.
[src:hopscotch-hashing/linear-probing] It is also possible to remove a key–value pair from the dictionary.
[src:hopscotch-hashing/linear-probing] However, it is not sufficient to do so by simply emptying its cell.
[src:hopscotch-hashing/linear-probing] This would affect searches for other keys that have a hash value earlier than the emptied cell, but that are stored in a position later than the emptied cell.
[src:hopscotch-hashing/linear-probing] The emptied cell would cause those searches to incorrectly report that the key is not present.
[src:hopscotch-hashing/linear-probing] Instead, when a cell i is emptied, it is necessary to search forward through the following cells of the table until finding either another empty cell or a key that can be moved to cell i (that is, a key whose hash value is equal to or earlier than i).
[src:hopscotch-hashing/linear-probing] When an empty cell is found, then emptying cell i is safe and the deletion process terminates.
[src:hopscotch-hashing/linear-probing] But, when the search finds a key that can be moved to cell i, it performs this move.
[src:hopscotch-hashing/linear-probing] This has the effect of speeding up later searches for the moved key, but it also empties out another cell, later in the same block of occupied cells.
[src:hopscotch-hashing/linear-probing] The search for a movable key continues for the new emptied cell, in the same way, until it terminates by reaching a cell that was already empty.
[src:hopscotch-hashing/linear-probing] In this process of moving keys to earlier cells, each key is examined only once.
[src:hopscotch-hashing/linear-probing] Therefore, the time to complete the whole process is proportional to the length of the block of occupied cells containing the deleted key, matching the running time of the other hash table operations.
[src:hopscotch-hashing/linear-probing] Alternatively, it is possible to use a lazy deletion strategy in which a key–value pair is removed by replacing the value by a special flag value indicating a deleted key.
[src:hopscotch-hashing/linear-probing] However, these flag values will contribute to the load factor of the hash table.
[src:hopscotch-hashing/linear-probing] With this strategy, it may become necessary to clean the flag values out of the array and rehash all the remaining key–value pairs once too large a fraction of the array becomes occupied by deleted keys.
[src:hopscotch-hashing/linear-probing] Linear probing provides good locality of reference, which causes it to require few uncached memory accesses per operation.
[src:hopscotch-hashing/linear-probing] Because of this, for low to moderate load factors, it can provide very high performance.
[src:hopscotch-hashing/linear-probing] However, compared to some other open addressing strategies, its performance degrades more quickly at high load factors because of primary clustering, a tendency for one collision to cause more nearby collisions.
[src:hopscotch-hashing/linear-probing] Additionally, achieving good performance with this method requires a higher-quality hash function than for some other collision resolution schemes.
[src:hopscotch-hashing/linear-probing] Using linear probing, dictionary operations can be implemented in constant expected time.
[src:hopscotch-hashing/linear-probing] In other words, insert, remove and search operations can be implemented in O(1), as long as the load factor of the hash table is a constant strictly less than one.
[src:hopscotch-hashing/linear-probing] In more detail, the time for any particular operation (a search, insertion, or deletion) is proportional to the length of the contiguous block of occupied cells at which the operation starts.
[src:hopscotch-hashing/linear-probing] If all starting cells are equally likely, in a hash table with N cells, then a maximal block of k occupied cells will have probability *k*/*N* of containing the starting location of a search, and will take time *O*(*k*) whenever it is the starting location.
[src:hopscotch-hashing/linear-probing] Therefore, the expected time for an operation can be calculated as the product of these two terms, *O*(*k*2/*N*), summed over all of the maximal blocks of contiguous cells in the table.
[src:hopscotch-hashing/linear-probing] This formula can be simplified by replacing Block(*i*,*k*) by a simpler necessary condition Full(*k*), the event that at least k elements have hash values that lie within a block of cells of length k.
[src:hopscotch-hashing/linear-probing] After this replacement, the value within the sum no longer depends on i, and the 1/*N* factor cancels the N terms of the outer summation.
[src:hopscotch-hashing/linear-probing] But by the multiplicative form of the Chernoff bound, when the load factor is bounded away from one, the probability that a block of length k contains at least k hashed values is exponentially small as a function of k, causing this sum to be bounded by a constant independent of n.
[src:hopscotch-hashing/linear-probing] It is also possible to perform the same analysis using Stirling's approximation instead of the Chernoff bound to estimate the probability that a block contains exactly k hashed values.
[src:hopscotch-hashing/linear-probing] In terms of the load factor α, the expected time to perform a successful search on a random key is *O*(1 + 1/(1 − *α*)), and the expected time to perform an unsuccessful search (or the insertion of a new key) is *O*(1 + 1/(1 − *α*)2).
[src:hopscotch-hashing/linear-probing] For constant load factors, with high probability, the longest probe sequence (among the probe sequences for all keys stored in the table) has logarithmic length.
[src:hopscotch-hashing/linear-probing] Linear probing hash tables suffer from a problem known as primary clustering, in which elements to group together into long contiguous runs.
[src:hopscotch-hashing/linear-probing] In terms of the load factor α, the expected length of the run containing a given element is $\Theta (1+1/(1-\alpha )^{2})$ .
[src:hopscotch-hashing/linear-probing] This is why insertions take time $\Theta (1+1/(1-\alpha )^{2})$ , as opposed to the running time of $\Theta (1+1/(1-\alpha ))$ achieved by other open-addressed hash tables such as uniform probing and double hashing.
[src:hopscotch-hashing/linear-probing] Primary clustering also affects unsuccessful searches, since like insertions, they must travel to the end of a run.
[src:hopscotch-hashing/linear-probing] Some variations of linear probing are able to achieve better bounds for unsuccessful searches and insertions, by using techniques that reduce primary clustering.
[src:hopscotch-hashing/linear-probing] Because linear probing is especially sensitive to unevenly distributed hash values, it is important to combine it with a high-quality hash function that does not produce such irregularities.
[src:hopscotch-hashing/linear-probing] The analysis above assumes that each key's hash is a random number independent of the hashes of all the other keys.
[src:hopscotch-hashing/linear-probing] This assumption is unrealistic for most applications of hashing.
[src:hopscotch-hashing/linear-probing] However, random or pseudorandom hash values may be used when hashing objects by their identity rather than by their value.
[src:hopscotch-hashing/linear-probing] For instance, this is done using linear probing by the IdentityHashMap class of the Java collections framework.
[src:hopscotch-hashing/linear-probing] The hash value that this class associates with each object, its identityHashCode, is guaranteed to remain fixed for the lifetime of an object but is otherwise arbitrary.
[src:hopscotch-hashing/linear-probing] Because the identityHashCode is constructed only once per object, and is not required to be related to the object's address or value, its construction may involve slower computations such as the call to a random or pseudorandom number generator.
[src:hopscotch-hashing/linear-probing] For instance, Java 8 uses an Xorshift pseudorandom number generator to construct these values.
[src:hopscotch-hashing/linear-probing] For most applications of hashing, it is necessary to compute the hash function for each value every time that it is hashed, rather than once when its object is created.
[src:hopscotch-hashing/linear-probing] In such applications, random or pseudorandom numbers cannot be used as hash values, because then different objects with the same value would have different hashes.
[src:hopscotch-hashing/linear-probing] And cryptographic hash functions (which are designed to be computationally indistinguishable from truly random functions) are usually too slow to be used in hash tables.
[src:hopscotch-hashing/linear-probing] Instead, other methods for constructing hash functions have been devised.
[src:hopscotch-hashing/linear-probing] These methods compute the hash function quickly, and can be proven to work well with linear probing.
[src:hopscotch-hashing/linear-probing] In particular, linear probing has been analyzed from the framework of k-independent hashing, a class of hash functions that are initialized from a small random seed and that are equally likely to map any k-tuple of distinct keys to any k-tuple of indexes.
[src:hopscotch-hashing/linear-probing] The parameter k can be thought of as a measure of hash function quality: the larger k is, the more time it will take to compute the hash function but it will behave more similarly to completely random functions.
[src:hopscotch-hashing/linear-probing] For linear probing, 5-independence is enough to guarantee constant expected time per operation, while some 4-independent hash functions perform badly, taking up to logarithmic time per operation.
[src:hopscotch-hashing/linear-probing] Another method of constructing hash functions with both high quality and practical speed is tabulation hashing.
[src:hopscotch-hashing/linear-probing] In this method, the hash value for a key is computed by using each byte of the key as an index into a table of random numbers (with a different table for each byte position).
[src:hopscotch-hashing/linear-probing] The numbers from those table cells are then combined by a bitwise exclusive or operation.
[src:hopscotch-hashing/linear-probing] Hash functions constructed this way are only 3-independent.
[src:hopscotch-hashing/linear-probing] Nevertheless, linear probing using these hash functions takes constant expected time per operation.
[src:hopscotch-hashing/linear-probing] Both tabulation hashing and standard methods for generating 5-independent hash functions are limited to keys that have a fixed number of bits.
[src:hopscotch-hashing/linear-probing] To handle strings or other types of variable-length keys, it is possible to compose a simpler universal hashing technique that maps the keys to intermediate values and a higher quality (5-independent or tabulation) hash function that maps the intermediate values to hash table indices.
[src:hopscotch-hashing/linear-probing] They point out that each table look-up require several cycles, being more expensive than simple arithmetic operations.
[src:hopscotch-hashing/linear-probing] They also found MurmurHash to be superior than tabulation hashing: "By studying the results provided by Mult and Murmur, we think that the trade-off for by tabulation (...) is less attractive in practice".
[src:hopscotch-hashing/linear-probing] The idea of an associative array that allows data to be accessed by its value rather than by its address dates back to the mid-1940s in the work of Konrad Zuse and Vannevar Bush, but hash tables were not described until 1953, in an IBM memorandum by Hans Peter Luhn.
[src:hopscotch-hashing/linear-probing] Luhn used a different collision resolution method, chaining, rather than linear probing.
[src:hopscotch-hashing/linear-probing] Knuth (1963) summarizes the early history of linear probing.
[src:hopscotch-hashing/linear-probing] It was the first open addressing method, and was originally synonymous with open addressing.
[src:hopscotch-hashing/linear-probing] According to Knuth, it was first used by Gene Amdahl, Elaine M.
[src:hopscotch-hashing/linear-probing] McGraw (née Boehme), and Arthur Samuel in 1954, in an assembler program for the IBM 701 computer.
[src:hopscotch-hashing/linear-probing] The first published description of linear probing is by Peterson (1957), who also credits Samuel, Amdahl, and Boehme but adds that "the system is so natural, that it very likely may have been conceived independently by others either before or since that time".
[src:hopscotch-hashing/linear-probing] Another early publication of this method was by Soviet researcher Andrey Ershov, in 1958.
[src:hopscotch-hashing/linear-probing] The first theoretical analysis of linear probing, showing that it takes constant expected time per operation with random hash functions, was given by Knuth.
[src:hopscotch-hashing/linear-probing] Sedgewick calls Knuth's work "a landmark in the analysis of algorithms".
[src:hopscotch-hashing/linear-probing] Knuth's original analysis was never formally published, and the first published analysis was due to Konheim and Weiss in 1966.
[src:hopscotch-hashing/linear-probing] Knuth's analysis disproved an earlier conjecture by Peterson that, at load factor $\alpha =1-\varepsilon$ , the expected time per insertion should be $\Theta (1+\varepsilon ^{-1})$ .
[src:hopscotch-hashing/linear-probing] Knuth showed instead that the time per operation grows as at a rate of $\Theta (1+\varepsilon ^{-2})$ .
[src:hopscotch-hashing/linear-probing] This effect, in which the insertion time grows quadratically faster than one might intuitively guess, became known as primary clustering.
[src:hopscotch-hashing/linear-probing] Subsequent work has introduced several algorithmic techniques to reduce primary clustering, including a variation of linear probing known as graveyard hashing that achieves the ideal bound of $\Theta (1+\varepsilon ^{-1})$ .
[src:hopscotch-hashing/linear-probing] Other later developments include a more detailed analysis of the probability distribution of the running time, amortized analyses for workloads with both insertions and deletions, and analyses of linear probing using explicit families of hash functions.
[src:hopscotch-hashing/open-addressing] in which the interval between probes is fixed — often set to 1.
[src:hopscotch-hashing/open-addressing] The main trade offs between these methods are that linear probing has the best cache performance but is most sensitive to clustering, while double hashing has poor cache performance but exhibits virtually no clustering; quadratic probing falls in between in both areas.
[src:hopscotch-hashing/open-addressing] Double hashing can also require more computation than other forms of probing.
[src:hopscotch-hashing/open-addressing] Some open addressing methods, such as Hopscotch hashing, Robin Hood hashing, last-come-first-served hashing and cuckoo hashing move existing keys around in the array to make room for the new key.
[src:hopscotch-hashing/open-addressing] This gives better maximum search times than the methods based on probing.
[src:hopscotch-hashing/open-addressing] A critical influence on performance of an open addressing hash table is the *load factor*; that is, the proportion of the slots in the array that are used.
[src:hopscotch-hashing/open-addressing] As the load factor increases towards 100%, the number of probes that may be required to find or insert a given key rises dramatically.
[src:hopscotch-hashing/open-addressing] Once the table becomes full, probing algorithms may even fail to terminate.
[src:hopscotch-hashing/open-addressing] Even with good hash functions, load factors are normally limited to 80%.
[src:hopscotch-hashing/open-addressing] A poor hash function can exhibit poor performance even at very low load factors by generating significant clustering, especially with the simplest linear addressing method.
[src:hopscotch-hashing/open-addressing] Generally typical load factors with most open addressing methods are 50%, while separate chaining typically can use up to 100%.
[src:hopscotch-hashing/open-addressing] The following pseudocode is an implementation of an open addressing hash table with linear probing and single-slot stepping, a common approach that is effective if the hash function is good.
[src:hopscotch-hashing/open-addressing] Each of the **lookup**, **set** and **remove** functions use a common internal function **find_slot** to locate the array slot that either does or should contain a given key.
[src:hopscotch-hashing/open-addressing] operation to insert all the elements of the old array into the new larger array.
[src:hopscotch-hashing/open-addressing] , for example by doubling the old array size.
[src:hopscotch-hashing/open-addressing] For all records in a cluster, there must be no vacant slots between their natural hash position and their current position (else lookups will terminate before finding the record).
[src:hopscotch-hashing/open-addressing] is a vacant slot that might be invalidating this property for subsequent records in the cluster.
[src:hopscotch-hashing/open-addressing] would naturally land in the hash table if there were no collisions.
[src:hopscotch-hashing/open-addressing] Another technique for removal is simply to mark the slot as deleted.
[src:hopscotch-hashing/open-addressing] However this eventually requires rebuilding the table simply to remove deleted records.
[src:hopscotch-hashing/open-addressing] The methods above provide *O*(1) updating and removal of existing records, with occasional rebuilding if the high-water mark of the table size grows.
[src:hopscotch-hashing/open-addressing] The *O*(1) remove method above is only possible in linearly probed hash tables with single-slot stepping.
[src:hopscotch-hashing/open-addressing] In the case where many records are to be deleted in one operation, marking the slots for deletion and later rebuilding may be more efficient.
[src:hopscotch-hashing/open-addressing] Here *n* is the number of elements in the table, *m* the table size, $\alpha =n/m$ the load factor, $U_{n}$ the number of probes in an unsuccessful search and $S_{n}$ the number of probes in a successful search.
[src:hopscotch-hashing/open-addressing] Note that the expectation deteriorates to infinity when the load factor approaches 1.
